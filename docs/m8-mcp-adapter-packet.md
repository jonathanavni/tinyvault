# M8 — MCP stdio adapter: plan-mode packet (🔴 security-core slice, full ladder)

**Revision 5.2 — LOCKED by the user on 2026-09-12** (`PLAN.md` Decisions Log "USER DECISIONS on the M8 packet (rev 5.2)"; register "M8 packet rev 5.2 LOCKED"). **Implementation is authorized to the incoming GPT-6 Astra continuity owner through §6's ladder, from a fresh Codex session — not from the Claude session that wrote this packet; handoff: `docs/m8-astra-implementation-handoff.md`.** The measured compatibility decisions (§3.4, fact 16) and the stated limitation (§3.11 (5)) are accepted as written; the metadata compatibility choices and their conformance uncertainty (§3.4 step 5) are preserved exactly; any contract conflict discovered during implementation is surfaced for the user's disposition (§5). No merge, push, additional cohort or public flip is authorized by the lock. *(5.1 = rev 5 plus the owner's dispositions of the round-1 Sol delta review — five P1 / two P2 / one P3 absorbed, one P2 rejected on the evidence; 5.2 = plus the owner's confirmation pass after round 2, the cap — two P1 / two P2 absorbed, no third round; register "Rev 5 delta review — round 1" and "— round 2 (the cap)".)* Rev 5 is the focused contract correction the user asked for on 2026-09-10 (`PLAN.md` Decisions Log "USER DECISIONS on the M8 packet (rev 4)"), not a re-opened review: (1) the modern `_meta` keys are reconciled against the pinned pages — **all four are namespaced** (`io.modelcontextprotocol/protocolVersion`, `io.modelcontextprotocol/clientInfo`, `io.modelcontextprotocol/clientCapabilities`, `io.modelcontextprotocol/serverInfo`; rev 4 had three of them bare) — with file:line in the register (`docs/m8-review-findings.md`, "Rev 5 correction", table (a)); (2) the authorized client probe was run (installed Claude Code 2.1.258, throwaway logging stdio server, one-off `--mcp-config`, synthetic data, $0.16) and its nine measured facts F1–F9 are folded in (fact 16): the default open is legacy `2025-11-25`, the modern era is reachable through the client's opt-in, `tools/call` carries `_meta` with `progressToken` and a vendor key in both eras, session teardown is SIGINT, SIGTERM ≈ 100 ms later, stdin still open at both signals, and — only if the process is still alive — an inferred SIGKILL within ≈ 0.5–0.75 s, the harness's discovery probe is a separate process terminated (SIGTERM plus stdin EOF) ≈ 5 ms after its answer, and **the harness respawns a dead stdio server automatically** (contradicting its documentation); (3) **O-1 is resolved on measurement** (dual-era, served concurrently); (4) the binding runtime-authorization requirements of `docs/m7-runtime-fill-control-packet.md` rev 3.2 §6.6 (0)–(6), the user decisions O-RC-3 and O-RC-8 (2026-09-11) and O-E8c-1 (2026-09-11) are incorporated as **§3.11 (D-M8-11)**, new tests T-HOST and the T-STRUCT/T-STDIO/T-WIRE additions, ledger rows A12–A15 and the §6.6→ledger map, one owner-applied test-literal edit (§4.6 item 3) and the restart-authority statement in which **the claim that renewal is inaccessible to the model is withdrawn for Claude Code and every harness with automatic restart or a model-accessible shell**; (5) `main.ts`'s shutdown triggers gain SIGINT/SIGHUP and a startup-time signal rule (§3.7), and every `src/supervisor/host.ts` / `src/core/fillService.ts` / `host.test.ts` line reference is re-pinned at `main` `08d4d01` (the runtime fill control `6812627` shifted them; register (d)). **Nothing settled by rounds 1–3 is re-opened; every rev 4 decision D-M8-1…D-M8-10 and the user's O-2…O-8 decisions stand as recorded** (O-4 now carries the user's required sentence verbatim in §3.8 and §4.5 — rev 5.1, round-1 P1-05; O-8 carries the retained open residual). Owner claude, 2026-09-11/12, session `2026-09-11-m8-rev5`. Authority: the user's decisions of 2026-09-10 (above) and 2026-09-11 (O-RC-3/O-RC-8/O-E8c-1). Rounds 1–3 (Sol read-only + blind fresh-context Opus each; round 3 was the cap) are dispositioned in `docs/m8-review-findings.md` and summarized in §10; the rev 4 → rev 5 delta is fact-checked by read-only Sol rounds capped at two (§8, register). Every fact marked *(verified)* was read from the tree at `main` `08d4d01` (rev 4: `bac91db`) or fetched from modelcontextprotocol.io on 2026-09-10 / 2026-09-12; every **D-M8-n** is an owner decision; every **O-n** is a question only the user can close.

---

## 0. What this is and is not

- **Is:** the `src/adapters/mcp` stdio server that exposes TinyVault's three vault tools and the six non-secret browser controls to an MCP client, composed on the *existing* supervised host so that every successful host payload returned over MCP is obtained through `host.tools` — eight tools pass the tripwire capture wrappers and `browser_snapshot` keeps its deliberate uncaptured exemption, exactly as for an in-process call; the dependency-gate amendment that makes such a composition root expressible without weakening the data-plane rule; the docker-invocation gate extension that pins the one spawn the slice adds; one additive re-export in the supervisor and one in the agent loop; the build mechanism that makes the entry a runnable process; the tests that prove seam traversal, sequential dispatch over the host's mutex, closed error mapping, stdout discipline and lifecycle; the SCHEMA/README/ORIENT/BACKLOG sentences. Authority: `docs/phase-0-plan.md` §1 row 8, §3, §4 layer 3, §7, §8 M8 row; `PROJECT-SPEC.md` §5, §6 *(verified)*.
- **Is not:** an evaluated MCP-transport scorecard row (roadmap step 5); per-caller entitlements or an audit log (D1); M9; M10; any change to `src/core/**`, `src/browser/**` (mutant-only in T-MUTEX, restored), `src/backends/**`, `src/shared/**`; no `src/supervisor/**` change beyond §4.6 item 2 and no `src/agents/loop.ts` change beyond §4.6 item 1; any new **TinyVault** tool name, reason string or result field beyond `src/core/types.ts`; any change to `SKILL.md`, `EVALUATED_AGENT_TOOLS`, `BASELINE_SYSTEM`, N, caps, timing gates, or the E8b pre-registration.
- **The complete adapter-originated fixed vocabulary this slice adds** (pinned in `SCHEMA.md` by A11): JSON-RPC members `jsonrpc`, `id`, `method`, `params`, `result`, `error`, `code`, `message`, `data` (`supported`, `requested`); MCP members `_meta` (the **adapter-originated** `_meta` carries exactly one key, `io.modelcontextprotocol/serverInfo`; the inbound keys the adapter validates are `io.modelcontextprotocol/protocolVersion` and `io.modelcontextprotocol/clientCapabilities`, and every other inbound `_meta` key — `io.modelcontextprotocol/clientInfo`, `io.modelcontextprotocol/logLevel`, `progressToken`, trace keys, vendor-prefixed keys — is accepted and ignored in both eras; rev 5, fact 15), `resultType` (`"complete"` only), `supportedVersions`, `capabilities` (`{tools:{}}`), `ttlMs`, `cacheScope`, `protocolVersion`, `serverInfo` (legacy `initialize` result only; `name:"tinyvault"`, `version`), `tools` (`name`, `description`, `inputSchema`), `content` (`type:"text"`, `text`), `structuredContent`, `isError`; method names `server/discover`, `tools/list`, `tools/call`, `notifications/cancelled` (both eras), `ping`, `initialize`, `notifications/initialized` (legacy only); version strings `2026-07-28`, `2025-11-25`, `2025-06-18`; error codes `-32700`, `-32600`, `-32601`, `-32602`, `-32603`, `-32022` (never `-32021`: no adapter method needs a client capability) with the fixed messages `Parse error`, `Invalid request`, `Method not found`, `Invalid params`, `tinyvault-mcp: internal error`, `Unsupported protocol version`; the two hand-written tool descriptions and schemas (§3.5); result text `tinyvault: internal error`; the two fixed host messages (§3.6); stderr lines exactly `tinyvault-mcp: internal error` and `tinyvault-mcp: framing error`; exit codes `0`, `3`, `4`, `5`, `6`. **The only other caller-visible bytes are:** the request `id` echoed verbatim; the unsupported version string echoed in `data.requested` (a string of at most 64 bytes, §3.4); `package.json#version`; the seven frozen `EVALUATED_AGENT_TOOLS` definitions; and exact host result values permitted by `SCHEMA.md` (`items`, `handle`, `label`, `kind`, `account`, `available`, `ok`, `filled`, `reason` (the closed `FillResult` set, `handle-exhausted` included — `SCHEMA.md:49,96`, `src/core/types.ts:44`), `instruction`, `sessionId`, `snapshot`, `url`, `nodes`, `tag`, `masked`, `role`, `name`, `value`), serialized into `content[0].text` and returned as `structuredContent`. No other adapter-originated member, method, string, or exit code may appear (round 3, Sol P1-01).

## 1. Required reading for the implementer (in this order)

`CLAUDE.md`; `PLAN.md` Current State; this packet; `docs/m8-review-findings.md`; `docs/phase-0-plan.md` §1 row 8, §2, §3, §4 layer 3, §8 M8 row; `SCHEMA.md` "Vault and browser contracts"; `src/core/types.ts`; `src/supervisor/host.ts` (whole file); `src/supervisor/host.test.ts:54-130`; `src/supervisor/evidenceLease.ts:270-320,420-470,540-550`; `testbed/runnerExecution.ts:315-373,445-474`; `src/agents/loop.ts:84-240,455-488`; `src/agents/transcript.ts:34-40`; `src/core/sessionMutex.ts`; `src/browser/session.ts:118-150,233-280`; `src/browser/session.test.ts:130-160`; `src/core/fillService.ts:259-264,352-357`; `scripts/dependency-boundary.mjs`, `scripts/dependency-boundary.vetted.mjs`, `scripts/dependency-boundary.selftest.mjs`, `scripts/dependency-boundary.selftest-fixtures.mjs`; `scripts/docker-invocation.mjs`, `scripts/docker-invocation.selftest.mjs`; `src/core/fillService.structure.test.ts:1-30,110-140`; `src/browser/retention.test.ts:95-112`; `testbed/rootOfTrust.test.ts`; `scripts/test-contract.mjs`; `testbed/docker/slice4.sourceInventory.test.ts:85-102`; `testbed/docker/Dockerfile:6`; `.claude/memory/gotchas_codex.md` ("Sandbox limits"), `.claude/memory/gotchas_verif.md` ("Owner gates and commit chains"); the MCP pages pinned in §3.4 (fetched copies in `artifacts/review-evidence/tinyvault-m8-rev5-20260911/spec/`); **for §3.11:** `docs/m7-runtime-fill-control-packet.md` rev 3.2 §6.6, §6.7 (the O-E8c-1 annotation), §8.3, §9 (R5, R12, R13, R20); the register entries "Runtime fill control MERGED" and "E8c — attempt `E8c-A1-N10`" in `docs/m7-review-findings.md`; `src/core/fillAuthorization.ts`; `src/supervisor/fillAuthorizationDomain.ts`; `src/core/fillService.ts:140-200,290-303`; `src/core/fillService.structure.test.ts:259-262,482-506,539-555,595-612,657-669` (T-RC-10); `docs/m8-review-findings.md` "Rev 5 correction" (the reconciliation table, the probe record, the citation re-pin).

## 2. Facts verified by the owner (2026-09-10)

1. **No MCP code, dependency or directory exists.** Deps `@anthropic-ai/sdk@0.124.0`, `libsodium-wrappers@0.8.4`, `playwright@1.62.1`; devDeps include `esbuild@0.28.2`; no `modelcontextprotocol` in the lockfile; `ls src` → `agents backends browser core shared supervisor`. `@anthropic-ai/sdk` is imported only by `src/agents/anthropicClient.ts`; `loop.ts` imports only `./transcript` and a `testbed` type. `dist/` is gitignored (`.gitignore:7`); `artifacts/` is gitignored.
2. **Path.** `docs/phase-0-plan.md` §8's Key-files column omits `src/` for every `src` file; `adapters/mcp/server.ts` (`:559`) and the §7 tree (`:517`) are the same path. No plan amendment.
3. **The dependency gate rejects the composition once today** (probed: `src/adapters/mcp/probe.ts` importing `createSupervisedHost` → exit 1, exactly `static import: src/adapters/mcp/probe.ts -> src/supervisor/host.ts`; file deleted → PASS, 163 modules / 153 roots). Mechanism: roots `:107`; protected `src/supervisor` `:40`; protected edge from a non-evaluator entry is a violation and traversal is pruned (`:239-244`). Edge classification lives in `classifyEdge` (`:206-245`: production-to-tooling `:208-212`, `classifyVettedReach`, `isForbiddenBrowserZoneEdge` `:641-649` current-file-keyed literal list, protected arm); `sourceZone` (`vetted.mjs:189-198`) is consumed only by `zoneConfigurationErrors` (`:177-187`). `import type` is a traversed edge (`:451-452`); alias relays are traversed (`buildGraph` `:412-419`, `scanExternalFile` `:561-592`). **Existing self-test facts the amendment touches:** `scripts/dependency-boundary.selftest.mjs:52-65` asserts the shipped vetted manifest deep-equal with `reachableFrom: ['src/browser','testbed']`; `configuredReachableDirectories` (`vetted.mjs:88-103`) requires every `reachableFrom` path to be an existing in-repo directory, and `installDefaultVettedConfiguration` (`selftest-fixtures.mjs:209-212`) creates only `src/browser/playwright.ts` and `testbed/` in each fixture root (round 3, Opus P1-05).
4. **Host wrappers.** `createTools` (`host.ts:409-443`): `capturedVault` (list/setup), `capturedVaultFill` (fill), `capturedOpen`/`capturedControl` (open/navigate/click/type/close), `uncaptured` (snapshot; pinned `host.test.ts:120-128`). `boundedTools` (`:342-363`) admits seven tools and applies the 10 s `bounded` wrapper to six; `browser_open_session` is admitted but unbounded (`:345-347`); `list_vault`/`request_vault_setup` are neither admitted nor bounded (`:344`). The direct caller is `invokeHostTool` (`runnerExecution.ts:461-474`), sequential, draining after every call (`:445-449`).
5. **Reachable host-tool rejections: two fixed messages plus one residual path.** `VAULT_TOOL_FAILURE_MESSAGE` (`host.ts:41`; `:361,474,490`); `BROWSER_OPEN_FAILURE_MESSAGE` (`controls.ts:12`; `host.ts:347,456`; not re-exported by `host.ts:43-48`). `capturedControl` swallows; snapshot cannot throw. Residual: `lease.settleAttach()` at `host.ts:420,427` runs before the `captured*` try and can reject with arbitrary text; `browser_open_session` never calls `runControl`, so only a real lease with a controllable `trackAttach` promise (`evidenceLease.ts:287,297`) can exercise it. `host.drainEvidence()` throws `InactiveEvidenceLeaseError` after the lease is dropped (`host.ts:268` → `evidenceLease.ts:442-444`, `#assertActive` `:543-545`); the host can abort outside any adapter call (`host.ts:84-97`).
6. **The host's 10 s bound starts before mutex acquisition** (`host.ts:322-340`; `session.ts:258-276`). `browser_close_session` closes the mutex and waits for the holder (`session.ts:118-150`); a fill losing to a close resolves to `session-unknown` via `fillService.ts:259-264,352-357`.
7. **The host's own shutdown.** `quiesce` (`host.ts:375-398`) closes sessions itself inside its sequence; its catch aborts; the post-try `hasCaptureFailed()` throw at `:397` does not abort; a `finishHost` precondition throw (`:280-285`) does not drop the lease; `abort()` drops it (`evidenceLease.ts:463-468`); `finish()` refuses pending evidence (`:452`) and drops in `finally` (`:459-461`); `abort()` on an already-dropped lease is safe (`#drop()` idempotent `:547-550`; `abortHost` memoized `host.ts:287-292`) but after a successful `finish()` it clears `abortedEvidence` and sets `captureFailed` (inert for M8). `quiesceEvidenceProducers` is optional on the type (`:59`), always set by `compose` (`:272`), and **is only observable after `createSupervisedHost` returned — i.e. after Chromium launched and the lease exists** (`:64-99`; round 3, Sol P1-07). `createSupervisedHost` launches Chromium (`:72`) before constructing the lease (`:74`). Locked rules: drain while targets remain alive before disposal (`docs/phase-0-plan.md:218-225`); a capture error drops the lease in a supervisor `finally` (`:277`).
8. **The tripwire** needs a non-empty canary (`tripwire.ts:110-114`) and is instrumentation, never a runtime gate (`docs/phase-0-plan.md:266`); in stdio the parent reads stderr and the exit code. Timing claims exist only "under probe P" (`:283,287`); this packet makes none for the standalone process.
9. **Tool schema source.** `EVALUATED_AGENT_TOOLS` (`loop.ts:84-240`, seven frozen entries with explicit `properties`/`required`); digest `c7475344…` (`anthropicClient.test.ts:103`, `agentEvidenceBudget.test.ts:180`, `sourceInventory.ts:70`). `validateToolCall` (`:470-476`) is registry-bound and needs a string id; `matchesSchema` (`:478-488`) dereferences `required`/`properties` unguarded and returns false for `undefined`. `browser_open_session` takes no argument (`types.ts:80`).
10. **`serializeExact` is `JSON.stringify` plus a throw on `undefined`** (`transcript.ts:34-40`).
11. **Structural pins.** `fillService.structure.test.ts` (2/5/7 inventories over all of `src`); `src/browser/retention.test.ts:100-109` sweeps every `src` file **including tests** for the secret-primitive tokens; `testbed/rootOfTrust.test.ts:13-42` pins `package.json#scripts`, `Makefile`, the vitest configs and every `scripts/check-*.mjs` (`scripts/dependency-boundary*.mjs` and `scripts/docker-invocation*.mjs` are **not** hash-pinned; their selftests run as commands 4 and 6 of the byte-pinned test chain, `scripts/test-contract.mjs:8-22`).
12. **The docker-invocation gate and its selftest.** A `node:child_process` import needs an exact allowlist row (`docker-invocation.mjs:11-46`). Spawn-shape pinning applies only to `scripts/**` paths today (`:164-166`); a `src/**` `reviewProfiles` entry would leave `bindings` empty and emit `spawn-call-count` (`:150`). `reviewProfiles` is a module-private `const` (`:79`). **`scripts/docker-invocation.selftest.mjs:56-60` synthesizes a source for every allowlist row — `import { spawnSync } … spawnSync(process.execPath, [])` for `scripts/**` child-process rows, a bare `import '<spec>'` otherwise — and asserts zero violations** (round 3, Opus P1-04). The `process` global needs no row; esbuild's own spawn needs no row (`slice4.sourceInventory.test.ts:94-102`).
13. **Launch mechanism (measured).** Node v24.19.0 rejects extensionless specifiers → `node src/adapters/mcp/main.ts` cannot run. Precedent: the container bundle (`Dockerfile:6`; `compose-schema.mjs:42`; asserted by `slice4.sourceInventory.test.ts:85-86`) and the in-test API build (`:94-102`). **Node resolves bare specifiers from the importing file's own directory upward**, so a bundle built with `packages:'external'` into a temp directory outside the repository cannot resolve `playwright`/`libsodium-wrappers` (round 3, Opus P1-03).
14. **MCP specification (fetched 2026-09-10).** Modern `2026-07-28` (**changelog**: item 2 removes the `initialize`/`initialized` handshake; **item 5 removes `ping`**, `logging/setLevel`, `notifications/roots/list_changed`; item 8 makes `resultType` required; minor item 5 requires `ttlMs`/`cacheScope` on list results, and the caching page extends the MUST to `server/discover`): per-request `_meta` (`io.modelcontextprotocol/protocolVersion` and `io.modelcontextprotocol/clientCapabilities` required, `io.modelcontextprotocol/clientInfo` SHOULD; a missing required field → `-32602`); every `result` carries `resultType`; `_meta['io.modelcontextprotocol/serverInfo']` SHOULD; `server/discover` MUST be implemented, result `{resultType, supportedVersions, capabilities, _meta['io.modelcontextprotocol/serverInfo'], instructions?, ttlMs, cacheScope}` with no top-level `serverInfo`; `-32022` with `data.supported`/`data.requested`; error id "except … where the ID could not be read due a malformed request"; **"Servers MUST NOT rely on prior requests over the same connection to establish context"** and a dual-era server "MAY serve both eras concurrently on the same endpoint or process" (versioning page; round 3, Opus P1-02); notifications "The receiver MUST NOT send a response" (both eras; round 3, Sol P1-03); the modern schema defines common request members `inputResponses`/`requestState` for the multi-round-trip pattern (tools page; round 3, Sol P1-05); stdio: one message per line, stdout MCP-only, stderr free-form, exit promptly on EOF, cancellation "MUST NOT send any further messages for it"; `tools/call {name, arguments?}`; `structuredContent` without `outputSchema` permitted; unknown tool / malformed → `-32602`. Legacy `2025-11-25` and `2025-06-18`: **"The request ID MUST NOT have been previously used by the requestor within the same session"** (2025-11-25 basic; round 3, Sol P1-02); the client SHOULD NOT send requests other than `ping` **before the server has responded to `initialize`** (so after that response every request is permitted whether or not `notifications/initialized` has arrived; round 3, Opus P2-06); `initialize` params `protocolVersion`, `capabilities`, `clientInfo`; result `protocolVersion`, `capabilities`, `serverInfo`, `instructions?`; negotiation: echo a supported requested version, else respond with another supported version (SHOULD be the latest); batching removed in `2025-06-18` (changelog item 1); legacy `ping` returns `{}`.

15. **`_meta` reconciliation (rev 5; register "Rev 5 correction" (a), fetched 2026-09-12 UTC, copies under `artifacts/review-evidence/tinyvault-m8-rev5-20260911/spec/`).** `basic/index.md:350-359` (reserved keys), `:369-384` (per-request fields: `io.modelcontextprotocol/protocolVersion` string **required**, `io.modelcontextprotocol/clientInfo` `Implementation` optional, `io.modelcontextprotocol/clientCapabilities` `ClientCapabilities` **required**, `io.modelcontextprotocol/logLevel` optional; "A request missing any required field is malformed; the server **MUST** reject it with … `-32602`"), `:386-387` (clients SHOULD send `clientInfo`), `:389-394` (`-32021` only when a required client capability is undeclared), `:398-404` (per-response field `io.modelcontextprotocol/serverInfo`, SHOULD), `:326-346` (key format; "implementations **MUST NOT** make assumptions about values at these keys"; `progressToken` reserved `:354`); `schema.ts:63-111` (`RequestMetaObject`: `:76`, `:90`, `:98`, `:110`, `progressToken` `:68`), `:151-158` (`ResultMetaObject`: `:157`), `:223-235` (`Result.resultType` required), `:450` (`-32022`), `:443` (`-32021`), `:678-698` (`DiscoverResult`), `:1094,1109` (`CacheableResult`); `server/discover.md:15-31` (request example with the three namespaced keys), `:44-60` (result example with `_meta['io.modelcontextprotocol/serverInfo']`), `:100-101`; `changelog.md:16` (the four keys named), `:22` (`ping` removed; `logLevel` per request); `basic/versioning.md:169-176` (compatibility matrix: a dual-era client sends `server/discover` first on stdio and falls back to `initialize` on a non-modern error or a timeout; a legacy client against a modern-only server fails at `initialize`), `:178-185` (dual-era rules). The legacy `initialize` keys stay bare on the `2025-11-25` / `2025-06-18` lifecycle pages and were observed bare (fact 16). **Rev 4 wrote `clientInfo`, `clientCapabilities` and `serverInfo` bare in §0, §3.4 and §4.4; corrected throughout rev 5.**
16. **Client probe (rev 5; authorized 2026-09-10; executed 2026-09-12 UTC; register "Rev 5 correction" (b); artifacts `…/tinyvault-m8-rev5-20260911/probe/`).** Installed Claude Code **2.1.258**, `-p` mode, one-off `--mcp-config` + `--strict-mcp-config`, a throwaway logging stdio server with one synthetic tool, seven runs, $0.16, nothing persistent. **F1** default open: legacy `initialize` `{protocolVersion:"2025-11-25", capabilities:{roots:{listChanged:true}, elicitation:{}}, clientInfo:{name:"claude-code", title, version:"2.1.258", description, websiteUrl}}` (bare keys), id `0`, then `notifications/initialized`, then `tools/list`; **no `server/discover`, no `ping`.** Under the documented opt-in (`MCP_PROTOCOL_NEGOTIATION=auto` with the v2 runtime, `claude-code-mcp-doc.md:331-340`) the client is modern: `server/discover` with all three namespaced request keys, then `tools/list` and `tools/call` each carrying them — no `initialize`. **F2** `tools/call` `params._meta` carries `progressToken` and the vendor key `claudecode/toolUseId` in both eras (plus the three namespaced keys in the modern era). **F3** ids observed: the integer `0`, small integers, the string `"server-discover-probe-1"`. **F4** under the opt-in the discovery probe is a **separate process**: `server/discover` answered → **SIGTERM and stdin EOF ≈ 5 ms later** → a second process starts the session ≈ 25 ms after that with `tools/list` (no second discover). **F5** session-process teardown at exit: **SIGINT first, SIGTERM ≈ 100 ms later, stdin still open at both signals; a signal-ignoring process stayed alive through +502 ms and was gone before its next heartbeat at ≈ +750 ms — an inferred SIGKILL** (the target cannot log it). The discovery process of F4 is the exception: SIGTERM and stdin EOF together, and it exited `0` (rev 5.1, round-1 P1-02/P3-01). **F6** a server that dies mid-session (exit 1, or SIGKILL) is **respawned by the harness ≈ 240 ms later** and the next `tools/call` is dispatched to the fresh process with a fresh `initialize`; a server that dies right after `tools/list` at startup is respawned ≈ 4.5 s later; the model saw the results and nothing signalled the restart. **F7** `tools/list` is not repeated after a respawn (the list is reused). **F8** ids restart at `0` on the new process. **F9** the client's documentation says stdio servers are not reconnected automatically (`claude-code-mcp-doc.md:357-359`) — **contradicted by F6 as measured**; the measurement governs and the discrepancy is recorded; interactive mode is unmeasured.
17. **Runtime fill authorization in the tree (`08d4d01`).** `createSupervisedHost` (`host.ts:64-99`) constructs one `createFillAuthorizationDomain()` (`:76`; `src/supervisor/fillAuthorizationDomain.ts:10-32`: one budget unit per handle, `renew` on the composer-only `lifecycle`) per host, hands `lifecycle` only to the optional `onFillAuthorization` hook (`:70`, `:77`) and `authorization` only to **the single `createFillService` call `:94`** (pinned to `src/supervisor/host.ts` alone by `fillService.structure.test.ts:495-499,517`); `composeSupervisedHost` (`:102-108`) is the test seam and takes a ready fill service. The fill path reserves the unit after the origin check, the destination pin and the lock check and before the secret is resolved (`fillService.ts:146-158,176-183`); refusal is the closed reason `handle-exhausted` (`:183`; `src/core/types.ts:44`; `SCHEMA.md:49,96`); `setupReasonFor` maps it to `null` (`:290-292`); `request_vault_setup` returns a fixed template chosen by the caller's `reason` (`results.ts:51-55`). `FillAuthorizationLifecycle` is "Trusted composer only. Never carried by a host or its tools" (`src/core/fillAuthorization.ts:14-17`); `SupervisedHost` exposes no renewal (`host.ts:53-62`). T-RC-10 (`fillService.structure.test.ts:309-…`) scans **every production file under `src` and `testbed`**: `HOST_CALL_FILES` (`:595-596`) must equal the set of files calling `createSupervisedHost`/`createHost` (`:495`); `AUTHORITY_FILES` (`:260-261`) must equal the set mentioning an authority name (`:259`); each host-construction call must be a single object literal whose keys are among `backend`/`canary`/`browser`/`launcher` (`inspectHostShape` `:604-612`), its argument must not carry `authorization`/`onFillAuthorization` by type or syntax or spread an opaque type (`inspectHostArgument` `:657-669`), and its inputs must not be written through a computed key (`inspectAuthorityAccess` `:539-555`, `unknown-host-key`) (rev 5.1, round-1 P1-04). The shipped backend's handle→record map is injective (`src/backends/localFile.ts:144-149`; R20).

## 3. Owner decisions (rev 4; rev 5 adds §3.11–D-M8-12 and the corrections marked *rev 5*)

### 3.1 D-M8-1 — Location and files

`src/adapters/mcp/protocol.ts`, `tools.ts`, `server.ts`, `main.ts` (only `main.ts` touches `process.*`). Under `src/`. Every production file **< 400 lines** (T-STRUCT); ≈ 600 production lines total; the framing residual is recorded (§9).

### 3.2 D-M8-2 — Composition zone: explicit list, five gate sites, entry-rooted C3, two self-test edits

`scripts/dependency-boundary.vetted.mjs`: `COMPOSITION_DIRECTORIES = ['src/adapters/mcp']` (explicit; a future adapter directory is a reviewed edit) and a `composition` branch in `sourceZone` — optional for classification; its only effect is that a file resolving across `src/adapters/mcp` and another zone becomes a `zoneConfigurationErrors` violation. `scripts/dependency-boundary.mjs`, five sites: (i) `classifyEdge`'s protected arm (`:239-244`) admits an entry for which a new `isCompositionEntry` predicate holds and continues traversal — **C1**; (ii) `VETTED_EXTERNAL_PACKAGES[0].reachableFrom` gains `src/adapters/mcp` — **C2**; (iii) a new predicate in `classifyEdge`, evaluated before the protected prune, flags `composition-reached-from-production` when the **entry** is a non-composition production `src/**` root and the **target** has any location under `src/adapters/**`, through any traversable intermediate — **C3**; (iv) `isForbiddenBrowserZoneEdge`'s literal list gains `src/adapters/mcp` — **C5**; (v) a direct current-file-keyed `src/adapters/mcp → testbed/**` edge is `production-to-evaluator` — **C4**. **C0** is a positive control, not a rule. **C6 (amended, round 3 Opus P1-05): no existing self-test case changes its verdict.** Two existing cases are edited in place as a direct consequence of C2 and must stay green: the shipped-manifest regression at `selftest.mjs:52-65` gains `'src/adapters/mcp'` in its expected `reachableFrom` literal, and `installDefaultVettedConfiguration` (`selftest-fixtures.mjs:209-212`) additionally creates `src/adapters/mcp/` in every fixture root (`configuredReachableDirectories` requires every `reachableFrom` path to exist). Any other movement of an existing case is a STOP. **C3 fixtures (each asserting the reason string):** direct import, re-export, dynamic `import()`, each supported `require` form, `import type`, external-package relay, testbed relay, resolvable `tsconfig.paths` alias, unscanned alias relay — all red; C1/C2 positives; C4/C5 direct red; a non-composition root importing the supervisor still red; `src/adapters/other/x.ts` importing the supervisor still red (not a composition entry); a cross-zone symlink red through `zoneConfigurationErrors` (owned there). **Declared C3 limits (§9):** a `scripts/**` or `testbed/**` entry reaching `src/adapters/**` is unruled; a relay through a protected intermediate is pruned before C3 can see it (already red under the protected rule); non-literal specifiers, `createRequire` and `node:module` loads are pruned and red under their own rules.

### 3.3 D-M8-3 — No MCP SDK; hand-written framing with provenance-headed fixtures

Unchanged. T-WIRE fixtures carry a header quoting the pinned page URL and section; owner-verified at §6 step 3. Residual: framing unfuzzed.

### 3.4 D-M8-4 — Dual-era, served concurrently; exact precedence; exact envelopes

Supported modern `['2026-07-28']`; legacy `['2025-11-25','2025-06-18']`. **Era rule (round 3, Opus P1-02; confirmed by probe F1/F4):** the two eras are served concurrently and independently. **A request is modern iff `params._meta` is an object with an `io.modelcontextprotocol/protocolVersion` member** (its type is then checked in step 5); a modern request is always served statelessly per `2026-07-28`, before and after any `initialize`. `initialize` (without modern `_meta`) and the legacy-shaped requests that follow it are served under the negotiated legacy revision; the legacy state is process-scoped and affects legacy-shaped requests only. **Message classification:** a message whose `method` begins `notifications/` is a notification; every other message with a `method` is a request; a message with `result` or `error` and no `method` is an inbound response object and is **ignored** (round 3, Sol P2-03, Opus P2-04). **Precedence for one incoming line (T-WIRE has a case per step):** (1) bytes: fatal UTF-8 decode failure or a line over `MAX_LINE_BYTES` (1 MiB, counted before decoding) → framing failure (§3.9); (2) JSON parse failure or a non-object top-level value → `-32700`, `id: null`, loop continues; (3) shape → `-32600`: `jsonrpc` not `"2.0"`, a request with `id` null/absent or non-string/non-integer (**the integer `0` and any string are valid ids** — probe F3; a falsy-id bug is a T-WIRE case), a notification with an `id`, a batch array (every era), a request `id` equal to an outstanding one (modern) **or to any id used since a successful legacy `initialize`** (legacy; fact 14; round 3, Sol P1-02), more than `MAX_QUEUED_REQUESTS` (64) outstanding; (3a) any request after shutdown began → `-32600`; **notifications never receive a response: an unknown, malformed or invalid notification is ignored** (round 3, Sol P1-03) — `notifications/cancelled` (optional `_meta`, optional `requestId` string|integer, optional `reason`; absent/unknown/answered ids ignored) is handled immediately (§3.9); `notifications/initialized` is recorded; (4) era: `initialize` without modern `_meta` selects legacy (validated: `protocolVersion` string, `capabilities` object, `clientInfo` an object with string `name`/`version` — additional members of `clientInfo` and `capabilities` are ignored, the installed client sends five `clientInfo` fields (probe F1) — else `-32602`; echo a supported requested version, else `2025-11-25`; result `{protocolVersion, capabilities:{tools:{}}, serverInfo:{name:'tinyvault', version}}`; a second `initialize` → `-32600`); in a process where legacy is active, a request without modern `_meta` is a legacy request: **`ping` is answered `{}` at any time; any other legacy request before the `initialize` response → `-32600`; after that response every legacy request is served whether or not `notifications/initialized` arrived** (fact 14; round 3, Opus P2-06); a request without modern `_meta` and no legacy session → `-32602` (missing required `_meta`); (5) **modern `_meta` validation before method lookup:** `io.modelcontextprotocol/protocolVersion` present and a string of ≤ 64 bytes, `io.modelcontextprotocol/clientCapabilities` present and an object, else `-32602`; **every other `_meta` key is accepted and ignored** — `io.modelcontextprotocol/clientInfo` (never read: the page says implementations SHOULD NOT change behaviour on it), `io.modelcontextprotocol/logLevel` (no `notifications/message` is ever emitted), `progressToken` (no progress notification is ever emitted; the page says the receiver "is not obligated"), trace keys and vendor-prefixed keys such as `claudecode/toolUseId` (fact 15; probe F2) — **without inspecting their values** — an explicit owner compatibility choice (round-1 Sol P2-01, round-2 P2-02 recorded): the page forbids assumptions about values at reserved keys (`basic/index.md:329-330`) but also types the optional fields (`:375-380`; `schema.ts:63-110`), so whether a malformed optional value must be rejected is a conformance uncertainty the adapter resolves toward acceptance, because it never uses those values — and `-32021` is never emitted; version unsupported → `-32022` with `data.supported` and `data.requested` (the validated string) — **regardless of method** (round 3, Opus P2-03, Sol P1-04); the modern common request members `inputResponses`/`requestState` are recognized and, because this server never issues an input-required result, rejected as unexpected request state with `-32602` (round 3, Sol P1-05); (6) method known else `-32601` — **modern `ping` and modern `initialize` are unknown methods** (fact 14; round 3 convergent); (7) per-method params validated exactly — `params._meta` (any object) is permitted on every request in **both** eras and never validated beyond step 5 (legacy `tools/call` from the installed client carries `_meta:{progressToken, "claudecode/toolUseId"}`, probe F2); the other members are exactly `server/discover`: none; `tools/list`: optional `cursor` string; `tools/call`: `name` string, optional `arguments` object; any other member → `-32602`; (8) `tools/call`: tool known else `-32602`; absent `arguments` → `{}`; `null`/non-object → `-32602`; `matchesSchema(arguments, inputSchema)` else `-32602`; (9) dispatch. **Envelopes:** every modern `result` — including a `tools/call` result with `isError:true` — carries `resultType:'complete'` and `_meta:{'io.modelcontextprotocol/serverInfo':{name:'tinyvault', version}}` (the only adapter-originated `_meta` key; fact 15); a JSON-RPC error response carries `error` only. Modern `server/discover` → `{resultType:'complete', supportedVersions:['2026-07-28'], capabilities:{tools:{}}, _meta:{'io.modelcontextprotocol/serverInfo':{name:'tinyvault', version}}, ttlMs:0, cacheScope:'public'}` (`server/discover.md:44-60`; `schema.ts:678-698`); modern `tools/list` → `{resultType, _meta (same single key), tools:[…], ttlMs:0, cacheScope:'public'}`; legacy requests may carry `params._meta` (`progressToken`) — accepted, ignored; legacy `tools/list` → `{tools:[…]}`; legacy `ping` → `{}`. `-32603` (`tinyvault-mcp: internal error`) only for an adapter-internal failure outside a tool call. Pinned pages: modern `…/2026-07-28/{changelog, basic/index, basic/versioning, basic/transports/stdio, basic/patterns/cancellation, server/tools, server/discover, server/utilities/caching, schema}`; legacy `…/2025-11-25/{basic/index, basic/lifecycle, basic/transports, basic/utilities/ping, basic/utilities/cancellation, server/tools}` and `…/2025-06-18/{basic/index, basic/lifecycle, basic/transports, basic/utilities/ping, server/tools, changelog}`. Plus `schema/2026-07-28/schema.ts` from the specification repository (`main`, fetched 2026-09-12; `spec/schema-2026-07-28.ts`). **STOP if any sentence here cannot be verified from these pages.**

### 3.5 D-M8-5 — Nine tools; seven are the registry's objects; `matchesSchema`

Fixed order: `list_vault`, `fill_from_vault`, `request_vault_setup`, `browser_open_session`, `browser_close_session`, `browser_navigate`, `browser_click`, `browser_type`, `browser_snapshot`. Seven are the frozen `EVALUATED_AGENT_TOOLS` objects by reference. Hand-written: `list_vault` → `{type:'object', properties:{}, required:[], additionalProperties:false}`; `request_vault_setup` → `{type:'object', properties:{reason:{type:'string', enum:['missing_item','backend_locked','backend_unavailable']}}, required:['reason'], additionalProperties:false}`; descriptions `"TinyVault supervised <name> operation."`. Validation via `matchesSchema` for all nine; `browser_open_session` invoked with no argument. T-SCHEMA additionally pins that the dispatch switch's nine case-clause literals, in order, equal the nine definition names (round 3, Opus P3-04). Threat-model note for the register. **O-6**.

### 3.6 D-M8-6 — Exact bytes; two fixed messages; fixed strings only

`tools/call` → host tool invoked as `invokeHostTool` does; result `R` → `{resultType,_meta (modern), content:[{type:'text', text: serializeExact(R)}], structuredContent: R, isError:false}`. **Claim:** every successful host payload over MCP is obtained through `host.tools`, so the eight captured tools passed the tripwire capture wrappers and `browser_snapshot` passed its uncaptured exemption before the adapter saw the bytes; adapter-originated envelopes are fixed constants outside the host seam. **Rejections:** `VAULT_TOOL_FAILURE_MESSAGE` / `BROWSER_OPEN_FAILURE_MESSAGE` → `{…text:<that string>, isError:true}`; every other rejection (the `settleAttach` residual included) → `{…text:'tinyvault: internal error', isError:true}` with stderr receiving exactly `tinyvault-mcp: internal error` and nothing else. After a successful call the adapter calls `host.drainEvidence()` exactly once (wrapped; an `InactiveEvidenceLeaseError` is swallowed and never changes the already-computed envelope). Unknown tool → `-32602`. No `outputSchema` (**O-3**). *(rev 5)* `{ok:false, reason:'handle-exhausted'}` is a host payload like every other `FillResult` (fact 17) and is delivered byte-identical with `isError:false`; a `request_vault_setup` that follows it returns the fixed template selected by the client's `reason` (`results.ts:51-55`) — `setupReasonFor` yields `null` for it (`fillService.ts:290-292`) and the adapter neither calls nor exposes any renewal (§3.11).

### 3.7 D-M8-7 — Lifecycle: bounded, verdict unobservable, lease always dropped, no uncaught byte

`main.ts`: read `TINYVAULT_VAULT_PATH`, `TINYVAULT_KEY_PATH`; validate a present `TINYVAULT_TRIPWIRE_CANARY` non-empty **before** `createSupervisedHost`, else mint (§3.8); **install `process.on('uncaughtException')` and `process.on('unhandledRejection')` before the host is created — both write exactly `tinyvault-mcp: internal error` to stderr and exit `4` before the host exists, `3` afterwards (after `host.abort()` and `closeAll()`), with no interpolated value** (round 3, Opus P2-02); `createSupervisedHost({backend, canary})`; **if the returned host lacks `quiesceEvidenceProducers`, call `host.abort()`, await `host.closeAll()`, exit `4`** (the host and lease already exist; round 3, Sol P1-07); start the reader and the sequential dispatcher (§3.9). **Shutdown** on stdin EOF, **SIGINT**, SIGTERM or SIGHUP — the first of them starts the sequence and the others are then ignored (idempotent); *(rev 5, probe F5)* at session teardown the installed client sends SIGINT first and SIGTERM ≈ 100 ms later with stdin still open (its discovery process instead receives SIGTERM and EOF together, F4), so a SIGTERM-only handler would let Node's default SIGINT disposition kill the process mid-flight; a trigger that arrives while `createSupervisedHost` is still pending (the harness's discovery probe terminates the process ≈ 5 ms after its `server/discover` answer, F4) is remembered and the sequence starts as soon as the host exists (a pre-host trigger with a host that then fails to construct → exit `4` as before): (1) stop admitting (later requests `-32600`; queued not-yet-started requests `-32600`); (2) await the one in-flight handler under `SHUTDOWN_HANDLER_DEADLINE_MS` (30 s); on expiry — reachable only through the three unbounded host tools, fact 4 — **the abandoned request is marked cancelled (its response suppressed) and its promise gets a `.catch(() => undefined)`**, then `host.abort()`, `closeAll()`, exit `3` (round 3, Opus P2-02/P2-08); (3) `host.quiesceEvidenceProducers({beforeClose: settle+drain, afterClose: settle+drain, settleTimeoutMs})` — the host closes sessions itself; (4) `host.finish()`, return value discarded; (5) `host.closeAll()` in `finally`. **Any failure in (3) or (4) calls `host.abort()` before `closeAll()`.** **A stdout write failure (a rejected write or an `'error'` event on the out stream) stops admission, calls `host.abort()`, awaits `closeAll()`, and exits `6`** (round 3, Sol P2-02). **Exit codes — the highest-priority observed terminal condition wins, `4 > 6 > 3 > 5 > 0`** (round 3, Sol P3-01): `4` startup (pre-host failures and the post-creation contract check); `6` stdout failure; `3` finalization failure (quiesce/finish/closeAll threw, deadline abort, post-host uncaught error); `5` framing failure (one stderr line `tinyvault-mcp: framing error`, then the normal shutdown sequence); `0`. **Kill grace (rev 5, measured, F5):** if the adapter is still alive after SIGINT and SIGTERM, heartbeat cessation supports an inferred SIGKILL ≈ 0.5–0.75 s after SIGINT (the signal-ignoring probe process was alive at +502 ms and gone before ≈ +750 ms; responsive probe processes exited `0` before that), so under that harness the sequence (quiesce with a budget of up to 5 s, `finish`, `closeAll`) **may be truncated — completion within the grace is not guaranteed**, and when it is truncated no exit code is observed. Nothing in the security claim depends on completion: the verdict has no consumer (§3.8), the lease is process-local, and the browser must be torn down by the operating system when its remote-debugging pipe closes with the process — **T-STDIO asserts that last part** (§3.10) and a survivor is a STOP (§5). **The verdict is never written anywhere and never influences the exit code**; T-LIFE's differential proves stdout, stderr and exit code are byte-identical between a run whose lease holds the planted canary and a clean run with identical wire input, and its mutant M-5 proves the differential can fail. **Residuals (§9):** an abandoned unbounded host operation runs to completion against a dropped lease; a client SIGKILL produces no exit code; shutdown timing may differ with a match (no probe-P claim); `host.abort()` after a successful `finish()` clears `abortedEvidence` and sets `captureFailed` (inert for M8). BACKLOG A4: **O-8**.

### 3.8 D-M8-8 — The tripwire in a standalone process

Verification mode: `TINYVAULT_TRIPWIRE_CANARY` (validated non-empty). Production default: a 32-byte `randomBytes` base64url token. Stated plainly: no reference value and no consumer; kept so the tested and production composition paths are one code path. **The randomly minted production canary does not establish detection of actual credential leaks** — it has no credential-derived reference value and no verdict consumer (the user's required sentence, 2026-09-10; rev 5.1). **O-4**.

### 3.9 D-M8-9 — Reader/dispatcher split; sequential dispatch; cancellation; stdout discipline

The reader runs continuously (fatal `TextDecoder`; byte-counted line limit; pause stdin only while decoding a line; a queue of at most 64 outstanding requests, overflow `-32600`); the dispatcher executes requests one at a time in arrival order (at most one host operation in flight; the host's bound measures its own work). **Cancellation** (both eras): validated immediately by the reader; naming the in-flight request → marked cancelled, **response suppressed** (host work runs to completion; the dispatcher waits before starting the next); naming a queued request → removed, never started; unknown/answered ids ignored; the adapter never emits a message for a cancelled request. **The modern era has no cheap liveness probe** (`ping` was removed; a slow `tools/call` blocks the modern stream with no interleaved alternative — residual); a legacy `ping` queued behind a slow call waits. Every stdout write is awaited to completion (`write` callback / `drain`); a write error → exit `6` (§3.7). `process.stdout` receives only JSON-RPC lines; every stderr line is one of the two fixed strings. No `SessionMutex` import, no per-session promise chains, no lock object.

### 3.10 D-M8-10 — Launch: esbuild bundle from a checkout; T-STDIO mandatory with metafile proof

Product entry `dist/tinyvault-mcp.mjs` built by `./node_modules/.bin/esbuild src/adapters/mcp/main.ts --bundle --platform=node --target=node24 --format=esm --packages=external --outfile=dist/tinyvault-mcp.mjs` (every `node_modules` package stays external; no new dependency), committed as `package.json#scripts.build:mcp` and a `Makefile` `mcp` target with the two `testbed/rootOfTrust.test.ts:15-16` digests updated in the same owner commit (**O-5**). **The bundle is not a relocatable artifact: it resolves its external packages from the checkout's `node_modules` and runs only from a checkout with dependencies installed** (fact 13) — stated in `SCHEMA.md` and the README beside the launch command. **T-STDIO** builds through the esbuild API with `bundle:true, platform:'node', target:'node24', format:'esm', packages:'external', metafile:true, write:true` **into a fresh directory under the gitignored `artifacts/` tree of the repository root** (`fs.mkdtempSync(path.join(process.cwd(), 'artifacts', 'mcp-stdio-'))`, removed in `finally`; round 3, Opus P1-03), asserts from the metafile that no `node_modules` input entered the bundle and every package import is external, asserts `package.json#scripts['build:mcp']` parses to the same entry, `--bundle`, `--platform`, `--target`, `--format`, `--packages` and `--outfile` values, writes a throwaway libsodium vault with the existing writer, and spawns the bundle with the exact call shape the docker-invocation profile pins — `spawn(process.execPath, [bundlePath], { env, stdio: 'pipe', shell: false })`, one call site (round 3, Opus P3-02) — driving: modern `server/discover`, `tools/list` (nine, cache fields), `list_vault`, `request_vault_setup`, `browser_open_session`, `browser_snapshot`, `notifications/cancelled` on a queued request (end-to-end suppression only; the never-started proof is T-WIRE's executor spy — round 3, Sol P2-04), EOF → exit `0`, exact stdout inventory, empty stderr; *(rev 5)* **the harness's probe sequence**: a second spawn receives `server/discover` alone and is sent SIGTERM plus stdin EOF 5 ms after the answer → exit `0` (or no exit code if the owner's SIGKILL follows), no stderr; **the kill grace**: a third spawn receives SIGINT and then SIGKILL 500 ms later → the adapter's Chromium process (the adapter's child processes enumerated before the kill) is gone within 5 s — **a survivor is a STOP**; **two hosts per two processes**: two concurrent spawns with the same environment each answer `list_vault` and `browser_open_session` independently. It launches real Chromium; it is discovered by `make test` and runs in the clean-clone gate on any host with Chromium (A10); "Not run: sandbox" for the implementer. **STOP if the spawned bundle cannot resolve its external packages.**


### 3.11 D-M8-11 *(rev 5)* — Runtime fill authorization in the adapter process: one host, no renewal path, restart authority stated

Binding source: `docs/m7-runtime-fill-control-packet.md` rev 3.2 §6.6 (0)–(6) (carried "before M8 locks"), user decisions O-RC-8 and O-RC-3 (2026-09-11) and O-E8c-1 (2026-09-11); tree facts in fact 17; measurements in fact 16. Each item names its verification; the ledger map is in §7.

- **(0) Exactly one `SupervisedHost` per process, never per connection, client or request.** `main.ts` calls `createSupervisedHost` exactly once, at startup, before the reader starts, and hands the result to exactly one server-factory call (the rev 4 composition pin, T-STRUCT); no other adapter file references `createSupervisedHost` or `composeSupervisedHost`; the server layer has no host-construction capability at all. **The stdio meaning of "a second connection":** within one process the only re-opening a client can perform is a second legacy `initialize` (rejected `-32600`, no host constructed) or a switch of era (served on the same host) — and, *(rev 5.1, round-1 P1-01)* to carry §6.6 (0)'s witness literally, the server layer is split into `createServer(host)` and `serve(server, input, output)`: `main.ts` calls `serve` once on the process streams and owns the shutdown wiring, while **T-HOST attaches two sequential injected stream-pair connections to one server over one host** (the first pair ends without triggering shutdown, which belongs to `main.ts`), on connection one performs the legacy `initialize` (process-scoped legacy state, §3.4), the era switch and a repeated `initialize` (`-32600`); on connection two — the legacy state is process-scoped, so its first `initialize` would already be a repeat — serves a legacy `tools/call` without any `initialize`, then modern requests, then one repeated `initialize` (`-32600`); and asserts by object identity that every dispatched call on both connections reached the same host and that the host factory (spied in the entry's composition function driven in-process) was called exactly once. **Across processes a second connection is a second process, which is a second host and a fresh authorization by construction** — T-STDIO spawns the bundle twice with the same environment and shows two independent hosts; that second half is not a weakness to hide: it is precisely the restart limitation of (3)–(5). Owner-run mutants: a host per server connection (in `serve`), a host per `initialize`, a host per `tools/call` → T-HOST reds.
- **(1) Composition only through `createSupervisedHost` without `onFillAuthorization`.** The `createSupervisedHost` argument in `main.ts` is an object literal with exactly the property names `backend` and `canary`, no spread, no computed key, no `browser`/`launcher`/`onFillAuthorization` (T-STRUCT pins the exact two-key literal; T-RC-10 already pins the single-object-literal shape and the allowed key set at `inspectHostShape` `fillService.structure.test.ts:604-612`, typed/syntactic/spread authority at `inspectHostArgument` `:657-669` and computed-key writes at `inspectAuthorityAccess` `:539-555`, all over `main.ts` automatically once it exists). No adapter file — production or test — references `onFillAuthorization`, `createFillService`, `FillAuthorizationLifecycle`, `createFillAuthorizationDomain`, `renew` or an `authorization` identifier/key (T-STRUCT; adapter tests compose through `composeSupervisedHost` with the `fakeFillService` of `host.test.ts:46-53` or through `createSupervisedHost`, never through `createFillService`). **The only `createFillService` call the adapter's process reaches is `src/supervisor/host.ts:94`** — pinned by the existing T-RC-10 assertion that `createFillService` is called from `src/supervisor/host.ts` alone (`:517`, `:499`), which runs over every production file including `src/adapters/**`; T-RC-10's `HOST_CALL_FILES` (`:595-596`) gains `src/adapters/mcp/main.ts` in the owner's integration commit (§4.6 item 3) — the one `src/core/**` line the slice touches, a test-only literal, every other assertion and every existing file's verdict unchanged; **any other T-RC-10 red on an adapter file is a STOP** (§5).
- **(2) `handle-exhausted` in the fixed vocabulary; setup after it is a fixed template, never a renewal.** `SCHEMA.md:49,96`, `src/core/types.ts:44`, `setupReasonFor` → `null` (`fillService.ts:290-292`), `createSetupResult` (`results.ts:51-55`). T-SEAM/T-ERR: a fake fill service returning `{ok:false, reason:'handle-exhausted'}` is delivered byte-identical with `isError:false`; the following `request_vault_setup` (any of the three `reason` values) returns the fixed template; the fake's `fill` is called exactly once per `fill_from_vault` (no adapter retry).
- **(3) Renewal in M8 v1 is process restart by the parent (R12); no stdio renewal method exists.** The method set is closed (§3.4 step 6): T-WIRE asserts `-32601` for renewal-shaped methods in both eras (`tinyvault/renew`, `authorization/renew`, `fill/renew`) and T-STRUCT asserts the dispatch `switch` has exactly the nine cases (§3.5). The adapter exposes no `SupervisedHost` member other than through the nine tools (`HOST_KEYS`, `fillService.structure.test.ts:262-263`, has no renewal member to expose).
- **(4) The budget is per adapter process** — a second legitimate task that logs in with the same handle in one process is refused (`docs/m7-runtime-fill-control-packet.md` §4 row 17, R12); accepted as the initial integration model (O-RC-8). Within the harness's process lifetime this is inherited from the composition and witnessed in-tree by T-RC-1 (`src/core`), not by an adapter test: no adapter test drives a real login page, so **M8 witnesses the composition (T-HOST, T-STRUCT, T-RC-10 over `main.ts`), not the budget end-to-end** — stated, not hidden (§9).
- **(5) Process restart is a fresh trusted authorization decision — who can cause one, documented and measured (fact 16; register (b)):** (i) the human (exit and restart, the client's `/mcp` reconnect); (ii) **the harness automatically** — the installed client respawns an adapter that dies by exit code or by signal and routes the next call to it with no model-visible sign (F6), and its discovery probe is itself a throwaway process (F4); (iii) **the model, in any harness that gives it a shell or process control** — the installed client's Bash tool can end the adapter (which the harness then respawns) or spawn a second copy with the same environment; this half is reasoned from the harness's tool surface, not measured (no probe gave the model a shell); (iv) the adapter's own exits `3`/`4`/`5`/`6`, of which only the framing failure `5` (a line over `MAX_LINE_BYTES`) is reachable from a tool argument, bounded by the harness's maximum tool-argument size (unreachable at the probe model's 32k output tokens; not bounded in general). **Therefore the claim that renewal is inaccessible to the model is withdrawn for Claude Code and for every harness with automatic restart or a model-accessible shell** (R5 of the runtime-control packet becomes a stated limitation of M8, not a residual): the honest claim is that **the adapter itself offers no renewal path — no method, no host member, no option — and one bounded injection per handle holds for the lifetime of one adapter process**; the harness, not TinyVault, decides how many processes a task gets. **User decision 2026-09-12, accepted as the stated limitation:** process recreation grants fresh authorization, so **the adapter does not establish renewal isolation in the tested Claude Code configuration**; the sentence stands beside the setup instructions (§4.5) and is verified — not claimed away — by the final interoperability check (§6 step 3). `SCHEMA.md`/README carry it (A11, §4.5) with the measured facts; D1 per-caller entitlements remains the eventual home of a renewal policy.
- **(6) Stable handle identity (R20)** — one budget per credential requires an injective handle→record map; true of the shipped backend (`localFile.ts:144-149`) and carried into M9's backend work as a stated requirement (BACKLOG sentence, §4.5); M8 adds no backend.
- **No renewal audit record in this slice (O-RC-3):** a second `{ok:true}` for one handle in one adapter process cannot be distinguished by the adapter from a control that did not fire; the in-tree witness is T-RC-1 and the composition pins; the attribution limitation is retained (§9).
- **Completion semantics for any future adapter-side scorecard row (O-E8c-1):** the harness's receipt oracle (`testbed/completion.ts` `verifyCompletion`, `runnerExecution.ts:112`, with the max-turn/truncation rules `:148`) decides completion; `handle-exhausted` never decides completion and never constitutes a leak; a refusal that prevents login is a noncompletion, a refused second fill after login may retain completion, and a refusal never erases an independently detected leak. M8 defines no scorecard row (§0 "Is not"); this sentence binds whoever adds one.
- **The only live-qualification claim M8's documents may cite** is `docs/m7-runtime-fill-control-packet.md` §8.3's bound as read in cohort `PFc7eGp2` (register "E8c — attempt `E8c-A1-N10`"): in that fixture and configuration the reference did not obtain a second injection after login (pooled reference 0/50, Wilson 0.0–7.1%, a heterogeneous bound, never a proof of zero); `ODMFYbwH` stays the measured, unqualified historical result. No sentence in M8's docs may extend it to the MCP transport, to any client, or to any harness. **E8c's evaluated configuration** (the in-process reference agent under `SKILL.md`, the five Docker fixtures, Haiku 4.5, N = 10 per cell) **is distinct from MCP interoperability evidence** (the installed client, a throwaway vault, a scripted driver); the docs keep them apart and let neither stand in for the other (user decision 2026-09-12).

**Design consequence recorded here, not re-opened (D-M8-12):** rev 4's startup order (host first, then the reader) stands. Under the client's modern opt-in every discovery probe therefore costs one Chromium launch in a throwaway process and the `server/discover` answer waits for host creation and the Chromium launch (duration unmeasured here — the probe server launched no browser; round-1 P2-03); the client's probe timeout is undocumented and a timeout falls back to legacy `initialize` on a new process (`basic/versioning.md:171`), which this server serves — measured harmless, recorded as a residual. Answering `server/discover`/`tools/list` before the host exists would add a "host pending" dispatcher state for a constant answer; not worth the state.

## 4. Scope by deliverable

### 4.1 Gate amendments — security core, slice B

§3.2 in full (five sites, the `sourceZone` branch, the two self-test edits, every fixture). **Plus** `scripts/docker-invocation.mjs`: (a) `export` `reviewProfiles`; (b) bind spawn pins whenever a profile exists for the file — `:164-166` becomes `(profile !== undefined || relative.startsWith('scripts/')) && spec === 'node:child_process'`; (c) `scripts/docker-invocation.selftest.mjs:56-60`'s synthesizer is re-keyed from `relative.startsWith('scripts/')` to `reviewProfiles[relative] !== undefined || relative.startsWith('scripts/')`, emitting for a profiled row the exact conforming source for the profile's single binding (`import { spawn } from 'node:child_process'; spawn(process.execPath, [], { env: {}, stdio: 'pipe', shell: false });`) — **the one existing docker self-test case the slice edits; its verdict (PASS) is unchanged** (round 3, Opus P1-04); (d) selftest mutants at the exact T-STDIO path for executable, argv, options, reference escape and call count. No existing `scripts/**` verdict moves (both predicates were already true there).

### 4.2 Protocol layer (`protocol.ts`) — slice A (Sol)

Everything in §3.4 and §3.9 that does not touch the host. Pure functions over injected `Readable`/`Writable` and a `dispatch(name, args)` callback; no `process.*`; no host knowledge.

### 4.3 Tool layer, server, entry (`tools.ts`, `server.ts`, `main.ts`) — slice B (Astra)

§3.5–§3.8, §3.10.

### 4.4 Tests (names fixed)

All under `src/adapters/mcp/`; pure Node unless marked *owner-run*; **no adapter test file may contain `.expose(`, `.consume(`, `new Secret(`, `primitives.open(` or `crypto_aead_`** (fact 11).

- **T-SEAM** `server.seam.test.ts` — compose on `composeSupervisedHost` with `FakeSessions`/`fakeFillService`/`EvidenceLease(CANARY)` (`host.test.ts:55-91`), drive through the real framing over injected streams. For each of the eight captured tools: plant the canary (out-of-contract fields for closed-enum controls, as `plantResult` at `host.test.ts:110-119`), call through MCP, drain, `finish()` → `verdict:'fail'`, `matched:true`, `transform:'raw'` (`evidenceIndex` not pinned); legitimate path byte identity, `verdict:'pass'`; `browser_snapshot` with a caller-typed canary visible passes; *(rev 5)* a fake fill outcome `{ok:false, reason:'handle-exhausted'}` delivered byte-identical, `isError:false`, the fake's `fill` called exactly once, and a following `request_vault_setup` (each of the three reasons) returning the fixed template. **Mutants (owner-run, red then restored):** M-1 canned host-shaped result without `host.tools`; M-2 `JSON.stringify(R, null, 2)` (bare `JSON.stringify(R)` is equivalent, not a mutant); M-3 the adapter dispatches `browser_snapshot` through `host.tools.browser_type` (snapshot byte identity red); M-4 a host throw swallowed into `isError:false` (T-ERR red).
- **T-SCHEMA** `tools.schema.test.ts` — order and count 9; seven `toBe` the registry objects; two hand-written schemas exact; frozen; digest `c7475344…` re-asserted; the switch's nine case literals equal the nine names in order; a copied-and-edited description reds.
- **T-ARGS** `tools.args.test.ts` — invalid arguments per tool → `-32602`, executor spy count 0; `{}` and absent `arguments` accepted for `list_vault` and `browser_open_session` only (executor called with no argument for the latter); `arguments: null`/non-object → `-32602`; integer and string ids; per-method params invalid → `-32602`.
- **T-ERR** `tools.errors.test.ts` — against a real `composeSupervisedHost`: the two fixed messages → `isError:true` with that text (`VAULT_TOOL_FAILURE_MESSAGE` via the host's quiescing refusal path `host.ts:301-307` or a throwing fake fill service; `BROWSER_OPEN_FAILURE_MESSAGE` via a throwing `openSession`); the `settleAttach` residual via a real lease with a controllable pending `trackAttach` promise rejected with `Error('x-secret-y')` while `browser_open_session` awaits `settleAttach` → `'tinyvault: internal error'`, `x-secret-y` absent from stdout and stderr, stderr exactly `tinyvault-mcp: internal error\n`; **the per-call drain: wrap the host with a pass-through `drainEvidence` spy, assert exactly one adapter invocation after every successful `tools/call`, configure it to reject and assert the success envelope is byte-identical; owner-run mutant M-6: delete the adapter's per-call drain → the invocation-count assertion reds** (round 3, Sol P1-06); unknown tool `-32602`; call after shutdown began `-32600`; modern `isError` results carry `resultType`/`_meta`.
- **T-WIRE** `protocol.wire.test.ts` — provenance-headed fixtures for every §3.4 step: modern `server/discover` (exact fields, `_meta` key exactly `io.modelcontextprotocol/serverInfo`), `tools/list` (cache fields), `tools/call`, `-32022` before method lookup, `_meta` type/length checks (`io.modelcontextprotocol/protocolVersion` object → `-32602`; 65-byte string → `-32602`; `io.modelcontextprotocol/clientCapabilities` array → `-32602`), `inputResponses`/`requestState` → `-32602`, missing `_meta` `-32602`, *(rev 5)* **the probe shapes verbatim** — the run-6 modern `server/discover`/`tools/list`/`tools/call` with the three namespaced keys plus `progressToken` and `claudecode/toolUseId`, the run-1 legacy `initialize` (member order `method, params, jsonrpc, id`, id `0`, `roots`/`elicitation` capabilities, five-field `clientInfo`) and the run-2b legacy `tools/call` with `_meta:{progressToken, "claudecode/toolUseId"}` — all served; ids `0` and `"server-discover-probe-1"` served; a `params` member other than the method's and `_meta` → `-32602`; renewal-shaped methods `tinyvault/renew`, `authorization/renew`, `fill/renew` → `-32601` in both eras; **modern `ping` → `-32601`, modern `initialize` → `-32601`**, unknown method `-32601`, per-method invalid params, absent/`null` arguments; legacy `initialize` (echo when supported; `2025-11-25` when not; invalid params `-32602`) → `ping` before the response? (queued behind it — answered `{}`) → `tools/list` immediately after the response (served) → `initialized` → `tools/list` (no cache fields), second `initialize` `-32600`, `initialized` before `initialize` ignored, **legacy id reuse after completion and after cancellation → `-32600`** (modern reuse after completion accepted); era concurrency (a modern request before, between and after legacy requests served modern); classification (a `method: 'tools/list'` message with no id → `-32600`; a `notifications/…` message with an id → `-32600`; a `{jsonrpc,id,result}` object → ignored, no output); JSON-RPC shape (null id, batch in every era, duplicate outstanding id, 65th outstanding); parse failure and non-object top-level → `-32700` id null, loop continues; oversized line and invalid UTF-8 → framing failure then shutdown (exit `5`), multibyte at the byte boundary, a line split across chunks; cancellation: in-flight (response suppressed, next request served), queued (never started — executor spy), unknown id (ignored), malformed notification (ignored, no output); exact envelopes with forbidden extra fields asserted.
- **T-STDOUT** `protocol.stdout.test.ts` — exact stdout line inventory per case; an extra syntactically valid JSON-RPC line reds; stderr never on stdout; backpressure (`false` then `drain` awaited); write error → exit `6` path with `abort`/`closeAll` spies called. Stated limit: the real-`process.stdout` discipline of `main.ts` is proved by T-STDIO and T-STRUCT's `console.` pin.
- **T-MUTEX** `server.mutex.test.ts` — part 1 (adapter): two back-to-back `browser_type` requests on one session, slow first, strict order; a second `tools/call` behind them answered after; cancellation of the queued second suppresses it. Part 2 (host, real mutex, bypassing the adapter loop): compose `createBrowserSessionHost` with the fake `newContext` pattern of `session.test.ts:137-138`, call `host.tools.browser_type` twice concurrently, assert FIFO; owner-run mutant replacing `#mutex.runExclusive(sessionId, op)` at `session.ts:263` with a direct `(async () => op(...))()` must red. Linearization: close then fill → `session-unknown` bytes, `isError:false`; fill then close → the fill's result, then `{"ok":true}`; never-issued session id → `session-unknown`.
- **T-LIFE** `server.lifecycle.test.ts` — real `EvidenceLease`, pass-through spies: EOF with two sessions open → in-flight handler awaited, `quiesceEvidenceProducers` with both hooks, then `finish`, then `closeAll`, never `browser_close_session`; an in-flight `list_vault` at EOF delays quiesce; the deadline path (never-settling `list_vault`) → cancelled, `abort`, `closeAll`, exit `3`, no response line, and a later rejection of the abandoned handler produces no output and no exit change; **differential**: two runs with byte-identical wire input and identical host results, one whose shared lease received `lease.captureTrusted(CANARY)` directly → byte-identical stdout, stderr and exit code; **owner-run mutant M-5: `main.ts` exits `7` (or writes one extra stderr line) when the discarded `finish()` verdict is `'fail'` → the differential reds** (round 3, Opus P2-07); `finish` throwing `FINISH_PRECONDITION_MESSAGE` → `abort` called, exit `3`, lease inactive afterwards (the fake's `openSessionCount()` returns 0 after `abortSessions()`, so a second `finish()` throws the inactive-lease error); `quiesce` throwing `CAPTURE_FAILED_MESSAGE` → same; `closeAll` throwing → exit `3`; startup failures (missing env; empty canary override → no browser launched; **missing `quiesceEvidenceProducers` after creation → `abort`, `closeAll`, exit `4`, lease inactive**) → exit `4`; framing failure → exit `5` after the normal sequence; stdout error → `abort`, `closeAll`, exit `6`, lease inactive; an uncaught exception after the host exists → one fixed stderr line, `abort`, `closeAll`, exit `3`; collisions per priority (framing + finalization failure → `3`; startup + anything → `4`); SIGTERM identical to EOF; *(rev 5)* SIGINT and SIGHUP identical to EOF; SIGINT + SIGTERM + EOF within 100 ms → one sequence, one exit; a trigger during host creation (factory pending) → the sequence runs after creation, exit `0`; a trigger during host creation whose factory then rejects → exit `4`; idempotent.
- **T-STDIO** `server.stdio.test.ts` *(owner-run; child process; Chromium; in `make test`)* — §3.10.
- **T-HOST** *(rev 5; shape per rev 5.1)* `server.host.test.ts` — one server (`createServer(host)`) over an identity-spied host (`composeSupervisedHost` on the fakes), **attached to two sequential injected stream-pair connections** (`serve` twice; the first pair's EOF ends only that connection — shutdown wiring lives in `main.ts`): connection one: legacy `initialize` → `tools/list` → three `tools/call` → a second `initialize` (`-32600`) → modern `server/discover` → modern `tools/call` → legacy `tools/call`; connection two (the legacy state is process-scoped, §3.4, so no `initialize` is sent): legacy `tools/call` served → modern `tools/call` → a repeated `initialize` (`-32600`); assert every dispatched call on both connections reached the same host object (`toBe`), the server layer never calls any host factory, and — through the entry's composition function driven in-process with a host-factory spy — the factory count is exactly `1` for the process lifetime regardless of connections or messages; owner-run mutants: a host per connection in `serve`, a host per `initialize`, a host per `tools/call` → red.
- **T-STRUCT** `adapters.structure.test.ts` — **no production file** under `src/adapters/**` (excluding `*.test.ts`) imports `redaction`, `Secret`, `SessionMutex`, `playwright`, `src/browser/**`, `testbed/**` or `scripts/**`; *(rev 5)* **no adapter file, production or test, contains the identifiers or string literals `onFillAuthorization`, `createFillService`, `FillAuthorizationLifecycle`, `createFillAuthorizationDomain`, `renew` or `authorization`**; the `createSupervisedHost` call's single argument is an object literal whose property names are exactly `backend` and `canary` (no spread, no computed key, no method) and the call's nearest enclosing function is the entry's startup function, not any per-message handler; adapter **test** files may import `src/browser/**` and `src/supervisor/**` for composition types and fakes and remain bound by the retention-token rule and the `playwright`/`testbed/**`/`scripts/**` exclusion (round 3, Opus P2-01); no `console.` in any adapter file; every production file < 400 lines; `main.ts` is the only file referencing `process.stdin|stdout|stderr|exit|on(`; `tools.ts` imports `serializeExact` from `src/agents/transcript` and calls it at the `text` site; composition pin: `main.ts` contains exactly one `createSupervisedHost` call whose awaited result is the argument of exactly one server-factory call; no other adapter production file references `createSupervisedHost` or `composeSupervisedHost` (adapter tests may import both for composition); the dispatch `switch` has exactly nine `case` clauses and a throwing `default`, every case body a member call on the injected host parameter — owner-run mutant sourcing a tool from a local object (red).
- **Existing tests that must stay byte-identical:** `src/supervisor/host.test.ts`, `loop.acceptance-j.test.ts`, `testbed/acceptance-j.integrity.test.ts`, `src/browser/retention.test.ts`, and `src/core/fillService.structure.test.ts` **except** the `HOST_CALL_FILES` literal (`:595-596`; §4.6 item 3, owner-applied) — T-RC-10 must otherwise pass unchanged over the tree that now contains `src/adapters/mcp/**`.

### 4.5 Docs (proposed patches returned as text; applied by the owner)

`SCHEMA.md`: "MCP stdio adapter contract" — the §0 vocabulary and boundary, nine tools, envelopes, precedence, era rules, exit codes, the non-relocatable-bundle sentence, the honest tripwire and seam sentences (including, verbatim, "the randomly minted production canary does not establish detection of actual credential leaks" — O-4, rev 5.1), *(rev 5)* **the runtime-authorization paragraph**: one host and one fill-authorization domain per adapter process; no renewal method, member or option; `handle-exhausted` refuses the second injection of a handle for the process lifetime; **process restart is a fresh authorization, the harness decides who can cause one, the installed client respawns a dead adapter automatically (measured 2.1.258), and the adapter makes no claim that renewal is inaccessible to the model in any harness with automatic restart or a model-accessible shell**; any backend must state handle→record injectivity (R20); the only live-qualification citation permitted is the runtime-control packet's §8.3 bound in its fixture and configuration, stated as distinct from MCP interoperability evidence; **beside the setup/launch instructions, verbatim: "Process recreation grants fresh fill authorization; the adapter does not establish renewal isolation in the tested Claude Code configuration (2.1.258, measured 2026-09-12)."** (user decision 2026-09-12) — same commit as slice B. `README.md:33,53` (the same restart sentence beside the launch command, plus the `MCP_PROTOCOL_NEGOTIATION` note: default legacy `2025-11-25`, modern `2026-07-28` under the client's opt-in); `ORIENT.md` paragraph; `BACKLOG.md` A4 per **O-8** and a new M9 line "any backend must state handle→record injectivity (R20)"; `docs/phase-0-plan.md:559` ✅ after merge; `PROJECT-SPEC.md` §6 checkbox after merge.

### 4.6 The two additive exports outside `src/adapters`

1. `src/agents/loop.ts`: `export function matchesSchema(...)`.
2. `src/supervisor/host.ts`: `export { BROWSER_OPEN_FAILURE_MESSAGE } from '../browser/controls';` in the block at `:43-48`.
3. *(rev 5, owner-applied in the integration commit, not by the implementer)* `src/core/fillService.structure.test.ts:595-596`: the `HOST_CALL_FILES` literal gains `'src/adapters/mcp/main.ts'` (sorted position). A test-only literal; the pin's verdict for every existing file is unchanged; T-RC-10 then enforces its host-argument shape rules over `main.ts` (§3.11 (1)).

### 4.7 Capability rows and root-of-trust pins

`scripts/docker-invocation.mjs` + selftest: `'src/adapters/mcp/server.stdio.test.ts': ['node:child_process']` and `reviewProfiles['src/adapters/mcp/server.stdio.test.ts'] = { spawn: { executable: 'node', options: ['env','stdio','shell'] } }`, bound by §4.1. Root of trust: `package.json#scripts` (+`build:mcp`) and `Makefile` (+`mcp`) digests at `testbed/rootOfTrust.test.ts:15-16`, owner-applied (**O-5**).

## 5. Non-goals, leave-alone, STOP triggers

**Leave alone:** `src/core/**`, `src/browser/**` (T-MUTEX mutant only, restored), `src/backends/**`, `src/shared/**`, `src/supervisor/**` except §4.6 item 2, `testbed/**` read-only except the two digest values at `testbed/rootOfTrust.test.ts:15-16` (owner-applied), `SKILL.md`, `src/agents/prompt.ts`, `src/agents/loop.ts` except §4.6 item 1, `package.json`/`Makefile` except the owner-applied §3.10 rows, `package-lock.json`, the vitest configs, every `scripts/check-*.mjs`, `scripts/test-*.mjs`, `scripts/retention/**`, the timing test files, `PLAN.md`, `BACKLOG.md`, the registers, `README.md`, `ORIENT.md`, `SCHEMA.md`, `docs/**` (proposed patches only), `.claude/memory/**`, `docs/m7-e8b-live-cohort-preregistration.md`. The gate self-test edits named in §3.2 (C6) and §4.1(c) are the only permitted edits to existing self-test cases; the `HOST_CALL_FILES` literal (§4.6 item 3) is the only permitted edit under `src/core/**` and is owner-applied.

**STOP and report if:** a new npm dependency appears necessary; any `package.json`/`Makefile`/vitest-config change beyond §3.10; a `src/supervisor` edit beyond §4.6 item 2; the `matchesSchema` export is blocked; a pinned page contradicts a §3.4 sentence; any test must weaken an existing pin; any existing self-test case beyond the three named in §3.2/§4.1 changes text, or any existing case changes its verdict; the sandbox cannot run `tsc` or the two gate selftests; any byte outside the §0 vocabulary and boundary would have to appear on stdout, stderr or the exit status; anything would make a caller-visible byte, stderr byte or exit code depend on a tripwire match; the bundle cannot be produced with `--packages=external` and no new dependency, or cannot resolve its external packages when spawned; the retention sweep or a structural pin reds on an adapter file; *(rev 5)* T-RC-10 reds on an adapter file for any reason other than `HOST_CALL_FILES` membership; any adapter code path would need a second host, a renewal, or a `createSupervisedHost` option beyond `backend`/`canary`; a Chromium process of the adapter survives the SIGINT-then-SIGKILL case of T-STDIO; the installed client (either era) cannot complete `tools/list` and one `list_vault` against the bundle in the manual interoperability run; **any conflict discovered during implementation between this locked contract and a pinned page, a tree fact or a gate behaviour — surface it to the user for disposition as a recorded amendment, never resolve it silently in code** (user decision 2026-09-12).

## 6. Ladder (tier 🔴)

1. **Paper ladder — complete** (rounds 1–3; `docs/m8-review-findings.md`). No round 4. The owner's post-cap confirmation pass is rev 4 itself; rev 5 is the owner's contract correction ordered by the user on 2026-09-10, fact-checked by read-only Sol rounds capped at two (§8; register "Rev 5 delta review").
2. Lock → the user receives this packet with O-1…O-8 → **only then** implementation in one worktree, sequentially: **slice A on `gpt-5.6-sol`** (`protocol.ts`, T-WIRE, T-STDOUT), owner gates, then **slice B on `gpt-6-astra`** (both gate amendments with their self-test edits, `tools.ts`/`server.ts`/`main.ts`, §4.6, T-SEAM/T-SCHEMA/T-ARGS/T-ERR/T-MUTEX/T-LIFE/T-STRUCT/T-STDIO, capability rows), uncommitted, single writer, "Not run: sandbox" where applicable.
3. Owner commits with explicit paths (§3.10 rows, rootOfTrust digests and the `SCHEMA.md` section in the same commit as slice B), runs `make test`, `make test-docker` (unchanged), `make eval-stub`, both gate scripts, the mutant tables (T-SEAM M-1…M-4, M-5, M-6, T-STDOUT, T-MUTEX part 2, T-STRUCT's local-tool-map mutant, the docker-invocation selftest mutants), T-STDIO, verifies every T-WIRE fixture header against its page, and records the **final interoperability check** (user decision 2026-09-12) against `dist/tinyvault-mcp.mjs` with a **throwaway vault** and a local login page on the authorized origin, stdout captured raw: (1) a first `fill_from_vault` → `{ok:true}`; (2) a second fill on the same handle in the same process → `handle-exhausted`; (3) the adapter process restarted by its parent; (4) whether another fill becomes possible on the fresh process — expected **yes**, and recorded as **verification of the stated limitation, never as a passing security claim**; plus `tools/list`, one `list_vault` and one wrong-origin refusal, and the installed client in both eras (default and `MCP_PROTOCOL_NEGOTIATION=auto` as a one-off environment variable) for the handshake shapes. **A scripted stdio driver (no model) covers (1)–(4) and needs no allowance; any paid client call — the installed Claude Code driving a model — requires an explicit provider-spend allowance from the user before execution, and the lock grants none.**
4. Post-implementation: Codex adversarial (Astra), blind Opus QA, `/security-review`; capped at three rounds; last-round P1 criteria: a layers-1–2 leak (a secret or secret-derived byte reachable in any MCP result, stdout line, stderr line or exit code), an undeclared layer-4 blind spot (a successful host payload delivered without passing `createTools`), a red `make test`, a gate amendment that admits a data-plane → supervisor path, or a spawn that escapes the docker-invocation pin.
5. Milestone close: read-only cross-model project assessment; merge and push only on the user's word.

## 7. Acceptance ledger

| # | Criterion | Verification |
|---|---|---|
| A1 | Nine tools in the fixed order; seven are the frozen registry objects; digest unchanged; switch cases equal the names | T-SCHEMA |
| A2 | Every successful host payload over MCP is byte-identical to the direct host result and obtained through `host.tools`; eight captured, snapshot exempt; production composition pinned; per-call drain invoked | T-SEAM + M-1…M-4; T-ERR + M-6; T-STRUCT composition pin + mutant; T-STDIO |
| A3 | Nothing outside the §0 vocabulary and boundary on stdout, stderr or the exit status; no secret or secret-derived byte anywhere; planted-vs-clean differential identical and falsifiable | T-ERR, T-STDOUT, T-LIFE + M-5; `/security-review` |
| A4 | Fill via MCP goes through the same gate and choke-point; invalid arguments never reach the host; wrong-origin fill bytes unchanged | T-ARGS; T-SEAM; interop evidence |
| A5 | Sequential dispatch; the host's exclusion proved with the real mutex; close/fill linearization; cancellation semantics | T-MUTEX (both parts + mutant); T-WIRE cancellation cases |
| A6 | Dependency gate: C1–C5 with every §3.2 fixture, reason strings asserted; real tree green; data-plane → supervisor still red; only the named self-test edits, no verdict moved; docker-invocation extension with its selftest mutants | both gate scripts and both selftests (in `make test`) |
| A7 | Wire conformance to the pinned pages in both eras, exact envelopes with cache fields, precedence, framing limits, cancellation, id-reuse rules | T-WIRE (headers owner-verified), T-STDOUT |
| A8 | Lifecycle: the host's own quiesce; in-flight vault tools awaited under the deadline; abort on every finalization/stdout/contract failure (lease dropped); no uncaught byte; verdict unobservable; exit priority | T-LIFE; T-STDIO |
| A9 | Structural pins unchanged; adapter files < 400 lines; forbidden imports/tokens absent per the production/test split; `serializeExact` at the `text` site | T-STRUCT; `make test` |
| A10 | The bundle builds with `--packages=external`, metafile proves no bundled package, `build:mcp` string pinned; the bundle runs from the checkout; rootOfTrust digests updated in the same commit; `make test` green from a literal clean clone (T-STDIO included); `make test-docker` 7/7 and `make eval-stub` unchanged | T-STDIO; `testbed/rootOfTrust.test.ts`; owner gates |
| A11 | Docs: SCHEMA section with the §0 vocabulary and boundary (same commit), README/ORIENT incl. the non-relocatable-bundle sentence, BACKLOG per O-8; *(rev 5)* the runtime-authorization paragraph with the restart-authority statement and the withdrawn claim, the R20 injectivity sentence, the §8.3-only qualification citation; register entry with the threat-model note, the tripwire/seam sentences and every §9 residual | owner integration commit |
| A12 *(rev 5)* | Exactly one `SupervisedHost` per process; the server layer cannot construct a host; a second connection to the same server, a second legacy `initialize` or an era switch reuses the one host; a second process is a second host | T-HOST (+ its three owner-run mutants); T-STRUCT composition pin; T-STDIO two-spawn case |
| A13 *(rev 5)* | Composition through `createSupervisedHost` with exactly `{backend, canary}`; no adapter file references `onFillAuthorization`, `createFillService`, `FillAuthorizationLifecycle`, `createFillAuthorizationDomain`, `renew` or `authorization`; the only `createFillService` call reached is `host.ts:94`; T-RC-10 green over `main.ts` with only the `HOST_CALL_FILES` edit | T-STRUCT; `src/core/fillService.structure.test.ts` (T-RC-10) in `make test`; `git diff` of the integration commit |
| A14 *(rev 5)* | `handle-exhausted` delivered as a host payload; setup after it is the fixed template; no renewal method in either era; the nine-case switch | T-SEAM/T-ERR case; T-WIRE `-32601` cases; T-SCHEMA |
| A15 *(rev 5)* | Wire conformance to the reconciled `_meta` keys; the probe shapes of both eras served verbatim; `_meta` accepted and ignored in both eras; ids `0` and long strings; SIGINT/SIGHUP shutdown, startup-time trigger, the harness's probe sequence and kill grace with no Chromium survivor | T-WIRE fixtures (headers owner-verified against the fetched copies); T-LIFE; T-STDIO; manual interop evidence in both eras |

**§6.6 → ledger map *(rev 5)*** (runtime-control packet requirement → where rev 5 carries it → what proves it):

| §6.6 item | Rev 5 home | Ledger | Verification |
|---|---|---|---|
| (0) one host per process; second-connection reuse test | §3.11 (0), §3.7 | A12 | T-HOST (two sequential connections on one host) + its per-connection/per-`initialize`/per-call mutants; T-STRUCT; T-STDIO two-spawn (the cross-process half) |
| (1) `createSupervisedHost` without `onFillAuthorization`; no lifecycle/domain reference; the only `createFillService` call is `host.ts:94`, pinned structurally | §3.11 (1), §4.6 item 3 | A13 | T-STRUCT literal/identifier pins; T-RC-10 (`fillService.structure.test.ts:495-499,517,539-555,604-612,657-669`) over `main.ts` |
| (2) `handle-exhausted` in the fixed vocabulary; `setupReasonFor` → `null`; fixed template, no renewal | §0, §3.6, §3.11 (2) | A14 | T-SEAM/T-ERR case; `SCHEMA.md:49,96` |
| (3) renewal = parent restart; no stdio renewal method | §3.11 (3), §3.4 step 6 | A14 | T-WIRE `-32601` cases; T-SCHEMA switch pin |
| (4) per-process budget accepted (O-RC-8) | §3.11 (4), §9 | A12/A13 (composition only) | T-RC-1 in `src/core` is the end-to-end witness; stated as not adapter-witnessed |
| (5) restart authority documented and verified; the "inaccessible to the model" claim withdrawn where the model can restart | §3.11 (5), fact 16 F4–F6/F9, §4.5, §9 | A11, A15 | probe record (register (b)); manual interop `kill` case; SCHEMA/README sentences |
| (6) stable handle identity carried to M9 (R20) | §3.11 (6), §4.5 BACKLOG line | A11 | `localFile.ts:144-149` today; M9 requirement sentence |
| O-RC-3 no audit record | §3.11, §9 | — | recorded limitation |
| O-E8c-1 completion semantics | §3.11 | — | binds any future scorecard row; none in M8 |
| §8.3 the only qualification citation | §3.11 last bullet, §4.5 | A11 | docs review at integration |

## 8. Paper rounds — the cap has been reached

Three rounds were run; round 3 was the cap with the criteria stated in advance ((a) contradiction with a locked sentence, (b) a test that cannot fail or an equivalent mutant, (c) a scope line beyond the authorized security-core/root-of-trust changes, (d) a wire claim contradicting the pinned pages, (e) a wrong environment fact). Rev 4 absorbs every in-criteria P1 from round 3 and, as the owner's confirmation pass, the verified P2/P3 wording; no further review round runs on paper. Any later change to a §3 sentence before implementation is an owner amendment recorded in the register, not a re-opened round. **Rev 5 is such an amendment** (ordered by the user on 2026-09-10 as a focused contract correction): its delta from rev 4 is fact-checked by read-only Sol rounds capped at two, with P1 restricted up front to (i) a rev 5 sentence contradicting a pinned page, the probe record or the tree at `08d4d01`, (ii) a §6.6/O-RC-3/O-RC-8/O-E8c-1 requirement not carried or carried in substance-changing words, (iii) a new test that cannot fail or a named mutant that is equivalent, (iv) a rev 4 decision or user decision silently changed; dispositions in the register.

## 9. Open questions for the user and recorded residuals

- **O-1 Era support — resolved on measurement (rev 5), for the user's lock.** **Dual-era, served concurrently (§3.4).** The authorized probe ran (fact 16): the installed client opens legacy `2025-11-25` by default and modern `2026-07-28` only under its documented opt-in; a modern-only server would fail the default client at `initialize` (`basic/versioning.md:172`); `2025-06-18` stays supported at no cost. Owner recommendation: lock as written.
- **O-2 Evidence persistence.** **None in M8.**
- **O-3 `outputSchema`.** **Omit** (`structuredContent` without it is permitted; clients cannot validate it; recorded).
- **O-4 Tripwire in the standalone composition.** **Keep one composition path, minted canary, env override.**
- **O-5 Root-of-trust pins.** **Approve** `build:mcp`, the `mcp` target and the two digest updates.
- **O-6 Tool surface.** Confirm the nine-tool MCP surface.
- **O-7 Slice routing.** **Sol for `protocol.ts`, Astra for the rest.**
- **O-8 BACKLOG A4.** Its premise is stale (`finish()` refuses pending evidence, `evidenceLease.ts:452`; `finishHost` refuses open sessions and admitted operations, `host.ts:280-285`); the remaining truth is narrower (the two unadmitted vault tools; the caller supplies the sequence). **Recommendation:** close A4 as superseded by M6 S4, recording the narrowed residual against the adapter. The user decides.
- **Stated limitation added by rev 5 — not a residual (§3.11 (5); round-1 P1-03):** **restart authority** — in Claude Code and any harness with automatic restart or a model-accessible shell, ending the adapter process renews every handle's budget; the adapter offers no renewal path of its own and claims nothing about the harness (R5 of the runtime-control packet becomes this stated M8 limitation; the shell half reasoned, not measured).
- **Residuals added by rev 5 (all stated in §3.7/§3.11):** the harness's documentation contradicts its measured respawn (F9; interactive mode unmeasured); the SIGKILL and the upper end of the ≈ 0.5–0.75 s grace are inferred from heartbeat cessation, not logged by the target; the adapter's own startup latency under the discovery probe is unmeasured; the per-process budget is composition-witnessed only (T-RC-1 is the end-to-end witness); no renewal audit record (O-RC-3, R13); the framing-failure exit `5` is a model-reachable restart in a harness whose tool arguments can exceed 1 MiB; the kill grace of ≈ 0.5–0.75 s means the shutdown sequence may be truncated under the installed client, in which case no exit code is observed there; the modern discovery probe costs one Chromium launch in a throwaway process and may time out into the legacy path (D-M8-12); the two probe teardown facts and the respawn are measured for `-p` mode of one client version. **Residuals for the register (rev 4):** hand-written framing unfuzzed; `settleAttach` rejections reach the caller only through the catch-all; the production tripwire has no reference value and no consumer; the modern era has no liveness probe and a slow `tools/call` blocks the modern stream; cancellation is best-effort (host work completes; only the response is suppressed); an abandoned unbounded host tool runs to completion against a dropped lease; SIGKILL yields no exit code; shutdown timing unbounded (no probe-P claim); `dist/` is generated, unscanned and non-relocatable; the static graph is hygiene, not containment; T-STDIO owner-run; the spawn pin covers shape, not behaviour; `data.requested` is the one caller-authored value echoed (length-capped string); the dual-era server can report different versions to a legacy `initialize` and a modern `server/discover` (era caching is the client's); C3 does not rule `scripts/**`/`testbed/**` entries and cannot fire for pruned edge classes; the three self-test edits (two dependency-boundary, one docker-invocation) are protected by review and their own selftests, not by a digest; `host.abort()` after a successful `finish()` clears `abortedEvidence`; `content[0].text` and `structuredContent` carry the same bytes twice.

## 10. Round dispositions (summary; full tables in `docs/m8-review-findings.md`)

**Round 1** (rev 1 → rev 2): 18 P1; all verified; one disproved. **Round 2** (rev 2 → rev 3): Sol 9 P1 / 8 P2 / 3 P3, Opus 9 P1 / 16 P2 / 5 P3; three regressions between round-1 fixes found and repaired; one disproved. **Round 3, the cap** (rev 3 → rev 4): Sol 7 P1 / 4 P2 / 1 P3, Opus 5 P1 / 8 P2 / 6 P3. Convergent: modern `ping` does not exist (changelog item 5; both P1); the §0 inventory needed a boundary for host-result values (Sol P1-01) and Node's uncaught paths (Opus P2-02). Disjoint in-criteria P1s, all verified and adopted: Opus — a legacy lock must not affect modern requests (versioning page), T-STDIO's bundle must live under the repository root (Node resolution, measured logic), the docker-invocation self-test synthesizer must be re-keyed (`selftest.mjs:56-60`), C2 edits two existing dependency self-test cases (`selftest.mjs:52-65`; `selftest-fixtures.mjs:209-212` + `vetted.mjs:88-103`); Sol — legacy request ids may never be reused in a session (2025-11-25 basic), notifications never receive responses, common modern request members recognized and rejected, the per-call drain needs a count assertion and mutant M-6, the missing-quiesce check happens after the host exists. Verified P2/P3 absorbed as owner wording: test-file import qualifier, uncaught-exception handlers, `data.requested` validation, notification/response classification, `initialize` with modern `_meta`, the legacy request window at the `initialize` response, differential mutant M-5, abandoned-handler suppression, stdout-failure cleanup, inbound response objects, priority wording, spawn call shape, case-name pin, C3 limits, line citations. **None disproved in round 3.**

**Rev 5 (owner contract correction, 2026-09-12; no review round):** the `_meta` reconciliation (three of four keys were bare — corrected, file:line in the register), the client probe (nine measured facts, fact 16), O-1 resolved on measurement, §3.11 (D-M8-11: §6.6 (0)–(6), O-RC-3, O-RC-8, O-E8c-1, the withdrawn renewal claim), D-M8-12 (startup order stands), T-HOST, the T-STRUCT/T-WIRE/T-LIFE/T-STDIO/T-SEAM additions, A12–A15 and the §6.6 map, §4.6 item 3, the SIGINT/startup-trigger shutdown rule, and the citation re-pin at `08d4d01`. **Delta review (read-only Sol, capped at two rounds):** see the register entries "Rev 5 delta review".

**Rev 5.1 (owner dispositions of the round-1 Sol delta review, 2026-09-12):** T-HOST re-shaped to carry §6.6 (0)'s witness literally (two sequential connections on one server and host; `createServer`/`serve` split); F5 and the header narrowed to what the logs show (stdin open at session teardown, EOF only on the discovery process, SIGKILL inferred, shutdown "may be truncated"); restart authority moved from the residuals to a stated limitation; the T-RC-10 and drain citations re-pinned (`HOST_CALL_FILES` `:595-596`, `inspectHostShape` `:604-612`, `inspectAuthorityAccess` `:539-555`, `inspectHostArgument` `:657-669`; `host.ts:268` → `evidenceLease.ts:442-444`); the O-4 sentence added verbatim to §3.8 and §4.5; optional `_meta` values accepted without inspection (page `:329-330`), stated; the discovery-probe latency labelled unmeasured. Rejected on the evidence: "`run2.log` absent" — the file is in the evidence directory and matches the manifest hash.

**Rev 5.2 (owner confirmation pass after round 2, the cap, 2026-09-12; no third round):** the SIGKILL inference narrowed to processes still alive after SIGINT/SIGTERM (header, §3.7); the `_meta` page locator corrected to `:329-330`; the optional-`_meta` handling reworded as an explicit owner compatibility choice with its conformance uncertainty stated; T-HOST's second connection reuses the process-scoped legacy state instead of re-initializing (one repeated-`initialize` rejection retained). **Rev 5.2 is the lock candidate returned to the user.**

**LOCKED 2026-09-12 (user).** Implementation authorized to the incoming GPT-6 Astra continuity owner through §6 from a fresh Codex session; the stated limitation (§3.11 (5)), the final interoperability check with a throwaway vault (§6 step 3, a verification of the limitation, not a security claim; paid client calls need an explicit provider-spend allowance), the metadata compatibility choices with their conformance uncertainty (§3.4 step 5) and the E8c-versus-interoperability distinction (§3.11, §4.5) are binding. Handoff: `docs/m8-astra-implementation-handoff.md`.
