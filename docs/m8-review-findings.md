# M8 — MCP stdio adapter: review register

Append-only. One entry per review round or acceptance step; corrections are appended, never edited in place. Packet: `docs/m8-mcp-adapter-packet.md`. Evidence: `artifacts/review-evidence/tinyvault-m8-packet-20260910/` (local, gitignored).

## Paper round 1 — packet rev 1 (2026-09-10, session `2026-09-10-e8b-prereg-m8-packet`, owner claude)

**Channels (parallel, blind, buffered before either was folded in):** Codex `gpt-5.6-sol` read-only (`task --fresh --model gpt-5.6-sol --effort high`, job `task-mtw5s8va-q637pn`, prompt `packet-m8-sol-r1-dispatch-prompt.md`, report `packet-m8-sol-r1.md`) — **NEEDS-ATTENTION, 9 P1 / 6 P2 / 2 P3**; blind fresh-context Claude Opus (`general-purpose` subagent, model opus, same eleven questions plus a twelfth, report `packet-m8-opus-r1.md`) — **NEEDS-ATTENTION, 9 P1 / 14 P2 / 5 P3**. Neither channel fetched the MCP specification (by instruction); protocol findings are internal-consistency findings. Every finding below was verified by the owner against the source before disposition; the packet is now **rev 2**.

**Convergent (both channels, independently):** the gate rejects the composition once, not twice (Sol P1-01 / Opus P1-04 — `scripts/dependency-boundary.mjs:239-244` prunes traversal at the protected edge; owner re-read, confirmed); `BROWSER_OPEN_FAILURE_MESSAGE` is not re-exported by `src/supervisor/host.ts` (Sol P1-03 / Opus P1-02 — `host.ts:41-46` block lacks it; confirmed by grep); T-SEAM mutant M-2 is equivalent because `serializeExact` is `JSON.stringify` (Sol P1-07 / Opus P1-06 — `src/agents/transcript.ts:34-40`; confirmed); the stderr rule leaked arbitrary error text while A3 promised none (Sol P1-08 / Opus P1-09); `list_vault`/`request_vault_setup` bypass the host's admission so shutdown could not see them (Sol P1-05 / Opus P2-04 — `host.ts:336`; confirmed); `validateToolCall` is the wrong export and `matchesSchema` dereferences `required`/`properties` unguarded (Sol P2-01 / Opus P1-07+P2-14 — `loop.ts:470-488`; confirmed); the legacy method count (Sol P3-02 / Opus P3-01); modern error results must carry the envelope fields (Sol P2-02 / Opus Q9).

**Disjoint catches, all verified and adopted:** Opus P1-01 — a verdict-dependent exit code and stderr line make a tripwire match caller-visible in stdio (the parent reads both; `docs/phase-0-plan.md:266`); rev 2 discards the verdict, fixes the exit-code table independent of it, and adds a planted-vs-clean differential to T-LIFE. Opus P1-08 — the host's 10 s bound is armed before mutex acquisition (`host.ts:313-330`; confirmed), so concurrency would turn queue wait into session disposal; rev 2 makes dispatch strictly sequential. Sol P1-04 — the packet closed sessions by hand before quiesce, inverting the locked "drain while targets remain alive" order (`host.ts:366-388` closes sessions inside quiesce; confirmed); rev 2 uses the host's quiesce with both hooks. Sol P1-06 — T-MUTEX on a fake session host proves the fake; rev 2 adds a real `createBrowserSessionHost` part with the `runExclusive` deletion mutant. Sol P1-09 — the launch path was an invention; the owner **measured** it (Node v24.19.0 strips types but rejects extensionless specifiers; scratch probe) and rev 2 pins an esbuild bundle mirroring `testbed/docker/Dockerfile:6`, making T-STDIO mandatory and raising O-5 (two root-of-trust digests). Sol P1-02 — C3 must be entry-and-target keyed like the protected rule, with relay/symlink fixtures. Opus P1-03 — the plan's §8 Key-files column omits `src/` for every `src` file, so there was no path divergence and no LOCKED-doc amendment; rev 2 fact 2 corrected. Opus P1-05 — an injected `Writable` cannot see a stray `console.log`; rev 2 spies on the real `process.stdout.write` and pins `console.` out of the adapter. Opus P2-02 — `import type` is a traversed edge and `src/agents/transcript.ts:4`/`loop.ts:1` type-import `testbed`; C4 is therefore direct-only, current-file keyed. Opus P2-03 — a `spawn` import needs a `reviewProfiles` entry, not only an allowlist row (`scripts/docker-invocation.mjs:78-97`; confirmed). Opus P2-05 — per-call drain, as the runner does. Opus P2-06 — `evidenceIndex` not pinned per tool; planting via out-of-contract fakes as `host.test.ts:110-118`. Opus P2-07 — production-composition AST pin. Opus P2-08 — the "no adapter lock" claim carried by T-STRUCT plus review, not T-MUTEX. Opus P2-09 — fixture provenance headers, owner-verified. Opus P2-10 — no modern→legacy lock; modern always served. Opus P2-12 — batching STOP per legacy revision. Opus P2-13 / Sol Q4 — close-vs-fill linearization pinned (`src/core/fillService.ts:325-329` → `session-unknown`, `isError:false`). Sol P2-03 — `recordFill`/`settleAttach` are host behaviours, not adapter properties; owner verifies existing coverage, residual if a deletion mutant is missing. Sol P2-04 — the seam claim narrowed to successful host payloads. Sol P2-05 — exact stdout inventory. Sol P2-06 — full exit-code table. Sol P3-01 — C4's `scripts` half redundant; C6 a regression requirement. Opus P3-02 — no capability row for the `process` global. Opus P3-04 — control order is the packet's own. Opus P3-05 — the composition zone is an explicit list (`src/adapters/mcp`). Opus Q12a — slice routing split (Sol framing, Astra core). Opus Q12b — the production tripwire stated plainly as overhead with no reference value and no consumer. Opus Q12c — O-1 gains a client probe (needs the user's go-ahead). Opus Q12d — `browser_open_session()` argument-less.

**Disproved (recorded, not adopted):** Opus P2-11 second half ("MCP forbids a null-id response"): the 2026-07-28 base-protocol page, fetched by the owner, says error responses MUST carry the request id "except in error cases where the ID could not be read due a malformed request"; the oversized-line `-32700` keeps `id: null`. The first half (`-32601` for unknown methods) is adopted.

**Owner corrections found while verifying (not raised by either channel):** none beyond the channels' findings; the owner's measured launch probe and the Node extensionless-import result are new facts recorded in packet fact 13.

**Residuals carried into rev 2 (recorded, not fixed):** the hand-written framing (~600 lines) is a TinyVault bug surface not fuzzed in `make test`; `lease.settleAttach()` rejections (`host.ts:411,418`) escape the `captured*` wrappers and reach the caller only through the catch-all mapping; the production tripwire has no reference value and no consumer; a `ping` behind a slow tool call waits up to the host's bound under sequential dispatch; T-STDIO is owner-run only (Chromium).

**Mechanism probe (owner, 2026-09-10, real checkout, no gate running, tree restored):** `src/adapters/mcp/probe.ts` importing `createSupervisedHost` → `check-dependency-boundary.mjs` exit 1, exactly one violation (`static import: src/adapters/mcp/probe.ts -> src/supervisor/host.ts`), no vetted-reach line; file deleted → gate PASS (163 production modules, 153 data-plane roots). Confirms the corrected fact 3 (`gate-probe-adapters-import-supervisor.log`).

**Rev 2 written 2026-09-10** (`docs/m8-mcp-adapter-packet.md`, §10 summary). Not authorized by this entry: implementation, any commit of code, push, live spend, public flip.

## Paper round 2 — packet rev 2 (2026-09-10, owner claude)

**Channels (parallel, blind, buffered):** Sol read-only (job `task-mtw6sze4-w3pxf8`, prompt `packet-m8-sol-r2-dispatch-prompt.md`, report `packet-m8-sol-r2.md`) — **NEEDS-ATTENTION, 9 P1 / 8 P2 / 3 P3**; blind Opus (report `packet-m8-opus-r2.md`) — **NEEDS-ATTENTION, 9 P1 / 16 P2 / 5 P3**. Sol fetched specification pages for its wire findings; the owner re-fetched every page cited before adopting (schema/discover/caching/2025-11-25 lifecycle/2025-06-18 changelog). Both channels found rev 2's round-1 fixes had introduced **three regressions**: sequential dispatch made the real-mutex mutant equivalent (Opus P1-03 / Sol P1-06); the per-call drain opened a post-success `InactiveEvidenceLeaseError` path (Opus P2-03); the explicit composition list plus the `sourceZone` branch confounded the symlink fixture with `zoneConfigurationErrors` (Opus P1-05).

**Convergent, verified, adopted in rev 3:** T-MUTEX part 2 now bypasses the adapter and calls `host.tools` concurrently with a compiling replacement mutant; the `src/**` `reviewProfiles` entry is inert and reds `docker-invocation.selftest.mjs` via the unconditional `spawn-call-count` (`docker-invocation.mjs:150,164-166`; owner read) — resolved by extending the gate to bind profiles for any path (Sol's variant; no existing verdict moves), with selftest mutants; absent `arguments` normalized to `{}` (`matchesSchema` returns false for `undefined`, `loop.ts:485`); exit-code precedence `4 > 6 > 3 > 5 > 0` and T-LIFE cases for `5`/`6`/collisions; the three unbounded host tools (`boundedTools` bounds six, admits `browser_open_session` unbounded, passes the two vault tools raw — `host.ts:336-338`, owner re-read) → a 30 s shutdown deadline then `abort`+`closeAll` exit 3, residual recorded; the composition AST pin made checkable (three assertions + a local-tool-map mutant); `build:mcp` string pinned to the in-test esbuild options with a metafile assertion; the seam claim states the snapshot exemption.

**Disjoint, verified, adopted:** Sol P1-01 — `DiscoverResult`/`ListToolsResult` require `ttlMs`/`cacheScope` and `serverInfo` lives under `_meta` (caching page: "Servers MUST include caching hints on results … `server/discover`, `tools/list`"; discover page result shape) — envelopes corrected, `ttlMs:0`, `cacheScope:'public'`; Sol P1-02 — a sequential reader cannot honour `notifications/cancelled` ("MUST NOT send any further messages for it") — reader/dispatcher split, response suppression, queue semantics; Sol P1-03 — legacy pings precede `initialized` and negotiation echoes a supported requested version else answers with the server's own (lifecycle page quoted in packet fact 14); Sol P1-04 — `-32022` adjudicated before method lookup, per-method param schemas; Sol P1-05 — the `settleAttach` T-ERR witness needs a real lease with a controllable `trackAttach` promise (`browser_open_session` never calls `runControl`); Sol P1-08 — the post-try capture-failed throw at `host.ts:388` and a `finishHost` precondition throw do not drop the lease (owner re-read) → `host.abort()` on every finalization failure; Sol P1-09 / Opus P2-12 — metafile + command pin; Sol P2-04 — validate the canary override before `createSupervisedHost` (Chromium launches first, `host.ts:68-71`); Sol P2-07 — fatal UTF-8, byte-counted limit, backpressure, write-error exit 6; Sol P2-08 / Opus P2-06 — the full fixed caller-visible inventory in §0; Opus P1-01 — six bounded tools, not seven; Opus P1-05/P1-06 — `sourceZone` is not classification (consumed only by `zoneConfigurationErrors`, `vetted.mjs:177-198`, owner read); the four real gate sites named; Opus P1-07 — absent arguments; Opus P1-08 — unparseable line: `-32700` and continue, exit 5 only for the bytes step; Opus P1-09 — M-3 replaced (no capture sink on `SupervisedHost`); Opus P2-01 — alias fixtures; Opus P2-02 — C0 a positive control; Opus P2-03 — drain rejection swallowed; Opus P2-04 / Sol §4 — `constructor.name` dropped, stderr fixed strings only; Opus P2-13 — BACKLOG A4's premise is stale (`finish()` refuses pending evidence, `evidenceLease.ts:452`; `finishHost` refuses open sessions/admitted ops, `host.ts:271-276`) → **O-8** for the user; Opus P2-14 — rootOfTrust carve-out in §5; Opus P2-15 — assert `quiesceEvidenceProducers` at startup; Opus P3-01 — the selftests run at positions 4 and 6 of the pinned test chain (`test-contract.mjs:8-22`), not in `rootOfTrust.test.ts`; Opus P3-04 — pass-through spies; Opus Q10 — the retention sweep has no test-file filter, so adapter tests may not contain the secret-primitive tokens; T-STDIO runs in the clean-clone gate.

**Disproved (recorded):** Opus P2-09 — `structuredContent` without `outputSchema` is permitted (tools page: "conforms to the tool's `outputSchema` if one is defined"). **Recorded as residual, not adopted:** Opus P2-16 — an oversized-but-parseable line is treated as unreadable by design. **Confirmed by the 2025-06-18 changelog (item 1, batching removed):** the every-era batch rejection is consistent with both pinned legacy revisions; the batching STOP in rev 2 is moot and removed.

**Residuals carried into rev 3 (recorded):** framing unfuzzed; `settleAttach` catch-all; production tripwire without reference value or consumer; `ping` waits behind a slow call; cancellation best-effort; unbounded host tools abandoned at the deadline, SIGKILL yields no exit code; shutdown timing unbounded (no probe-P claim); `dist/` unscanned; static graph is hygiene; T-STDIO owner-run; the spawn pin covers shape, not behaviour.

**Rev 3 written 2026-09-10.** Not authorized by this entry: implementation, code commits, push, live spend, public flip.

## Paper round 3 — the cap — packet rev 3 (2026-09-10, owner claude)

**Channels (parallel, blind, buffered):** Sol read-only (job `task-mtw7n2up-jmplwb`, prompt `packet-m8-sol-r3-dispatch-prompt.md`, report `packet-m8-sol-r3.md`) — **NEEDS-ATTENTION, 7 P1 / 4 P2 / 1 P3**; blind Opus (report `packet-m8-opus-r3.md`) — **NEEDS-ATTENTION, 5 P1 / 8 P2 / 6 P3**. Cap criteria were stated in rev 3 §8; both channels classified every P1 by criterion letter. The owner re-fetched every specification page a P1 cited (2026-07-28 changelog, versioning, basic index; 2025-11-25 basic index and lifecycle) and re-read every self-test line cited before disposition.

**In-criteria P1s, all verified and absorbed in rev 4:**
- (d) **Modern `ping` does not exist** — convergent (Opus P1-01, Sol P1-04); 2026-07-28 changelog major change 5 "Remove `ping`, `logging/setLevel`, and `notifications/roots/list_changed`". Rev 3 had a modern `ping` success case and used `ping` as the ordering probe. Rev 4: `ping` is legacy-only; modern `ping` → `-32601`; the ordering probe is a second `tools/call`; residual: the modern era has no liveness probe.
- (d) **A legacy lock must not change modern handling** — Opus P1-02; versioning page ("served statelessly … MAY serve both eras concurrently") and base index ("Servers MUST NOT rely on prior requests over the same connection"). Rev 4: eras served concurrently; no modern→legacy or legacy→modern lock; `initialize` carrying modern `_meta` is an unknown modern method (Opus P2-05).
- (d) **Legacy request ids may never be reused within a session** — Sol P1-02; 2025-11-25 basic index quoted in packet fact 14. Rev 4 step 3.
- (d) **Notifications never receive responses** — Sol P1-03; both base-protocol pages ("The receiver MUST NOT send a response"). Rev 4: a method-prefix classification; malformed/unknown notifications ignored; inbound response objects ignored (Sol P2-03, Opus P2-04).
- (d) **Common modern request members** `inputResponses`/`requestState` — Sol P1-05; recognized and rejected `-32602` as unexpected request state.
- (e) **T-STDIO's bundle cannot resolve external packages from a temp directory outside the repository** — Opus P1-03 (Node resolves from the importing file's directory). Rev 4: build under `artifacts/mcp-stdio-*` in the repository root; the product bundle is stated non-relocatable.
- (c) **The docker-invocation self-test synthesizer would red `make test`** — Opus P1-04; `docker-invocation.selftest.mjs:56-60` synthesizes a bare import for every allowlist row and `reviewProfiles` is module-private (owner read). Rev 4 §4.1: export `reviewProfiles`, re-key the synthesizer, one existing case edited with its verdict unchanged, C6 amended.
- (c) **C2 moves two existing dependency self-test cases** — Opus P1-05; `selftest.mjs:52-65` deep-equals the shipped manifest; `configuredReachableDirectories` (`vetted.mjs:88-103`) requires the directory to exist in every fixture root created by `installDefaultVettedConfiguration` (`selftest-fixtures.mjs:209-212`) (owner read). Rev 4 §3.2: both edits named, verdicts unchanged, any other movement a STOP.
- (b) **The per-call drain deletion survived every test** — Sol P1-06. Rev 4: pass-through `drainEvidence` spy with an invocation-count assertion and mutant M-6.
- (e) **The missing-`quiesceEvidenceProducers` check happens after the host and lease exist** — Sol P1-07; `createSupervisedHost` returns the composed host (`host.ts:63-89`). Rev 4: `abort` + `closeAll` then exit 4.
- (a) **The §0 inventory contradicted the result contracts** — Sol P1-01; host result values are caller-visible by design. Rev 4 §0: vocabulary plus an explicit boundary (echoed id, `data.requested`, `package.json#version`, the seven registry objects, host result values per `SCHEMA.md`).

**Verified P2/P3 absorbed as owner wording (the post-cap confirmation pass, no fourth round):** Opus P2-01 (T-STRUCT's import ban must exclude test files — four named tests were otherwise unwritable), P2-02 (uncaught exception / unhandled rejection handlers with fixed output; abandoned handler `.catch`), P2-03 (`data.requested` must be a validated ≤ 64-byte string), P2-04 (classification rule), P2-05, P2-06 (the legacy request window closes at the `initialize` response, not at `initialized` — lifecycle page re-read), P2-07 (differential mutant M-5), P2-08 (abandoned request's response suppressed), P3-01…P3-06 (citations, spawn call shape, C3 limits declared, case-name pin, `abort`-after-`finish` residual, heading count); Sol P2-01 (C3 limits declared; `import type` fixture added), P2-02 (stdout failure → `abort`, `closeAll`, exit 6), P2-03, P2-04 (T-STDIO wording), P3-01 (priority wording `4 > 6 > 3 > 5 > 0`).

**Disproved in round 3:** none. **Owner note on the cap rule:** rev 3 §8 said no production sentence changes after the cap except for in-criteria P1s; the owner absorbed verified P2/P3 wording as well, because the conventions make the cap round "followed by an integrator confirmation pass" and the packet has no implementation yet — this is recorded as the owner's choice, not as a fourth round.

**Rev 4 written 2026-09-10 — the lock candidate.** The paper ladder is complete. Next: the packet returns to the user with O-1…O-8; no implementation dispatch until the user locks it. Not authorized by this entry: implementation, code commits, push, live spend, public flip.

## Rev 5 correction — `_meta` reconciliation, the authorized client probe, and the runtime-authorization requirements (2026-09-11/12, session `2026-09-11-m8-rev5`, owner claude)

**Authorization and scope:** `PLAN.md` Decisions Log 2026-09-10 "USER DECISIONS on the M8 packet (rev 4)" (probe authorized with synthetic data and no persistent configuration change; the `_meta` keys reconciled and the probe result recorded before O-1 and the packet lock; a focused contract correction, not a re-opened review) and the session-E brief (paper only; no implementation, no `--write` dispatch, no `src/**`, `testbed/**`, `SKILL.md`, fixture or checker edit; no push, cohort or provider call). **State verified before anything was written:** `main` = `08d4d01`, `git status --porcelain` empty, `origin/main` = `bac91db`, E8c tarball SHA-256 `cdcc4958…` reproduced, packet header "Revision 4 … NOT LOCKED". The session-D scratchpad clones `e8c-clone` and `derive-6812627` were deleted (authorized); every evidence directory untouched. Evidence for this entry: `artifacts/review-evidence/tinyvault-m8-rev5-20260911/` (`spec/` — the pinned pages fetched 2026-09-12 UTC as HTML and, where the site serves it, Markdown, plus `schema-2026-07-28.ts` from the specification repository at `main`; `probe/` — the logging server, the one-off config, six run logs, outputs and stderr; `probe-sha256.txt`; `claude-code-mcp-doc.md`).

### (a) `_meta` reconciliation — rev 4 was wrong on three of the four keys

Rev 4 wrote the modern `_meta` keys as `io.modelcontextprotocol/protocolVersion`, `clientInfo`, `clientCapabilities`, `serverInfo` (§0, §3.4 step 5, §3.4 envelopes, §4.4 T-WIRE, fact 14). The pinned `2026-07-28` pages namespace **all four**. Line numbers are in the fetched copies under `spec/` (`2026-07-28_basic_index.md` = `…/2026-07-28/basic/index`, `2026-07-28_server_discover.md` = `…/server/discover`, `2026-07-28_changelog.md`, `schema-2026-07-28.ts` = `schema/2026-07-28/schema.ts`):

| Key (exact) | Direction | Required | `basic/index.md` | `schema.ts` | `server/discover.md` | `changelog.md` |
|---|---|---|---|---|---|---|
| `io.modelcontextprotocol/protocolVersion` | request `_meta` | **Yes** | :355 (reserved), :377 (table), :382-384 (missing → `-32602`) | :76 (`RequestMetaObject`) | :24 | :16 |
| `io.modelcontextprotocol/clientInfo` | request `_meta` | No (SHOULD, :386-387) | :356, :378 | :90 | :25-28 | :16 |
| `io.modelcontextprotocol/clientCapabilities` | request `_meta` | **Yes** | :357, :379, :389-394 (`-32021` when a required capability is undeclared) | :98 | :29 | :16 |
| `io.modelcontextprotocol/serverInfo` | result `_meta` | No (SHOULD, :398-404) | :398-404 | :157 (`ResultMetaObject`) | :51-55, :100-101 | :16 |
| `progressToken` | request `_meta` (reserved, every era) | No | :354 | :68 | — | — |
| `io.modelcontextprotocol/logLevel` | request `_meta` | No (deprecated) | :358, :380 | :110 | — | :22 |
| key rules: "implementations **MUST NOT** make assumptions about values at these keys"; reserved prefixes | — | — | :326-346 | :45 | — | — |

Also confirmed on the same fetch: `Result` requires `resultType` (`schema.ts:223-235`; `basic/index.md:75`); `DiscoverResult` = `supportedVersions`, `capabilities`, `instructions?` over `CacheableResult` (`schema.ts:678-698`, `:1094`, `:1109`); the caching MUST covers `server/discover` and `tools/list` (`server_utilities_caching.md:15-22`); `-32022` (`schema.ts:450`, `basic/index.md:137`); the versioning page's dual-era rules (`basic_versioning.md:178-185`: modern `_meta` served statelessly, `initialize` selects legacy scoped to the stdio process, "MAY serve both eras concurrently") and its compatibility matrix (`:169-176`; `:172` a legacy client against a modern-only server fails at `initialize`; `:171` a dual-era client sends `server/discover` first on stdio and falls back to `initialize` on "a non-modern error or a timeout"). The legacy `initialize` keys stay bare (`protocolVersion`, `capabilities`, `clientInfo` in params; `serverInfo` in the result) on the `2025-11-25` and `2025-06-18` lifecycle pages (HTML fetched; text located) — and were observed bare in the probe. **Disposition:** rev 5 corrects every occurrence; the adapter-originated `_meta` carries exactly one key, `io.modelcontextprotocol/serverInfo`; the inbound keys it validates are `io.modelcontextprotocol/protocolVersion` and `io.modelcontextprotocol/clientCapabilities`; `io.modelcontextprotocol/clientInfo`, `io.modelcontextprotocol/logLevel`, `progressToken`, trace keys and vendor-prefixed keys are accepted and ignored in both eras; `-32021` is never emitted because no adapter method requires a client capability.

### (b) The client probe — installed Claude Code 2.1.258 against a throwaway logging stdio server

**Setup (synthetic, nothing persistent):** `probe/probe-server.mjs` (logs every stdin line verbatim with pid and time; never logs the environment; answers `initialize` legacy, `server/discover` / `tools/list` / `tools/call` in the era of the request, `ping` `{}`, never answers notifications; `PROBE_CRASH_AFTER=<method>` exits 1 after answering that method, `PROBE_KILL_AFTER` sends itself SIGKILL, `PROBE_IGNORE_SIGNALS` logs signals and heartbeats instead of exiting), one tool `probe_echo`; `probe/mcp-config.json` passed as a one-off `--mcp-config` with `--strict-mcp-config`; every run `claude -p … --model claude-haiku-4-5-20251001 --output-format json --max-turns ≤ 6` from the session scratchpad (no `CLAUDE.md`), `CLAUDECODE` unset, `perl -e 'alarm …'` as the outer timeout (macOS has no `timeout`). Spend: seven runs (`run2` denied the tool for lack of `--allowedTools` and is kept as a teardown witness), **$0.16 total**, ≤ 4 turns each; no key-like string in any artifact (grep). Node v24.19.0; macOS 15.6.1.

| Run | Configuration | Observed on the wire (verbatim in `probe/run*.log`) |
|---|---|---|
| 1 | default | `initialize` id `0` (integer), `params.protocolVersion` `"2025-11-25"`, `capabilities` `{roots:{listChanged:true}, elicitation:{}}`, `clientInfo` `{name:"claude-code", title, version:"2.1.258", description, websiteUrl}` (bare keys); → `notifications/initialized` → `tools/list` id `1` (legacy shape, no `_meta`). **No `server/discover`, no `ping`.** Member order `method, params, jsonrpc, id`. |
| 2 | default, tool not allowed | same open; teardown at exit: **SIGINT**, no stdin EOF first. |
| 2b | `PROBE_CRASH_AFTER=tools/call`, tool allowed | after `tools/call` id `2` (`params._meta` = `{"claudecode/toolUseId": "toolu_…", "progressToken": 2}`) the server exited 1; **≈ 240 ms later Claude Code spawned a new process** (new pid), sent `initialize` id `0` + `initialized`, **no `tools/list`** (the list was reused), and dispatched the second call `tools/call` id `1` to it. The model saw both results; nothing signalled the restart. |
| 3 | `PROBE_CRASH_AFTER=tools/list` (startup) | exit 1 after `tools/list`; **≈ 4.5 s later a new process** was spawned, `initialize` + `initialized`, then `tools/call` without a new `tools/list`; result delivered. Teardown: **SIGINT, then SIGTERM ≈ 100 ms later, stdin never closed** (`stdinEnded=false` at both signals). |
| 4 | `PROBE_IGNORE_SIGNALS` | SIGINT at *t*, SIGTERM at *t*+101 ms, the process kept logging heartbeats to *t*+502 ms and was gone before *t*+750 ms (≈ 40 ms after `claude` itself exited): **the kill grace after SIGINT is ≈ 0.5–0.75 s, then SIGKILL** (inferred: no further signal was catchable). |
| 5 | `PROBE_KILL_AFTER=tools/call` (SIGKILL self) | identical to 2b: respawn ≈ 240 ms after the signal death, second call served on the new process. |
| 6 | `MCP_PROTOCOL_NEGOTIATION=auto MCP_SDK_GENERATION=v2` (one-off env; the doc's opt-in for stdio, `claude-code-mcp-doc.md:331-340`) | **Process A:** `server/discover`, id `"server-discover-probe-1"` (string), `params._meta` = `{"io.modelcontextprotocol/protocolVersion":"2026-07-28", "io.modelcontextprotocol/clientInfo":{…}, "io.modelcontextprotocol/clientCapabilities":{roots:{listChanged:true}, elicitation:{}}}`; **5 ms after the answer: SIGTERM and stdin EOF together**; process A ended. **Process B** (≈ 25 ms later): modern `tools/list` id `0` with the same three namespaced keys — **no `initialize`, no second `server/discover`** — then modern `tools/call` id `1` whose `_meta` carries the three namespaced keys **plus** `claudecode/toolUseId` and `progressToken`; teardown SIGINT → SIGTERM (+104 ms). The server's (deliberately bare) `_meta.serverInfo` was accepted without complaint — the client does not rely on it. |

**Measured facts carried into rev 5 (F1–F9):** F1 the default open is legacy `2025-11-25` and the modern era is reachable only through the documented opt-in — dual-era serving is required by the only measured client; F2 in both eras `tools/call` carries `params._meta` with `progressToken` and a vendor key (`claudecode/toolUseId`), so a `tools/call` validator that rejects unknown `params` members or unknown `_meta` keys makes the adapter unusable with the installed client (rev 4 §3.4 step 7 "validated exactly" was ambiguous on this and is now explicit); F3 request ids include the integer `0` and long strings; F4 the harness's discovery probe is a **separate process** that is terminated ≈ 5 ms after the discover answer, so startup, `server/discover` and shutdown on a host with no sessions is the first thing a modern client exercises; F5 Claude Code never closes the adapter's stdin — teardown is SIGINT, SIGTERM ≈ 100 ms later, SIGKILL ≈ 0.5–0.75 s after SIGINT — so **rev 4's shutdown triggers (EOF, SIGTERM) miss the first signal and the shutdown sequence cannot complete inside the grace**; F6 **Claude Code respawns a stdio server that dies mid-session or at startup and routes the next tool call to the fresh process**, whether it died by exit code or by signal, with no model-visible indication; F7 the `tools/list` result is reused across the respawn (no re-list); F8 request ids restart at `0` on the new process; F9 the documentation (`claude-code-mcp-doc.md:357-359`: "Stdio servers are local processes, and Claude Code doesn't reconnect them automatically") **contradicts F6 as measured on 2.1.258 in `-p` mode**; the measurement governs, the discrepancy is recorded, and the interactive mode is unmeasured.

**Consequence for §6.6 (5) (restart authority):** in Claude Code, ending the adapter process is a renewal: the harness re-creates it and with it a fresh `createSupervisedHost` and a fresh fill-authorization domain. Parties able to end the process: the human; the harness itself (crash recovery, F6; the discovery probe, F4; exit); **the model, in any harness that gives it a shell or process control (Claude Code's Bash tool can `kill` the adapter or spawn its own copy)**; and the adapter's own exit paths (`3`/`4`/`5`/`6`) — of which only the framing-failure exit `5` (a line over 1 MiB) is reachable from a tool argument, bounded by the harness's maximum tool-argument size (unreachable at Haiku 4.5's 32k output tokens; not bounded in general). **The claim that renewal is inaccessible to the model is withdrawn for Claude Code and for every harness with automatic restart or a model-accessible shell**; rev 5 says so in §3.11, §9 and the SCHEMA/README sentences (A11). The measured half (F6) is the harness's behaviour; the shell half is reasoned from the harness's tool surface, not measured (no probe gave the model a shell).

### (c) O-1 resolved (owner recommendation for the lock)

**Dual-era, served concurrently, stands** — now on measurement rather than on the specification alone: the installed client opens legacy `2025-11-25` by default (F1) and modern `2026-07-28` under its opt-in (run 6), and the versioning page's fallback (`basic_versioning.md:172-173`) means a modern-only server would fail the default client at `initialize`. Legacy `2025-06-18` stays supported (no cost; one lifecycle page). The user locks O-1 with the packet.

### (d) Citation re-pin — rev 4 was verified at `bac91db`; the runtime fill control (`6812627`) shifted `src/supervisor/host.ts` and `src/core/fillService.ts`

Every `host.ts` reference in rev 5 was re-read at `08d4d01`: `VAULT_TOOL_FAILURE_MESSAGE` `:41` (was `:39`); the re-export block `:43-48` (was `:41-46`; still without `BROWSER_OPEN_FAILURE_MESSAGE`, which is imported at `:3` and thrown at `:347`, `:456`); `createSupervisedHost` `:64-99` (was `:63-89`) — Chromium at `:72` before the lease at `:74`, `onFillAuthorization` option `:70`, the domain `:76`, the hook `:77-82`, **the only `createFillService` call `:94`** (the runtime-control packet's `host.ts:85` was written before the slice landed), `compose` `:95`; `composeSupervisedHost` `:102-108`; `finishHost` `:280-285`; `abortHost` `:287-292`; `admit` `:301-307` (refusal `:302`); `bounded` `:322-340` (timer armed `:324-326` before the operation); `boundedTools` `:342-363` (`browser_open_session` admitted unbounded `:345-347`; the two vault tools raw `:344`; fill refusal `:361`); `quiesce` `:375-398` (post-try throw `:397`); `createTools` `:409-443` (`settleAttach` `:420`, `:427`); `uncaptured` `:494-505` (`InactiveEvidenceLeaseError` `:502`); `VAULT_TOOL_FAILURE_MESSAGE` thrown `:361`, `:474`, `:490`. `src/core/fillService.ts`: the reservation `:182-183` after the lock check `:176-181` and the pin `:152-158`; close-vs-fill → `session-unknown` `:259-264` and `mapOuterFailure` `:352-357` (was `:325-329`); `setupReasonFor` `:290-303` (`handle-exhausted` → `null` at `:292`). Unchanged and re-confirmed: `evidenceLease.ts:287,297,450-452,460,464-468,547`; `session.ts:118-150,233-280`; `loop.ts:84,470-488`; `transcript.ts:34-40`; `runnerExecution.ts:447-448,461-474`; `dependency-boundary.mjs:40,107,206,217,239-244,403,561,641`; `vetted.mjs:12,88-103`; `selftest.mjs:58` (the shipped-manifest literal, was cited `:52-65`); `selftest-fixtures.mjs:209-212`; `docker-invocation.mjs:11-46,79,150,165`; `docker-invocation.selftest.mjs:57`; `rootOfTrust.test.ts:15-16`; `SCHEMA.md:49,96`; `src/core/types.ts:44`; `fillService.structure.test.ts:259-261` (`AUTHORITY_NAMES`/`AUTHORITY_FILES`), `:495` and `:632-633` (`HOST_CALL_FILES`), `:517` (the single `createFillService` caller pin), `:656-670` (`inspectHostArgument`).

**Rev 5 written 2026-09-12 (UTC).** The delta from rev 4 is listed in the packet header; the paper-review ladder for the delta (read-only Sol, capped at two rounds, P1 criteria stated up front) follows as separate entries. Not authorized by this entry: implementation, code commits, push, live spend, public flip.

## Rev 5 delta review — round 1 (2026-09-12, session `2026-09-11-m8-rev5`, owner claude)

**Channel:** read-only Codex `gpt-5.6-sol` (`task --fresh --model gpt-5.6-sol --effort high`, job `task-mtxwtfa8-7716w8`, prompt `packet-m8-sol-rev5-r1-dispatch-prompt.md`, report `packet-m8-sol-rev5-r1.md`, evidence `artifacts/review-evidence/tinyvault-m8-rev5-20260911/`) — **NEEDS-ATTENTION, 5 P1 / 3 P2 / 1 P3**; P1 criteria (i)–(iv) stated in the prompt and in packet §8. Sol used the preserved fetched pages rather than the live site; it reported no deviation. **Every finding was verified by the owner against the tree at `08d4d01`, the fetched pages or the run logs before disposition; the packet is now rev 5.1.**

| # | Finding (Sol) | Owner verification | Disposition |
|---|---|---|---|
| P1-01 (ii) | §6.6 (0) asks for "a test that a second connection reuses the first host"; rev 5's T-HOST drove a second `initialize` and an era switch on one connection, and T-STDIO's two-process case proves the opposite | Confirmed by reading `docs/m7-runtime-fill-control-packet.md:121` against packet §3.11 (0)/§4.4 | **Absorbed.** Server layer split into `createServer(host)` and `serve(server, input, output)`; T-HOST attaches two sequential injected stream-pair connections to one server over one host, asserts identity on both and a factory count of 1; mutants: a host per connection, per `initialize`, per `tools/call`. The two-process case stays as the cross-process half (restart authority). §3.11 (0), §4.4, §7 A12 and the §6.6 map updated |
| P1-02 (i) | F5 overstated: stdin was open at session teardown, but the run-6 discovery process received SIGTERM **and** stdin EOF and exited 0; "killed ≈ 5 ms" wrong; "cannot complete inside the grace" not established (5 s is an upper bound) | Confirmed: `run6.log:4-6` (`signal SIGTERM stdinEnded=false`, `eof stdin closed`, `exit code=0`); `run3.log`/`run4.log` show stdin open at both signals | **Absorbed.** Header, fact 16 F5, §3.7 and §9 reworded: session teardown SIGINT → SIGTERM (+≈ 100 ms) with stdin open; discovery process SIGTERM + EOF, exit 0; shutdown "may be truncated — completion within the grace is not guaranteed" |
| P1-03 (ii) | §3.11 (5) says R5 is a stated limitation, §9 filed restart authority under "Residuals added by rev 5" | Confirmed by reading §9 | **Absorbed.** §9 split: "Stated limitation added by rev 5 — not a residual" (restart authority) and "Residuals added by rev 5" (the rest) |
| P1-04 (i) | `HOST_CALL_FILES` is at `fillService.structure.test.ts:595-596`, not `:632-633`; computed-key enforcement is `inspectAuthorityAccess` `:539-555`, not `inspectHostArgument` `:657-669`; the drain-after-drop citation `host.ts:500-503` is the snapshot fallback, the real path is `host.ts:268` → `evidenceLease.ts:442-444` (`#assertActive` `:543-545`) | Confirmed by `grep -n` and `sed -n`: `HOST_CALL_FILES` `:595`; `inspectHostShape` `:604-612` (single object literal; keys among `backend`/`canary`/`browser`/`launcher`); `unknown-host-key` `:553`; `inspectHostArgument` `:657`; `drainEvidence` `host.ts:268`, `evidenceLease.ts:442-444`, `#assertActive` `:543-545`. The owner's rev 5 `:632-633` came from misreading a `sed` window; the register's paragraph (d) above carries the wrong number and is corrected here (append-only) | **Absorbed.** Every occurrence re-pinned in the packet (fact 5, fact 17, §3.11 (1), §4.4, §4.6 item 3, §7 map); T-RC-10 now described as membership `:495`, sole `createFillService` caller `:499`/`:517`, host-call shape `:604-612`, computed-key checks `:539-555`, typed/syntactic/spread checks `:657-669` |
| P1-05 (iv) | The user's required O-4 sentence ("the randomly minted production canary does not establish detection of actual credential leaks") was not in §3.8 although the rev 5 header claimed O-4 carried it | Confirmed: §3.8 read "no reference value and no consumer" only | **Absorbed.** Sentence added verbatim to §3.8 and to the §4.5 SCHEMA/README contract; header corrected |
| P2-01 | Optional standard `_meta` keys are accepted without type checks although the schema types them | Read `basic/index.md:326-346` and `schema.ts:63-110`: the page says implementations MUST NOT make assumptions about values at reserved keys (`:328-329`) | **Recorded, not adopted as validation.** Owner choice stated in §3.4 step 5: optional keys are accepted **without inspecting their values**; a malformed optional key is not an error. Validating values the adapter never uses would add rejection paths for no security gain |
| P2-02 | `probe/run2.log` absent although `probe-sha256.txt:6` hashes it | **Rejected on the evidence:** `ls artifacts/review-evidence/tinyvault-m8-rev5-20260911/probe/` lists `run2.log`; `shasum -a 256` reproduces the manifest value `788f99fc…`; the row's facts (legacy open; SIGINT at exit) are in the file | Rejected; recorded |
| P2-03 | D-M8-12's "≈ 1–2 s" Chromium-launch latency is unmeasured by the delta evidence | Confirmed: the probe server launched no browser | **Absorbed.** Labelled unmeasured; added to §9 residuals |
| P3-01 | SIGKILL should be labelled inferred | Confirmed: the target cannot log a SIGKILL; heartbeats end at +502 ms | **Absorbed** (header, fact 16, §3.7, §9) |

**Sol's PASS items, standing as independently reproduced:** the four `_meta` keys (spelling, direction, required/optional) and every principal table cell; `resultType`, discovery and caching citations; F1–F4 and F6–F9 against the logs (respawn 236 ms / 244 ms / 4.492 s; SIGINT→SIGTERM 101–104 ms; the run-6 two-process sequence; `tools/list` not repeated; ids restarting at 0; the F9 quotation); §6.6 (1)–(6), O-RC-3, O-RC-8 and O-E8c-1 carried in substance (with (0) fixed above); fact 17's substantive claims (`host.ts:65-98,70,76-82,94,52-63`; `fillService.ts:145-184,290-303`; `results.ts:51-55`; `localFile.ts:144-149`); T-RC-10 scans `src`+`testbed` production files and the only existing-test edit needed is `HOST_CALL_FILES`; T-STRUCT's bans are hygiene (computed spellings evade them — T-RC-10 and R19 carry the stronger evidence), falsifiable as written; T-LIFE/T-STDIO/T-WIRE additions falsifiable; the verbatim probe fixtures are accepted by §3.4's precedence; no D-M8-1…10 or O-2/O-3/O-5…O-8 decision changed; O-1 presented as a recommendation for the user's lock; §0's `_meta` boundary consistent with §3.4's envelopes.

**Rev 5.1 written.** Round 2 (the cap) follows: read-only Sol, delta rev 5 → 5.1 only, same P1 criteria. Not authorized by this entry: implementation, code commits, push, live spend, public flip.

## Rev 5 delta review — round 2 (the cap) and the owner confirmation pass → rev 5.2 (2026-09-12, session `2026-09-11-m8-rev5`, owner claude)

**Channel:** read-only Codex `gpt-5.6-sol` (job `task-mtxxgsdt-pd3nfk`, prompt `packet-m8-sol-rev5-r2-dispatch-prompt.md`, report `packet-m8-sol-rev5-r2.md`) on the rev 5 → 5.1 delta, same P1 criteria — **NEEDS-ATTENTION, 2 P1 / 2 P2**. Round-1 closures **confirmed:** P1-03, P1-04 (every re-pinned line re-read), P1-05, P2-03, and the **P2-02 rejection** (Sol reproduced `run2.log`'s SHA-256 `788f99fc…` = `probe-sha256.txt:6`). **Not confirmed as closed:** P1-01 (the new T-HOST sequence was internally inconsistent — round-2 P2-01) and P1-02 (the header and §3.7 still said the client ends the adapter with a SIGKILL — round-2 P1-01). This was the cap; per the conventions the owner ran a confirmation pass (rev 5.2), no third round.

| # | Finding | Owner verification | Disposition |
|---|---|---|---|
| P1-01 (i) | "an inferred SIGKILL" stated categorically in the header and §3.7, but runs 3 and 6 exited `0` after SIGINT/SIGTERM; only the signal-ignoring run supports the inference | Confirmed: `run3.log` and `run6.log` end with `exit code=0` from the probe's own handler; only `run4.log` shows heartbeats ending | **Absorbed.** Both places now say: if the adapter is still alive after SIGINT and SIGTERM, heartbeat cessation supports an inferred SIGKILL ≈ 0.5–0.75 s after SIGINT; responsive processes exited `0` before that |
| P1-02 (i) | The "MUST NOT make assumptions about values" sentence is at `basic/index.md:329-330`, not `:328-329` | Confirmed by `sed -n 326,332p` (the sentence spans the fetched file's lines 329–330) | **Absorbed** in the packet (§3.4 step 5, §10). The round-1 entry above cites `:328-329`; corrected here, append-only |
| P2-01 | T-HOST had both connections begin with legacy `initialize`, but §3.4 makes the legacy state process-scoped and every later `initialize` `-32600` — connection two's first `initialize` would already be a repeat | Confirmed by reading §3.4 step 4 against §3.11 (0)/§4.4 | **Absorbed.** Connection one initializes the process; connection two sends no `initialize` — a legacy `tools/call` is served on the process-scoped state, then modern requests, then one repeated `initialize` (`-32600`) |
| P2-02 | Accepting malformed optional `_meta` values is a compatibility choice, not a specification consequence (the page and schema type those fields) | Confirmed: `basic/index.md:375-380`, `schema.ts:63-110` | **Absorbed as wording.** §3.4 step 5 now calls it an explicit owner compatibility choice and states the conformance uncertainty; the behaviour (accept without inspection) is unchanged, because the adapter never uses those values |

**Residuals recorded for the delta:** the optional-`_meta` permissiveness is an owner compatibility choice with a stated conformance uncertainty; the SIGKILL endpoint is inferred and applies only to a process still alive after SIGINT/SIGTERM; the adapter's startup latency under the discovery probe is unmeasured; the harness's documentation contradicts its measured respawn; the shell half of the restart-authority analysis is reasoned, not measured.

**Ladder summary for the rev 4 → rev 5 correction:** two read-only Sol rounds (the cap), 7 P1 / 5 P2 / 1 P3 in total; 7 P1, 4 P2, 1 P3 absorbed; 1 P2 rejected on the evidence; owner confirmation pass after the cap. **Packet rev 5.2 is the corrected lock candidate returned to the user.** Not authorized by this entry: implementation, code commits, push, live spend, public flip, cohorts.

## M8 packet rev 5.2 LOCKED — user decisions (2026-09-12, session `2026-09-11-m8-rev5`, owner claude)

**User decision (2026-09-12, verbatim in substance; `PLAN.md` Decisions Log "USER DECISIONS on the M8 packet (rev 5.2)"):** (1) **rev 5.2 is locked**; (2) **implementation is authorized to the incoming GPT-6 Astra continuity owner through the prescribed ladder** (packet §6; `docs/handoff-pattern.md` §0 Codex-led ladder) — **not implemented or dispatched from the Claude session**; (3) the measured compatibility decisions and the stated limitation are **accepted**: process recreation grants fresh authorization, so the adapter does not establish renewal isolation in the tested Claude Code configuration; the limitation must stand **beside the setup instructions**, and E8c's evaluated configuration is **distinguished from** MCP interoperability evidence; (4) the handoff carries the **final interoperability check with a throwaway vault** — first fill, same-process exhaustion, process restart, whether another fill becomes possible — as a **verification of the limitation, not a passing security claim**; (5) **any paid client call requires an explicit provider-spend allowance before execution** (none granted); (6) the metadata compatibility choices and their conformance uncertainty are **preserved exactly**; (7) implementation-discovered contract conflicts are **surfaced for disposition**; (8) the four pending files are committed with explicit paths, then the session wraps up with an execution-ready handoff to a fresh Codex session with GPT-6 Astra (exact base SHA, locked packet, accepted decisions, completed reviews and caps, evidence paths, required gates, pending work, authorization boundaries), records running jobs, relinquishes continuity and stops writing shared state before Astra begins; (9) the independent Claude QA/security channels and the fresh Codex adversarial review under handoff-pattern §0 are preserved; completed paper reviews are not restarted; (10) Astra returns the reviewed, gated candidate **with interoperability evidence** before merge approval; **no merge, push, additional cohort or public flip is authorized.**

**Packet edits made at the lock (rev 5.2 header and body, owner, same day):** the header's status line; §3.11 (5) the accepted limitation sentence; §3.11 last bullet the E8c-versus-interoperability distinction; §4.5 the verbatim setup-instruction sentence; §6 step 3 the final interoperability check with the spend-allowance rule; §5 the surface-for-disposition clause; §10 the lock line. No §3.4 sentence, no test, no ledger row, no decision D-M8-1…12 changed.

**Completed reviews and caps (not to be restarted):** paper rounds 1–3 on revs 1–3 (Sol read-only + blind Opus each; round 3 the cap; rev 4 the post-cap confirmation pass); the rev 4 → 5 delta ladder (two read-only Sol rounds, the cap; rev 5.2 the confirmation pass). What remains is packet §6 steps 2–5: implementation (slice A Sol, slice B Astra or an Astra-owned worker), owner gates and mutant tables, the interoperability check, then the post-implementation ladder (fresh Claude QA via `scripts/claude-review.mjs`, the security channel per §7.1, a fresh-context Codex adversarial pass) capped at three rounds with §6 step 4's P1 criteria.

**Handoff:** `docs/m8-astra-implementation-handoff.md`. Continuity relinquished by the Claude session at its wrapup; no jobs running.

## M8 implementation entry — M8-C1 (2026-09-12, session `2026-09-12-m8-impl`, owner codex)

**Handover and scope:** user explicitly transferred continuity and authorized implementation. Verified clean `main` at `2249a6248900f84fe4da5f897836318732fd2430`, parent lock `36aeb54`, one worktree, `origin/main` `bac91db`, no pinned-source drift from `08d4d01`, no running companion jobs or TinyVault gate/probe/monitor processes. Open Claude clients were observed, with the prior owner's explicit closed handover governing continuity. Created `codex/m8-mcp-adapter` from the wrapup tip. Fresh `gpt-5.6-sol` worker `m8_slice_a` owned only `protocol.ts`, `protocol.wire.test.ts`, `protocol.stdout.test.ts`; owner performed read-only slice B preparation while Sol was the sole implementation writer. No paper or delta review was restarted.

**M8-C1 — implementation blocked pending user contract disposition.** Packet `docs/m8-mcp-adapter-packet.md:54` classifies every method beginning `notifications/` as a notification. Its shape step says "a notification with an id" receives `-32600`, while the same paragraph says "notifications never receive a response: an unknown, malformed or invalid notification is ignored". T-WIRE at `:120` explicitly requires an ID-bearing notification to receive `-32600` and also requires malformed notifications to produce no output. The preserved pinned page `artifacts/review-evidence/tinyvault-m8-rev5-20260911/spec/2026-07-28_basic_index.md:159-175` says notifications are one-way, the receiver MUST NOT respond, and notifications MUST NOT include an ID. The owner independently verified these exact clauses after the worker reported the conflict. Example input: `{"jsonrpc":"2.0","id":1,"method":"notifications/cancelled","params":{"requestId":0}}`. Both locked outcomes cannot hold for this message under the packet's own classification.

**Disposition: pending, no amendment applied.** The owner surfaced the conflict to the user, recommending that the invalid notification be ignored without response or cancellation, and that the two `-32600` clauses be corrected. Packet §5 requires a recorded user disposition; implementation authorization is already granted and is not being requested again. No severity escalation or new paper round is claimed here.

**Partial draft and evidence:** the three slice A files are uncommitted, with `protocol.ts` at 381 lines. Its notification-ID path has an explicit unresolved-contract throw; this is unfinished code, not accepted behavior and not an implementation candidate. Owner verification on the frozen draft: `npm run typecheck` PASS; focused Vitest 2 files / 34 tests PASS; dependency gate PASS (166 modules, 155 roots); dependency self-test PASS; docker-invocation gate PASS; docker-invocation self-test PASS (111 existing source mutants, 41 guard deletions, profile deletions, B2 production CLI checks). These are existing gate self-tests, **not the M8 mutant table**. Logs and source hashes: `artifacts/review-evidence/tinyvault-m8-impl-20260912/slice-a-checkpoint.md`. Every launched check completed with exit 0. No source changes during owner checks; worker stopped.

**Not run:** full `make test`, `make test-docker`, `make eval-stub`, literal clean clone, M8 mutants, scripted or paid interoperability, Claude QA/security or fresh Codex implementation review — no complete candidate exists and the required sequential slice A acceptance is blocked. Post-implementation review rounds used: **0/3**. No commit, merge, push, additional live cohort, public flip or provider-spend call. Historical cohorts remain untouched. **Deviations From Handoff:** execution paused during partial slice A on the explicit §5 STOP; slice B and all dependent integration/acceptance work remain pending. Continuity stays with Codex, paused pending the user's contract disposition.


## M8-C1 approved (2026-09-12, session `2026-09-12-m8-impl`, owner codex)

The user approved the proposal: ignore ID-bearing `notifications/*` messages without response or action, correcting the two conflicting `-32600` clauses. Applied narrowly to packet §3.4 and T-WIRE; no other requirement changed and no completed review round reopened. Codex resumed active ownership on the same branch and uncommitted draft. Slice A completion precedes slice B. Provider spend for interoperability remains ungranted.


## Slice A completed (2026-09-12, owner codex)

Fresh Sol worker completed only `src/adapters/mcp/protocol.ts`, `protocol.wire.test.ts`, and `protocol.stdout.test.ts` after the approved M8-C1 amendment. Production protocol is 397 lines. Owner independently reran typecheck (PASS), both focused suites (54/54 PASS), dependency boundary (PASS: 166 modules, 155 roots), and docker-invocation gate (PASS) with source frozen. Logs: `artifacts/review-evidence/tinyvault-m8-impl-20260912/slice-a-complete-{typecheck,focused,dependency,docker-invocation}.log`. `git diff --check` PASS. These are slice checks, not final candidate acceptance or the M8 mutation table. Worker reports five captured probe messages byte-identical; owner fixture-header verification remains due with candidate gates. Sequential slice B and owner integration follow this checkpoint. Reviews remain 0/3.


## Slice B partial integration — C2–C4 pending (2026-09-12, owner codex)

Fresh Astra worker `m8_slice_b` owned the three remaining production files, eight assigned test files, six gate files and two additive exports. Owner verified every entry of its 19-file frozen manifest before taking over writing. The worker returned partial work at the locked STOP: no `server.stdio.test.ts` was authored. Owner integrated the authorized build command/Makefile target, two root-of-trust digest values, sole core HOST_CALL_FILES literal, SCHEMA in this same integration checkpoint, README/ORIENT/BACKLOG and corrected two inaccurate A fixture-header section names plus the stdout header's claim of a preserved cancellation page. No production protocol behavior changed. Total production lines 586, each file below400. A4's stale premise closed per approved O-8, with narrowed unadmitted-tool/caller-finalization residual retained and linked; backend injectivity carried to M9; exact restart limitation beside both launch instructions; E8c scope separate.

**Contract conflicts, owner-verified and surfaced; none applied:**

| ID | Evidence | Proposed narrow disposition | Status |
|---|---|---|---|
| M8-C2 | §3.10 requires pre-kill child PID enumeration; §4.7 permits only Node spawn. On this macOS host /proc is absent, /bin/ps exists; the proposed inventory binding currently produces spawn-import-shape and spawn-call-count. | Add only one fixed `/bin/ps -axo pid=,ppid=,comm=` execFileSync site to the MCP test profile, exact argv/options/call/reference pins and new mutants. No process args/env collected, no production change. | Pending user |
| M8-C3 | Actual complete dependency selftest rejects runSymlinkedNodeModulesFixture: `reachableFrom path must be a normalized, non-empty, in-repo directory: src/adapters/mcp`. This existing fixture bypasses installDefaultVettedConfiguration. §3.2 C6/§5 exclude its edit. | Permit one mkdirSync of src/adapters/mcp before assertPass, preserving every assertion and expected verdict. | Pending user |
| M8-C4 | §3.10 mandates `{ env, stdio: 'pipe', shell: false }`; pinOptions only accepts PropertyAssignment. Owner inspectSource witness: shorthand → spawn-options; explicit env: env → no violation. | Correct only the required spawn spelling to env: env; preserve the existing gate guard. | Pending user |

Exact combined proposal and separate C2/C3 detail: `artifacts/review-evidence/tinyvault-m8-impl-20260912/m8-c2-c4-proposal.md`. Actual witnesses: `m8-c2-gate-witness.json`, `m8-c3-dependency-selftest.log`, `m8-c4-gate-witness.json`. These are implementation-discovered conflicts under the user's explicit §5 surface-for-disposition rule, not a reopened paper review or a request for implementation authorization.

**Owner checks on frozen integrated partial tree:** `npm run typecheck` PASS; `npm run build:mcp` PASS (external-package bundle); Vitest over src/adapters/mcp, src/browser/retention.test.ts, src/core/fillService.structure.test.ts, testbed/rootOfTrust.test.ts: **13 files, 203 tests PASS**, 47.49seconds. Logs `owner-partial-{typecheck,build,focused}.log`. `git diff --check` PASS. Existing core assertions now pass with only the authorized caller-list edit. Source carve-outs verified exactly: loop export only, supervisor re-export only, one core literal only, two testbed digest values only. Five preserved client probe messages independently byte-matched (probe-fixture-byte-verification.json); actual fetched page headers audited (fixture-header-audit.md). Node24.19.0 and installed `claude --version`2.1.258 verified; Docker29.6.2 Linuxaarch64 daemon reachable after sandbox socket denial via approved host read. Docker availability is not a test-docker result.

**Worker preflight (not final acceptance):** typecheck PASS;146 adapter +6 retention tests PASS; real dependency gate and isolated newly appended composition fixtures PASS; complete dependency selftest RED at C3; docker gate/full docker selftest PASS, retaining111 existing source mutants,41 guard deletions and adding5 MCP mutants. M1 canned result, M2 pretty serialization, M3 snapshot misroute, M4 false error flag, M5 verdict-observable stderr and M6 drain deletion each red on intended assertions with hashes restored (`slice-b-mutation-results.json`, individual logs). Owner exact-candidate mutation table remains due. All worker job sessions completed; no browser job was launched.

**Not run:** final make test, make test-docker, make eval-stub, T-STDIO, literal clean clone, owner exact-candidate mutants, scripted interoperability, paid-client interoperability, independent Claude QA/security and fresh Codex adversarial review. Reason: implementation incomplete at three explicit contract STOPs; the no-spend owner driver compiles and is prepared but has not been executed. Reviews0/3; do not spend a capped review round on the unfinished candidate. Provider interoperability allowance remains absent. Neither historical cohort changed; no merge/push/cohort/public flip.

**Deviations From Handoff:** partial checkpoint at verified M8-C2–C4 instead of a reviewed/gated acceptance candidate. Additional worker mutation preflight is explicitly separate from owner-required mutation gates. All dependent changes await user disposition; the standing implementation/review authorization is retained. Codex continuity paused after preserving this checkpoint; no jobs remain.


## M8-C2–C4 approved (2026-09-12, owner codex)

The user approved the combined exact proposal after checkpoint `c546a99daf2b8bc240f21a75f648b2d76dd225ca`. Owner reverified clean branch/worktree/HEAD, one worktree, completed native workers and no competing TinyVault test/review/interop processes. Codex resumes active continuity. Recorded the three narrow amendments in the locked packet, preserving previous historical entries and completed review caps. Implementation authorization and independent-review authorization remain active; paid interoperability allowance remains absent. Resume the same Astra B worker for only the approved gate fixes and T-STDIO; owner holds all other candidate files unchanged until the worker freezes its delivery.


## M8 implementation — C2–C4 integrated; M8-C5 signal ownership conflict (2026-09-12)

Codex verified the frozen Astra four-file hashes (`slice-b-c2-c4-frozen-sha256.json`). Typecheck, both static gates, complete selftests and9 structural/retention tests PASS. Owner T-STDIO sandbox run: four startup failures with fixed internal-error/code4. Escalated real-host run:3/4 PASS, forced-kill witness RED with exit130 before SIGKILL (`owner-stdio-host.log`). No Chromium survivor was established by this failure; it failed before the forced-kill witness.

**M8-C5 — pending user disposition, not implemented:** Playwright1.62.1 `coreBundle.js:8861-8878` installs a SIGINT handler which closes browsers then calls `process.exit(130)`; launch defaults all three signal handlers to true (`:39534-39536`). This conflicts with packet§0 exit vocabulary and§3.7 entry-owned shutdown. Recommend adding optional `handleSignals?: false` to `createSupervisedHost`, forwarding it through `launchChromium` to the three Playwright signal flags only when explicitly false (existing callers unchanged), and passing exact literal `handleSignals: false` only in MCP main. Amend the MCP composition pin and T-RC-10 host-shape pin narrowly for that exact MCP option; add positive/default and rejection/mutation tests. Required scope exceptions: browser launch helper and focused new test, supervisor factory type/forwarding, core structural test, MCP main/structural/lifecycle/stdio tests as needed; no fill or authorization behavior change. Do not use global listener removal or loosen the forced-kill witness. Locked§5 requires disposition before dependent edits. Review rounds remain0/3.


## M8 owner integration — SCHEMA section placement correction (2026-09-12)

Exact checkpoint `adbb5d0ec360ccdd569faea5bf8043d604754d0d`: owner `make test` reached3,499 PASS,2FAIL,1SKIP (JSON/log preserved); no timing stages ran after the main-stage failure. In addition to C5, `testbed/parity/claims.test.ts:450` failed because appending a heading after the terminal claim-pinned section made its unmarked-tail-addition mutant ineffective. Owner moved the **unchanged** MCP contract before the existing terminal evidence section, preserving every claim/test byte and gate condition. Four P-scope tests PASS (`owner-schema-claim-position.log`); MCP section exact-byte equality and hash in `schema-position-fix.json`. This is within authorized owner SCHEMA edits, no contract amendment or test change. C5 remains pending; review rounds0/3. Docker sandbox attempt actually failed with daemon-unreachable/Chromium launch errors; owner host attempt follows.


## M8 owner verification checkpoint d15a11d — C5 pending (2026-09-12)

**Correction to the immediately preceding entry:** the P-scope command passed9 tests, not4;144 unrelated tests were deliberately deselected. The MCP section was byte-identical before/after placement.

Exact implementation checkpoint `d15a11d3e28e272fdb883d7de1a2be090a20c3dc`: owner Docker7/7 and execution guard PASS; stub eval1/1(189.40s) and guard PASS; decoder timing5/5,host timing26/26PASS, executed serially on a quiet host. All13 owner mutants killed through runtime assertions; exact before/restoredSHA256 equal; restored focused baseline152/152PASS. Table `artifacts/review-evidence/tinyvault-m8-impl-20260912/owner-mutants-d15a11d3/table.json`. Both gate scripts/selftests and typecheck passed in theadbb5d0 full-gate invocation; the later d15a11d delta is the SCHEMA placement fix and this register only. Full make-test remains RED from the documented SIGINT failure; do not call these component results a passing full gate.

Scripted no-spend interoperability on the exactd15a11d bundle: firstfillok:true and authorized synthetic receipt; freshcontrol sameprocess samehandle→handle-exhausted; all three setup templates do not reset it; wrongorigin→origin-not-authorized; parent EOF/restart with samevault/handle→anotherok:true and secondreceipt. Both exits0; no tested secret transform in stdout/stderr;0provider calls. Result/wire `interop-d15a11d-{result.json,wire.jsonl}`. **This verifies the stated restart limitation, not renewal isolation or E8c qualification.** Historical cohorts unchanged. Accepted metadata conformance uncertainty and all packet§9 residuals retained.

C5 still awaits user disposition; no browser/supervisor/core-test source changes made. Separate$2total provider allowance requested for two bounded installed-client runs (legacy/modern;Haiku4.5;4turns;$0.50CLI budget each;no automatic retry), not granted or spent. Clean-clone green gate and independentClaudeQA/security/freshCodexadversarial remain pending the corrected committed candidate; review rounds0/3. All jobs completed; writer remainsCodex. Complete report/evidence index `owner-checkpoint-d15a11d.md`. No merge,push,new cohort or public flip. Deviations: approvedC1–C4, surfacedC5, authorizedSCHEMA placement correction; incomplete acceptance explicitly retained.


## M8-C5 and separate provider allowance APPROVED (2026-09-12)

User approved the final checkpoint proposal: C5 as recorded in `artifacts/review-evidence/tinyvault-m8-impl-20260912/m8-c5-proposal.md`, including narrow browser/supervisor/core-test exceptions and exact MCP signal option, and a separate$2total allowance for two bounded installed-client runs (legacy+modern;Haiku4.5;4turns;$0.50CLI budget each;no automatic retry). Verified clean cbca9f0, sole worktree, no active matching jobs before writes. OwnerCodex active; required independent reviews retain standing authorization; rounds0/3. No completed paper/delta reviews reopened.


## M8-C5 implementation preflight (2026-09-12)

Owner implemented the approved false-only signal option and forwarding, MCP-only literal pins, default-preservation and negative tests, and normalSIGINT/SIGTERM/SIGHUP stdio checks. Typecheck and real static gates PASS;7 selected structural/option tests PASS;12 real stdio/old-new launch tests PASS (`owner-c5-real-stdio.log`). Actual forcedSIGKILL now observed, with every captured descendant absent within the unchanged5s limit. No Playwright globals/listeners were intercepted; sole host/domain/fill construction preserved. C5 remains subject to full exact-candidate gates and the0/3-used independent review ladder. Provider allowance$2total/two calls remains unused.


## M8-C5 auditability-cap correction (2026-09-12)

Full make-test onb6704e2:3507PASS,1FAIL,1SKIP; the sole failure was the existing core structural file's under800-line cap (818lines). Signal checks passed; no timing/Docker/stub stages followed the failed gate. Owner moved only the newly added C5 negative cases to the already approved `src/browser/playwright.signals.test.ts`; they extract and execute the actual private `inspectHostShape` and its actual wrapper/key helpers via TypeScript transpilation, without copying the guard or importing/registering the whole core suite. The guard predicate occupies one line; core file799lines. All existing core assertions remain unchanged. Typecheck/static gates PASS;9 selected cap/authority/option/adapter checks PASS. No limit, verdict or approved scope was widened. Full exact-candidate sequence restarts after commit; rounds0/3; provider allowance unused.

## M8 post-implementation round 1 — owner dispositions (2026-09-12)

Reviewed candidate **234c2f219e13aa2256b6732dbea098c14f3921e2**, base2249a6248900f84fe4da5f897836318732fd2430, held clean throughout all three reviews. Owner: codex. **One of three implementation rounds used**; paper/delta caps remain closed. Fresh Codex Astra adversarial returned NEEDS-ATTENTION (one P2); fresh Claude Opus5 QA returned PASS (three P3); separate fresh Opus5 security returned NEEDS-ATTENTION (three P3/limitations). Both Claude dispatches completed with unchanged candidate digest `cdb1610046684f2a15bfcfbad7a99a0ad2da234c075b56ce81499ef2b0ba4b89`, actual assistant model `claude-opus-5`, no denied-tool/model/drift failure. Raw reports remain outside source worktrees at `/tmp/tinyvault-m8-implementation-reviews/round1-{qa,security}-234c2f2/` and `round1-codex-adversarial-234c2f2-report.md`. Review spend uses standing review authorization, separately from interoperability.

**Exact reviewed-candidate execution evidence:** real checkout make test PASS (3508passed,0failed,1intentional offline-eval skip; timing5/5 and26/26; execution guard PASS); Docker7/7 with both metric records complete; stub eval1/1,50/50stub-safe completions/0leaks; literal `git clone --no-local`, detached sameSHA, real independently installed node_modules, full make test PASS with identical counts. Initial clone npmci sandbox DNS failure preserved; host npmci passed without lock/script-policy changes. Both gate scripts and selftests ran in both full gates: prior111subprocess source mutants/41guard deletions/2profile deletions preserved; MCP43source mutants/6guard deletions plus capability/profile/ps-binding deletions PASS; compositionC0–C6 PASS. Owner20mutants killed and all source hashes restored; restored adapter/browser baseline163/163PASS. **Evidence precision:**19mutants had assertion failures; T-HOST-CONNECTION failed at an unexpected successful response shape with TypeError before the later factory-count assertion. It is a real non-equivalent runtime failure, not a compile/timeout kill; the old blanket “runtime assertion” label was too broad. The fix candidate moves the existing factory-count assertion earlier so that mutant fails directly on the violated host invariant. Nothing in this paragraph grants acceptance to later changed bytes.

**Interoperability on234c2f2:** scripted driver firstfill succeeds, same-process fresh control and post-setup reuse return handle-exhausted, wrong-origin fill refuses, same vault/handle after parent restart fills again; two authorized synthetic receipts, exits0, no tested secret-transform hits. This verifies the stated restart limitation, not renewal isolation or E8c qualification. Installed Claude Code2.1.258 passed tools/list and exactly one list_vault in legacy2025-11-25 and modern2026-07-28 opt-in. Two bounded Haiku4.5 provider runs cost **$0.01255 total**; no provider retries; all three adapter processes exited0, all observed proxy/adapter PIDs gone. An initial CLI-only argument failure consumed the prompt as a second --mcp-config filename, before any MCP/model events; preserved and corrected with `--` before the first actual provider calls. The logging wrapper is an instrumentation limit. The two-run allowance is now consumed by count; further paid client runs require a count extension even though money remains under$2.

| Finding | Owner verification and disposition |
|---|---|
| ADV-1 P2 cancellation after response enqueue | **Verified and fixed.** Reader sets cancelled while response waits on deferred output; old closure checks only connection.failed. Owner reproduced same-chunk tools/list+cancel and completed tool result held behind parse-error output without candidate edits during review. Four new legacy/modern regressions red on old code, then green after passing the request to the deferred write and rechecking cancelled immediately before output.write. FIFO/following request and single dispatch remain proved; no host-work cancellation or authority change. |
| ADV evidence label | **Corrected as above.** All20before/restored/current hashes match. Move existing host/factory-count assertions before response dereferences, keeping every original assertion/verdict. |
| QA-F1 P3 default createServer options | **Removal declined; coverage added.** createServer(host) is the locked public factory form, not an unreachable branch merely because current callers pass explicit test options. Keep default version behavior and add a real one-argument discovery test. No second host/version source is introduced. |
| QA-F2 P3 EOF registration order | **Comment and regression added.** Existing entry end listener closes admission before protocol EOF fragment processing. New actual-entry test sends an unterminated tools/call, requires -32600, zero host calls and exit0. No behavior/contract change. |
| QA-F3 P3 ps prerequisite | **Docs clarified.** README/SCHEMA now name `/bin/ps -axo pid=,ppid=,comm=` as a test-only prerequisite of T-STDIO/default gate, not a production subprocess. C2 executable/options pins unchanged. |
| SEC-F1 P3 cwd | **Docs clarified; relocation change declined.** Locked launch is from an installed checkout; README/SCHEMA now explicitly require process cwd at checkout root, even with an absolute bundle path. Owner unsupported-cwd probe gives exit4/fixed diagnostic/empty stdout before host creation. The successful installed-client proxy source explicitly spawns with cwd=checkout; the summary JSON did not independently record OS cwd. No new build define, manifest relocation or spawn-profile option. |
| SEC-F2 P3 final flush | **Accepted existing residual, mechanism recorded.** Unowned protocol writes can leave final flush pending on an open unread pipe after the active-handler deadline; no new shutdown bound is adopted. Contrary to the report's Chromium-residency sentence, main awaits closeAll before this final flush. Shutdown timing remains unbounded; no ProbeP claim. |
| SEC-F3 P3 legacy-ID history | **Recorded design residual.** Process-lifetime no-reuse requires a growing Set; the1MiB per-line bound does not bound cumulative history memory. No new history cap or reuse rule is authorized. |
| Other test gaps | Exact owner build and scripted driver already launch dist/tinyvault-mcp.mjs (QA's “neither is a launch” statement disproved by driver source and result bundleBytes156728). New cancellation mutation will accompany the fix. Additional validation/classifier mutants, Linux MCP descendant cleanup and a suite-resident real-fill secret scan remain coverage residuals beyond the named packet gates; existing mandatory owner scripted fill scan remains required. |

**Review precision/limitations:** supplied test executions are owner evidence, not reviewer executions. Several reviewer line locators for the51-line signals test and SCHEMA refer to concatenated/diff offsets rather than actual file lines; owner reverified the actual source. Security's “21-row” prior table is20. Security explicitly omitted reading the full handoff and handoff-pattern despite packet instructions; retain that procedural limitation and supply their required text directly to the fresh round-2 review. No fourth paper/delta round or extra implementation round is inferred from a reviewer mistake.

**Absorption sweep:** `r1-doc-absorption-sweep.log` covers entire edited docs and sibling locked packet/handoff. Old current-status “no code yet”/“M7 not started”/initial pending-gate phrases removed from current README/index/build-status. The checkout-relative launch statements remain with explicit cwd clarification; historical dated handoff/register statements are preserved, with this entry as current disposition. Exact restart sentence remains immediately beside both setup blocks; optional-meta compatibility, all accepted decisions/no-touch boundaries and historical cohorts remain unchanged. No milestone-complete checkbox, merge, push, cohort or public flip.

Evidence home: `artifacts/review-evidence/tinyvault-m8-impl-20260912/`; exact-candidate index `evidence-index-234c2f21.md`, gates/clone/mutants/interop/client logs, `r1-cancellation-witness-result.json`, `r1-regression-red.json` (4intendedfails/2passes), `r1-fix-focused.json`, `r1-cwd-witness.json`. **Next:** commit correction with explicit paths, freeze new SHA, rerun prescribed exact-candidate gates/clone/mutants/scripted interoperability, obtain any needed installed-client count extension while independent work proceeds, and fresh round2QA/security/Codex fix-absorption. No active reviewer remains at the time of these edits.


## M8 post-implementation round 2 — owner dispositions (2026-09-12)

Frozen reviewed candidate **c8588ca5d6a5adf3daa01933c359d34e52e32cef**; base2249a6248900f84fe4da5f897836318732fd2430; branchcodex/m8-mcp-adapter. Round2of3 complete; both fresh Claude Opus5/high channels PASS, fresh Codex adversarial NEEDS-ATTENTION (oneP2). Claude actual assistant model verified from events, no permission denials, both candidate digests `69d84c563eee61cc84abdbf8fae04df04944025d78e35a2640f1283eafa4e5b8`. Reports `/tmp/tinyvault-m8-implementation-reviews/round2-{qa,security}-c8588ca5/report.md` and `round2-codex-adversarial-c8588ca5-report.md`; summaries/events preserved. Both Claude runs read the embedded full required handoff/protocol, repairing round1's reading limitation without resetting the cap. Security says “four” criteria but actually lists all five. All reviewers completed and tree/worktree/jobs verified before edits.

**Exact c8588ca evidence:** make test main3514PASS/0FAIL/1intentional offline skip, timing5/5+26/26 and execution guardPASS; Docker7/7 with both metrics complete; stub1/1,50/50completion,0leaks. Literal `git clone --no-local` detachedc8588ca, no alternates, real independently extracted node_modules via integrity-checked offline npm cache, full make test samecountsPASS. Both gate scripts/fullselftests run in both default gates.21/21 owner mutants each fail with actual AssertionError; every before/restored hash matches; restored baseline169/169PASS. Zero-spend freshly built actual dist scripted check: first fill succeeds, fresh-control and post-setup reuse exhaust, wrong origin refuses, restart permits another fill,2receipts,bothexits0,no tested secret-transform hits. This verifies the restart limitation only. Exact records in `artifacts/review-evidence/tinyvault-m8-impl-20260912/evidence-index-c8588ca5.md`; full logs, clone provenance/install, per-mutant JSON and raw wire retained. Historical cohort archives rehashed unchanged (`historical-cohort-preservation-c8588ca5.json`). No paid client call on c8588ca: prior234c2f2 evidence stays historical; requested two-run extension remains pending.

| Finding | Owner verification and disposition |
|---|---|
| ADV-R2-1 P2 / QA shutdown P3 | **Verified and fixed.** stopAdmission deleted queued IDs before their deferred -32600 and omitted the Request argument. Owner standalone both-era witness emitted cancelled ID2 after held output resumed. Two new tests fail with `[1,2,3,4]` versus `[1,3,4]` before correction. Keep the queued Request outstanding until the existing #replyError settles, so cancellation marks it and the shared deferred writer suppresses it. Noncancelled queued ID3 and post-stop ID4 still receive -32600; only active ID1 dispatches. QA's claim that synchronous deletion made the bug unreachable is rejected: deletion is the defect while output is asynchronous; passing Request alone would be insufficient. |
| SEC unzoned alias P3 | **Verified and fixed.** An unzoned `lib/probe.ts` importing supervisor, symlinked into the adapter directory, passes the real gate CLI before correction. No tsconfig extension is needed, contrary to the report. Change only isCompositionEntry `.some` to `.every`, matching the existing evaluator predicate; keep all locations/zones/old guards unchanged. New isolated fixture requires a static-import violation and real CLI exit1; it fails before and passes after correction. This is repository-author hygiene, not a demonstrated hostile-page exploit or existing data-plane-to-supervisor path. |
| QA default-version P3 | **Coverage limit recorded; suggested assertion declined.** The public one-argument API is exercised. Reading the same current0.0.0 manifest in the expectation would not distinguish a hardcoded0.0.0 default, so that suggested change is not a meaningful mutant kill. Manifest-isolation mocking remains optional coverage, no API/default change. |
| QA EOF mutant P3 | **Absorbed as additional owner mutation evidence.** The existing actual-entry regression remains; the final descriptor adds listener-order relocation rather than modifying its assertions. |
| QA setup prose P3 | **Absorbed.** README/SCHEMA setup lead-ins are sentences, preserving the explicit cwd prerequisite and verbatim restart sentence beside the launch blocks. |
| SEC pending-write memory / legacy history | **Residuals retained and clarified.** Malformed/unadmitted fire-and-forget responses can grow an unbounded pending-write chain on unread stdout, as well as prevent final flush completion after closeAll; process-local self-denial, no new queue/deadline bound authorized. Legacy ID history remains lifetime-unbounded by locked no-reuse semantics. |
| SEC test-only new Function / cap pressure | **Recorded; no change recommended or made.** Fixed trusted repository source is transpiled for supplemental negative witnesses; actual core inventory/call checks still enforce the production caller. No production dynamic code or security-containment claim. Core799 under<800; protocol remains under<400. |
| Remaining coverage observations | Cancellation after output.write has begun cannot retract bytes and remains an unpinned boundary; deleting the early cancellation guard is equivalent while the deferred guard remains. Suite-resident real-fill transform scan, Linux MCP signal/descendant proof and default-manifest isolation remain coverage limits. Mandatory owner real-fill scripted scan remains required on the final candidate. `/bin/ps` is documented mandatory test-only infrastructure, not a production spawn. |

**Red/green proof:** `r2-shutdown-cancel-witness-result.json`, `r2-cancel-regression-red.json` (two AssertionError failures,10passes), `r2-fix-focused.json`; `r2-composition-alias-witness.json` (empty violations/CLI0), `r2-alias-regression-red.log` (actualERR_ASSERTION), `r2-alias-regression-green.log` (full selftest exit0), `r2-typecheck.log` exit0. Both findings were reproduced before correction; no gate or reviewer was running while source changed. Final mutation plan adds two shutdown-lifetime/argument mutants, one EOF-order mutant and the composition-predicate mutant to the prior21, without discarding earlier evidence.

**Final-round rule stated before dispatch:** round3 is the cap. P1 only for a layers1–2 secret/derived-byte MCP-result/stdout/stderr/exit leak; undeclared successful host payload bypassing createTools; red make test; composition amendment admitting data-plane→supervisor; spawn escaping the reviewed pin. Everything else becomes a recorded residual. No round4 or reopened paper/delta ladder. Preserve all packet§9/approved residuals, optional-meta uncertainty and -32021never emitted, no per-caller entitlement or renewal audit claim, unchanged snapshot exemption and production-canary limitation. E8c is qualified only in its evaluated fixture/configuration, distinct from MCP interoperability. No merge,push,cohort or public flip.

**Absorption sweep:** current README/docs index/build status and PLAN now identify round2 absorption and finalround3 pending. Dated historical handoff/register records remain intact. Next: explicit-path correction commit, exact candidate freeze/gates/clean clone/mutants/scripted check, paid-client extension only if explicitly granted, then final independent three-channel review. Deviations remain approvedC1–C5, SCHEMA placement correction, and these owner-verified correctness/hygiene/test/docs absorptions within slice ownership.


## M8 post-implementation round 3 — CAP, final owner disposition and merge-approval candidate (2026-09-12)

**Exact reviewed/gated implementation candidate: `38efe85c4e56b4a371a6670f7225735a89162404`**, base/main2249a6248900f84fe4da5f897836318732fd2430, branchcodex/m8-mcp-adapter. All three final independent channels **PASS under the five locked final-round P1 criteria**; no P1 established. The implementation ladder is complete at3/3; no round4 and no paper/delta restart. This is a return for user merge approval, not a merge/release decision. All jobs/reviewers completed and clean HEAD verified before this documentation-only continuity update; every other tracked file remains byte-identical to the reviewed candidate.

ClaudeQA Opus5/high session4c3b6bb3-0049-4ce2-b1e8-b460b7b14ef7 and security Opus5/high session93773dce-8854-41c4-9d5d-3bc018759d69 completedPASS; actual assistant models and empty permission-denial lists independently parsed from events. Both candidate digests `6d803e968490a8c3c9203dc5f72cd1730a7c8491975dadec8f4cb1ab99bcbdf9`. External reports `/tmp/tinyvault-m8-implementation-reviews/round3-{qa,security}-38efe85c/report.md`; fresh Codex report `round3-codex-adversarial-38efe85c-report.md`. All three verified shutdown cancellation absorption and stricter supervisor-facing composition authority. Reviewers inspected owner execution evidence rather than rerunning gates. QA disclosed it did not read the entire candidate diff end-to-end, despite the packet, instead using the fix diff/inventory and current source; retain this procedural limitation, do not restart the capped channel. Security and Codex read the required full context; several Claude source offsets still needed owner correction (e.g. shutdown regression isprotocol.stdout.test.ts:68–89, not167–189). All findings were adjudicated against actual source/evidence.

**Final exact-candidate gates and evidence:**

| Check | Actual result |
|---|---|
| make test, real owner checkout | PASS;3516passed/0failed/1intentional offline-evalskip; timing5/5+26/26; execution guardPASS.18:23:57–18:36:17UTC. |
| make test-docker | PASS7/7; both parity/surface records complete.18:36:17–18:53:10UTC. |
| make eval-stub | PASS1/1; execution guardPASS.18:53:10–18:56:21UTC. No live model/cohort. |
| Both boundary/subprocess gates and selftests | PASS in both full make-test invocations, all old cases/guards preserved; composition alias regression also executes. |
| Literal clean clone | PASS full make test, matching3516+1skip and5/5+26/26; `/tmp/tinyvault-m8-clean-38efe85c-20260912`, git clone --no-local, detached38efe85c, no alternates, independent realnode_modules via locked offline npm cache. |
| Owner mutants | **25/25 killed by actual AssertionError/ERR_ASSERTION**, all before/restored/currentSHA256 equal. Includes two shutdown ownership/writer mutants, EOF listener ordering and composition predicate revert. No compile/timeout-only kill. |
| Restored baseline | PASS171/171 including actual stdio/signals/descendants, browser retention and C5 witnesses. |
| Build + scripted interoperability | PASS actual156712-byte dist bundle, SHA2565dd96d110393da69e665843d4b5629e88e5154c73358056df24c987f7ac2c2da; firstfillok, sameprocessfreshcontrol and post-setup reuse exhausted, wrongorigin refused, samevault/handle restart permits anotherfill;2authorizedreceipts,bothexits0,no tested secret-transform hits,0provider calls. |
| Historical preservation | Both cohort archives rehashed exactly to e6081902… (E8b) and cdcc4958… (E8c); no cohort/fixture/checker edits. |

Evidence home `artifacts/review-evidence/tinyvault-m8-impl-20260912/`: `evidence-index-38efe85c.md`, owner/clone logs and JSON, `mutant-table-38efe85c.md`, full `owner-mutants-38efe85c/table.json`, descriptor and exact mutation logs, `interop-38efe85c-{result.json,wire.jsonl}`, bundle hash, `scope-audit-38efe85c.json`, fixture provenance and cohort preservation records, `round3-review-verification.json`. All prior failures/runs remain preserved. Timing suites ran serially; no candidate edits during gates/reviews. The final extra residual witnesses below modify only throwaway fixtures/ignored evidence, never candidate source.

**Final-round findings and owner dispositions (all nonblocking under the exact cap):**

| Finding | Verified disposition |
|---|---|
| QA6 / SEC evaluator-facing predicate coupling | **Confirmed, prominent capped residual.** An unzoned real `lib/probe.ts` importing testbed, symlinked under MCP, yields oldc858CLI1/production-to-evaluator and new38CLI0/emptyviolations. Owner actual CLI witness `r3-evaluator-alias-witness.json`; no tsconfig extension needed. This corrects security's narrower “future include change only” framing. The same `.every` closes supervisor authority but removes the evaluator-facing verdict for this alias shape. It violates the broad no-verdict-movement intent in that direction, while no data-plane→supervisor path is admitted. Record the tradeoff for user merge disposition; no fourth-round patch or P1 inflation. |
| QA7 / SEC deferred deletion and flush | **Confirmed API-level hygiene residual.** Owner blocked-output witness with1/2/3queued shutdown rejections leaves one output error listener after firstflush and a microtask turn; secondflush clears it. `r3-flush-witness-result.json`. Public flush ordering reproduced; actual main-finalization timing was not separately reproduced. Responses correct; process-local retention, no secret/authority effect. |
| QA8 SCHEMA shutdown paragraph | **Clarification residual.** SCHEMA575's unqualified queued-request rejection sentence must be read with509's cancellation-suppression rule. The latter remains governing; no cancelled request receives a response before write. This is not a new cancellation-policy amendment, and even earlier shutdown skipped already-cancelled work. No locked-SCHEMA patch after the cap. |
| QA9 alias case has no dedicated PASS banner | **Reporting residual, not silent green on failure.** The case runs after the prior banner; an assertion failure exits1 and stops make test, as the retained red witness proves. A banner alone would not detect removal of the case. The final owner alias mutant proves current execution; no additional cap fix. |
| QA10 hypothetical async PassThrough “false green” | **Rejected as stated.** If asynchronous delivery emitted `[1,2,3,4]`, the existing expected `[1,3,4]` assertion fails; that is not a false green. Current synchronous delivery and both runtime mutant kills establish the intended witness. |
| SEC cancelled-request continue branch | **Future-maintenance observation.** Current cancellation removes queued entries and marks only active work otherwise, so the branch cannot currently leave an outstanding slot. No demonstrated reachable defect. |
| Remaining coverage/limits | No subprocess-specific shutdown-cancel/backpressure regression beyond the in-process protocol tests; no post-output.write retraction witness or default-manifest isolation; real-fill transform scan remains owner-scripted, not suite-resident; Linux MCP SIGKILL/descendant proof absent. `/bin/ps` is mandatory test-only infrastructure. Preserve unbounded pending writes/legacy history/finalflush/trustedwork, finite framing, no shutdown-timing guarantee, unchanged snapshot exemption, nonrelocatable/unscanneddist/static hygiene limits, no per-caller entitlement/renewal audit. |

**Interop and spend:** the exact-candidate zero-spend scripted driver satisfies the requested firstfill→sameprocess exhaustion→restart→possible refill verification. It verifies the stated limitation. Process recreation grants fresh fill authorization; no renewal-isolation claim in automatic-restart/model-shell harnesses. Installed Claude Code2.1.258 two-era paid evidence remains **historical on234c2f2**, total$0.01255. Two authorized client runs consumed the count; the requested two-run extension received no answer, so **Not run: paid installed-client confirmation on38efe85c, pending separate allowance**. Required independent Claude review calls retain their separate standing authorization. No extra client calls, automatic retries or cohort runs occurred. E8cPFc7eGp2 remains qualified only in its evaluated fixture/configuration; E8bODMFYbwH remains measured/unqualified. Neither is MCP interoperability evidence.

**Deviations From Handoff:** user-approved C1 notification-ID reconciliation; C2 exact test-only ps inventory; C3 fixture mkdir; C4 explicit env/shell spawn shape; C5 narrowly confined Playwright signal ownership, with old-caller defaults and core authority guards preserved. Authorized SCHEMA section placement correction retained existing claim-test boundaries. Post-implementation owner corrections added cancellation and alias regressions/mutants plus launch/status clarification. Finalcap records the newly verified evaluator-alias verdict movement and flush-listener residual rather than silently correcting or reopening reviews. QA's full-diff-reading omission and reviewer static-only execution limits are disclosed. A residual-witness build invocation mistakenly passed the native esbuild binary to Node; it failed before a witness ran, then the ordinary esbuild API built the ignored witness successfully—no candidate/gate failure or source change. Historical sandbox failures were attempted and retained; real owner gates actually passed. Final continuity/status documentation commit is separate from the exact38efe85c reviewed/gated implementation; no claims of full-gate/review execution on that later record commit. No merge, push, additional cohort or public flip.


## 2026-09-12 — Authorized local merge; merged-tree gate RED; push held

**Owner: Codex Astra.** User approved the recommendation to merge, run merged-tree checks, then push, including the 19 previously unpushed commits and M8 history, with the disclosed capped residuals accepted. This supersedes the earlier no-merge/no-push authorization status only; no provider-client extension, cohort, public flip, M9 work, or fourth M8 implementation round is authorized.

**Merge identity:** `412b6687fe28fb5736ce91692eb0a1c5ac2bb9b0`, `main`, explicit `--no-ff`; parents `2249a6248900f84fe4da5f897836318732fd2430` and `196e7170b2fa54bfbc4c1e3e8a6b775f1e202901`. Tree `bf3a69524ea0ee3b40396f4592893f6f12b13bd3` equals the branch tip exactly. One worktree, no merge conflicts. Live remote main remained `bac91dbe73b1d27c583694252360f4ba579bcd89` at preflight. **No push performed.** Prior exact-candidate acceptance on `38efe85c` remains historical evidence, not a passing merged-tree gate.

**Actual integration result:** `make test` on merge412b6687, 2026-09-12 19:45:10.065Z–19:53:42.534Z, exit2: main3517 total,3515passed,1failed,1 intentional offline-eval skip. The failed test is `testbed/runner.finalization.browser.test.ts:435-464`, “runEval arms finalization before a genuine wedged CDP body capture and fails after disposal settles it.” At457 expected `Evidence capture failed`; actual rejection was `No run observed the canary in the authorized login capture...`. The serial pipeline stopped on that red. Timing1/timing2, Docker and stub integration stages **not run: earlier main-stage failure**. Milestone-close cross-model assessment **not run: closure blocked** (draft prepared outside checkout, never dispatched). No retry-to-green and no gate waiver.

**Owner diagnosis, not a cleared gate:** this M6 test file is unchanged by M8. Its `holdActualBodyUntilDisposal` combines real `Network.getRequestPostData` and `Runtime.evaluate('0')`; `endWithPendingPost` verifies pending after50ms, but does not guarantee the real round trip remains pending until finalization disposal. The saved failed-run capture contains the harmless POST body `payload=runEval-pending-body` with `bodiesUnobserved:0`; finalization completed and the deliberately non-login stub subsequently failed the positive-control checker. The precise cause of the renderer round trip resuming was not established. Black-hole TCP preflight covers2s, not the whole finalization interval. A predeclared fixed diagnostic of exactly three serial isolated runs on unchanged merge412b6687 passed3/3, exits0; these are diagnosis only and do not overwrite or clear the full gate failure. No production/test byte changed during any gate or diagnostic.

Evidence (ignored, preserved): `artifacts/review-evidence/tinyvault-m8-impl-20260912/merge-identity.json`, `owner-412b6687-{test.log,main.json,gates.json}`, `merge-red-finalization/{provenance.json,run-artifacts/,diagnostic-plan.json,diagnostic-results.json,diagnostic-1.json,diagnostic-2.json,diagnostic-3.json}` and each diagnostic log. The original failed fixture artifacts remain under `.vitest/s4-finalization/runEval-wedged-body-1789242502249` and a separate evidence copy. Earlier candidate, clone, mutant, interop and review artifacts are retained.

**Scope conflict and proposed follow-up — NOT AUTHORIZED:** M8 packet§5 makes `testbed/**` read-only except named carve-outs; this test is outside them. Request a separate bounded M6 witness repair, owner Astra, production and M8 adapter unchanged, test ownership only `testbed/runner.finalization.browser.test.ts` plus canonical owner dispositions. Investigate replacing the incidental renderer stall with an explicitly pending real CDP `Runtime.evaluate` awaited promise that disposal must reject, while retaining the actual POST/body capture, real `runEval`/host finalization path, strict capture-failure expectation, settled-work/context-removal and no-scorecard assertions. This is a proposed mechanism, not a proven patch. Do not broaden the expected error to accept the positive-control failure, add skip/retry, relax timing thresholds, or substitute a Node-side fake promise for real CDP work. Validate the targeted real-browser witness and a deletion mutant proving the finalization-order assertion still fails, then full candidate/merged-tree/clean-clone gates and bounded independent review of this separate repair; keep the completed M8 cap closed. Any production change or additional file ownership requires a further explicit disposition.

**Continuity and remaining work:** merge is retained locally; push stays blocked. All owner gate and diagnostic processes completed; no new review or assessment dispatched. After the separate test disposition and passing required gates, complete the mandatory cross-model close assessment and owner verification, reconcile milestone completion checkboxes, recheck live remote and push non-force under the existing user approval. No need to ask for merge/push approval again. The paid-client count extension remains unanswered; do not execute it. Both historical cohorts remain untouched and the exact restart limitation stays beside setup.

**Deviations From Handoff:** user-authorized merge supersedes the original return-before-merge boundary. Merge acceptance/push and completion checkboxes are deferred because the mandatory merged-tree gate is red; the permitted diagnostic3/3 does not replace it. The proposed out-of-scope test repair has not been made. Prior C1-C5 amendments and capped residuals remain as recorded.


## 2026-09-12 — Separate M6 finalization witness repair APPROVED

User approved the preceding bounded repair proposal and verification sequence. Owner Codex Astra; branch `codex/m6-finalization-witness`, base `14b0801f9ddc65f5d36d6f9eb5d31697b30046c5`. Exact editable test path: `testbed/runner.finalization.browser.test.ts`; owner may update existing continuity/register documents. Production edits are not authorized; the named finalization deletion mutant is temporary and restored byte-for-byte. Keep the actual POST and real CDP work, strict capture-failure expectation, settled capture/context removal/no-scorecard assertions, no skip/retry/threshold relaxation. The failed merged gate remains preserved; the three isolated passes were diagnostic only. Independent bounded review of this separate repair and mandatory close assessment retain standing authorization; they do not reopen the completed M8 three-round ladder. Merge/push approval persists after required verification. No provider-client allowance extension, cohort or public flip.


**Repair implementation and focused evidence (before candidate commit):** the sole test-helper change sends real `Runtime.evaluate` with `awaitPromise:true` and stores its unresolved promise on the renderer global, so the pending command does not depend on an incidental navigation stall or collection of a temporary promise. Every original assertion and test timeout remains unchanged. Final repaired witness1/1PASS;30other tests filtered out, not a full-gate result. `npm run typecheck` passed before the final string/comment adjustment; full candidate typecheck remains required. Finalization deletion mutant (remove the quiesce branch, leaving evidence drain before finalization): the named real-browser test fails at18000ms; exact production restorationSHA256 `a908e6258adeadd82845b8e70b750211a50f7f2f2dce5d55ede9f971ad1b6d01`. Initial JSON-only mutant evidence lacked the timeout message and its evidence validator failed; a verbose rerun recorded it, and the same mutant was rerun once against the final renderer-retained promise. All attempts preserved, one distinct mutant. No other tracked implementation file changed. Evidence `artifacts/review-evidence/tinyvault-m8-impl-20260912/m6-witness-repair/{repair-provenance.json,final-focused.json,order-mutant-final-result.json,order-mutant-final.log}`. Full gates, clone and independent scoped review pending; no cleared integration claim yet.


## 2026-09-12 — Separate M6 repair QA round1 and owner correction

Fresh Opus5/high read-only QA on `265def0b1d0b4016b1086224edf42590487ff4ee`: **NEEDS-ATTENTION**, session `9ec1a296-c307-48a1-ac19-7d64c39ce2bf`, digest `8b96ff6da9d733876f66a38935eb8c09ec324af512ac908e4668f27a720cb6c2`; raw report `/tmp/tinyvault-m8-implementation-reviews/m6-witness-repair-qa-265def0/report.md`. This is the separate authorized M6 repair review, not M8 round4.

- **P1 line cap — CONFIRMED and corrected in scope.** Actual split-newline count800 violates the unchanged `<800` assertion in `src/core/fillService.structure.test.ts:87-98`. The full candidate `make test` also completed RED at20:39:17.633Z (started20:30:53.845Z), exit2,3515passed/1failed/1skip, only the auditability assertion failed; the repaired real-browser witness passed. Timing/Docker/stub stages not run after that red. Evidence `owner-265def0b-{test.log,main.json,gates.json}`. Owner compacted only the new helper comment/call to798 split lines; no cap change, assertion removal or production edit. Initial focused verification missed this structural gate; the independent review and full gate caught it.
- **P2 settlement-source observability — accepted residual.** The witness does not identify separately which of native body/renderer work kept the capture pending; early settlement still fails closed at pending check or positive-control validation. Reviewer traced the real capture/finalization path and found no false-green counterexample; the deletion mutant remains meaningful. No extra instrumentation is included in this bounded repair.
- **P3 comment about resumed navigation — confirmed/narrowed.** A committed navigation can destroy the execution context and cause a red; the comment now states only renderer retention against collection. The2s TCP preflight still does not guarantee the full interval; no broad deterministic-network claim is made.
- **Info authorization home — clarified.** M6 test ownership is authorized by the user and recorded in PLAN plus this append-only register, not as a rewritten M8 contract. The review packet's general reference to appended scope exceptions was ambiguous; the correction packet names the actual authorization entry. No new packet exception section is required. Reviewer line references to PLAN9 should be15 in the real file.
- **Review limits:** read-only, no executed tests/digest calculation by reviewer. The report listed only§0:28-74 and§13:599-615 rather than complete§0/§13; this is a procedural reading limitation. The next focused confirmation must read the complete requested sections. Other positive findings (all prior assertions, real POST/native CDP, closed contexts, no scorecard, production identity, ~7.35s execution) were checked against source and owner evidence.

The correction was applied only after gate67117 and reviewer81268 completed. Candidate and clone gates will be rerun on the corrected commit; the old clone is retained as evidence but cannot stand for the correction. No push yet; original merged-tree red and both repair-red attempts remain preserved.


**Correction verification:** native line-cap assertion and real-browser witness2/2PASS (`m6-witness-repair/compact-focused.json`;61unselected assertions, not a full gate). TypeScript AST comparison against265def0 confirms executable AST unchanged after comment/format/trailing-comma compaction;798split lines, `compact-ast-identity.json`. The actual finalization deletion mutant remains applicable to the unchanged executable AST; no new mutant is claimed. Full acceptance and fresh focused confirmation remain pending until their actual reports.
