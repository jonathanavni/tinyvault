# M8 implementation handoff — to a fresh Codex session with GPT-6 Astra as continuity owner (execution-ready; written at the 2026-09-12 wrapup of Claude session `2026-09-11-m8-rev5`)

**Purpose.** A fresh Codex session, with **GPT-6 Astra** selected, becomes continuity owner and implements the M8 MCP stdio adapter under the **locked** packet [`m8-mcp-adapter-packet.md`](m8-mcp-adapter-packet.md) **rev 5.2** through the ladder of [`handoff-pattern.md` §0 (Codex-led ladder)](handoff-pattern.md#codex-led-ladder) and packet §6, and returns the **reviewed, gated implementation candidate with interoperability evidence** for the user's merge approval. Nothing here re-opens a settled paper review, an accepted decision or the locked contract. The Claude session that wrote the packet has **relinquished continuity** (its `PLAN.md` checkpoint is `closed`) and has stopped writing shared state; it dispatches nothing.

## 1. State to verify first (read-only)

- **Integration checkout:** `/Users/jonathanavni/Documents/Coding/tinyvault`, branch `main`. **Base SHA for M8 work: `36aeb54`** ("M8 packet rev 5.2 LOCKED"), whose parent is `08d4d01` (the session-D wrapup); the expected `main` tip is the wrapup commit that added this file (its parent is `36aeb54`). `origin/main` = `bac91db`; **everything after is local and unpushed; no push is authorized.** Working tree clean apart from gitignored `artifacts/`, `.vitest/`. No worktrees, no running Codex jobs, no background monitors (verified at the Claude wrapup: `codex-companion status --all --json` running list empty; `pgrep` for the session's monitors and probe servers empty).
- **Locked packet:** `docs/m8-mcp-adapter-packet.md` header must read "Revision 5.2 — LOCKED by the user on 2026-09-12". Register: `docs/m8-review-findings.md`, last entry "M8 packet rev 5.2 LOCKED — user decisions". `PLAN.md` Decisions Log: "USER DECISIONS on the M8 packet (rev 5.2) — LOCKED" (2026-09-12), "USER DECISIONS on the M8 packet (rev 4)" (2026-09-10), "USER DECISIONS on the runtime fill-control packet" (2026-09-11, O-RC-3/O-RC-8), "USER DECISIONS on E8c" (O-E8c-1).
- **Tree facts the packet pins at `08d4d01`** (unchanged by `36aeb54`, a docs-only commit): `src/supervisor/host.ts` (`createSupervisedHost` `:64-99`, the only `createFillService` call `:94`, `composeSupervisedHost` `:102-108`), `src/core/fillService.ts:140-200,290-303`, `src/core/fillService.structure.test.ts` T-RC-10 (`HOST_CALL_FILES` `:595-596`; `inspectHostShape` `:604-612`; `inspectAuthorityAccess` `:539-555`; `inspectHostArgument` `:657-669`; the sole-`createFillService`-caller assertion `:499`/`:517`), `SCHEMA.md:49,96`, `src/core/types.ts:44`. Re-read them; a drift is a STOP for disposition, not a silent re-pin.
- **Evidence directories (gitignored, keep):** `artifacts/review-evidence/tinyvault-m8-packet-20260910/` (paper rounds 1–3), `artifacts/review-evidence/tinyvault-m8-rev5-20260911/` (`spec/` fetched pages + `schema-2026-07-28.ts`; `probe/` the logging server, config, seven run logs/outputs; `probe-sha256.txt`; `claude-code-mcp-doc.md`; the four Sol delta prompts/reports), runtime control `…/tinyvault-m7-runtime-control-{20260911,impl-20260911}/`, E8b `…/tinyvault-m7-e8b-20260910/` (tarball `e6081902…`), E8c `…/tinyvault-m7-e8c-20260911/` (tarball `cdcc4958…`).

## 2. The user's decisions (2026-09-12) — binding

1. **Rev 5.2 is locked; implementation is authorized to you (Astra, continuity owner) through the prescribed ladder.** Not implemented or dispatched from the Claude session.
2. **Accepted as measured and as the stated limitation:** the compatibility decisions of packet §3.4 and fact 16, and §3.11 (5): **process recreation grants fresh authorization, so the adapter does not establish renewal isolation in the tested Claude Code configuration.** That sentence stands **beside the setup instructions** (SCHEMA/README, packet §4.5, verbatim) and **E8c's evaluated configuration is distinguished from MCP interoperability evidence** (packet §3.11 last bullet).
3. **Final interoperability check with a throwaway vault** (packet §6 step 3): first fill → `{ok:true}`; second fill on the same handle in the same process → `handle-exhausted`; parent restarts the adapter process; whether another fill becomes possible (expected yes). **It verifies the limitation; it is not a passing security claim.** A scripted stdio driver (no model) needs no allowance. **Any paid client call — the installed Claude Code driving a model — requires an explicit provider-spend allowance from the user before execution; none is granted.**
4. **Preserve exactly** the metadata compatibility choices and their conformance uncertainty (packet §3.4 step 5: optional `_meta` values accepted without inspection; `-32021` never emitted; `_meta` permitted on every request in both eras).
5. **Surface implementation-discovered contract conflicts for the user's disposition** (packet §5 last clause) — a recorded amendment, never a silent resolution in code.
6. **Preserve the independent Claude QA/security channels and a fresh-context Codex adversarial review** (handoff-pattern §0 ladder steps 4–5; §7.1 security channel; `scripts/claude-review.mjs` with the standing 2026-09-05 authorization). **Do not restart completed paper reviews** (§4 below).
7. **Return the reviewed, gated candidate with interoperability evidence before merge approval. No merge, no push, no additional cohort, no public flip.**

## 3. What is already done (do not redo)

- **Paper ladder on the packet, complete:** rounds 1–3 on revs 1–3 (Sol read-only + blind fresh-context Opus each; round 3 was the cap; rev 4 the post-cap confirmation pass) — `docs/m8-review-findings.md` "Paper round 1/2/3". User decisions on O-2…O-8 (2026-09-10).
- **Rev 4 → 5 contract correction, complete:** the `_meta` reconciliation (all four modern keys namespaced; table with file:line, register "Rev 5 correction" (a)); the authorized client probe (register (b); packet fact 16 F1–F9); O-1 resolved on measurement (dual-era, served concurrently); §3.11 D-M8-11 (runtime-control §6.6 (0)–(6), O-RC-3, O-RC-8, O-E8c-1) and D-M8-12; T-HOST; T-STRUCT/T-WIRE/T-LIFE/T-STDIO/T-SEAM additions; ledger A12–A15 and the §6.6→ledger map; the citation re-pin at `08d4d01`. **Delta ladder, complete and capped:** two read-only Sol rounds (register "Rev 5 delta review — round 1" and "— round 2 (the cap)"): 7 P1 / 5 P2 / 1 P3, 12 absorbed, one P2 rejected on the evidence, owner confirmation pass → rev 5.2.
- **Measured client facts you build against (packet fact 16):** Claude Code 2.1.258 opens legacy `2025-11-25` by default; modern `2026-07-28` only under `MCP_PROTOCOL_NEGOTIATION=auto` (a throwaway process for `server/discover`, SIGTERM + stdin EOF ≈ 5 ms after the answer; the session on a second process with fully namespaced `_meta`); `tools/call` `_meta` carries `progressToken` and `claudecode/toolUseId` in both eras; ids include the integer `0` and long strings; session teardown SIGINT → SIGTERM (+≈ 100 ms) with stdin open, an inferred SIGKILL ≈ 0.5–0.75 s later if still alive; a dead stdio server is **respawned automatically** (≈ 240 ms) with `tools/list` reused; the client's docs say otherwise.

## 4. Remaining work, in order (packet §6 steps 2–5)

1. **Kickoff** per handoff-pattern §0 (`$tinyvault-start`): verify §1, claim ownership with the focus-stamp checkpoint in `PLAN.md` Current State (`owner: codex`, `state: active`, state checkout, worktree). One writer.
2. **Implementation, one worktree, single writer, sequential slices** — branch `codex/m8-mcp-adapter` from `36aeb54` (or the wrapup tip):
   - **Slice A** (`gpt-5.6-sol` worker, or the owner session for a small agreed slice): `src/adapters/mcp/protocol.ts`, T-WIRE, T-STDOUT (packet §4.2, §3.4, §3.9).
   - **Slice B** (Astra-owned worker, security core): the two gate amendments with their named self-test edits (§3.2, §4.1), `tools.ts`/`server.ts`/`main.ts` (`createServer(host)` / `serve(server, input, output)` split; `main.ts` the only `process.*` file; `createSupervisedHost({backend, canary})` exactly once), §4.6 items 1–2, T-SEAM/T-SCHEMA/T-ARGS/T-ERR/T-MUTEX/T-LIFE/T-HOST/T-STRUCT/T-STDIO, capability rows (§4.7), the docs patches as text (§4.5).
   - Give each worker explicit file ownership, base/head, the §5 leave-alone list and STOP triggers, tests, and the §13 return format; "leave the work uncommitted; the owner commits with explicit paths"; the Codex sandbox cannot launch Chromium, bind loopback or `mkdtemp` — mark those tests "Not run: sandbox" and run them yourself.
3. **Owner integration commit** (explicit paths): the `build:mcp` script and `mcp` Makefile target with the two `testbed/rootOfTrust.test.ts:15-16` digests (O-5), the `SCHEMA.md` section (same commit as slice B), **`src/core/fillService.structure.test.ts:595-596` `HOST_CALL_FILES` gains `'src/adapters/mcp/main.ts'`** (§4.6 item 3 — the only permitted `src/core/**` edit), README/ORIENT/BACKLOG sentences incl. the verbatim limitation beside the setup instructions.
4. **Owner gates on the exact candidate, real checkout, quiet host, results read from logs, never chained behind a commit:** `make test` (T-STDIO included), `make test-docker` (7/7, unchanged), `make eval-stub` (unchanged), both gate scripts and both self-tests, the mutant tables (T-SEAM M-1…M-4, M-5, M-6, T-STDOUT, T-MUTEX part 2, T-STRUCT's local-tool-map mutant, T-HOST's three, the docker-invocation self-test mutants), every T-WIRE fixture header verified against the fetched pages under `artifacts/review-evidence/tinyvault-m8-rev5-20260911/spec/`, a literal clean clone green. **Then the final interoperability check** of §2 item 3 — scripted driver first (no spend); the installed-client runs only after the user grants a provider-spend allowance. Preserve every log under a new `artifacts/review-evidence/tinyvault-m8-impl-<date>/`.
5. **Post-implementation ladder on the committed candidate** (base and head pinned; hold commits while a review runs): fresh Claude QA and the **security channel** through `scripts/claude-review.mjs` (Opus 5, read-only; standing authorization of 2026-09-05), and a **fresh-context Codex adversarial pass** (Astra default). **Capped at three rounds**; the last round's P1 criteria are packet §6 step 4's: a layers-1–2 leak (a secret or secret-derived byte reachable in any MCP result, stdout line, stderr line or exit code), an undeclared layer-4 blind spot (a successful host payload delivered without passing `createTools`), a red `make test`, a gate amendment that admits a data-plane → supervisor path, or a spawn that escapes the docker-invocation pin. Everything else is a recorded residual. Verify every finding against the tree before disposition; register entries append-only in `docs/m8-review-findings.md`.
6. **Return for merge approval:** the candidate SHA, gate evidence, mutant table, the interoperability record (with the limitation verified), the review dispositions and residuals, deviations from this handoff and from the packet — **no merge, no push.**

## 5. Authorization boundaries (absolute)

No merge; no push (`origin/main` = `bac91db`); no public flip; no additional live cohort or replacement run; no paid client call without an explicit provider-spend allowance; no change to `src/core/**`, `src/browser/**`, `src/backends/**`, `src/shared/**`, `testbed/**`, `SKILL.md`, fixtures or checkers beyond packet §4.6/§3.10/§5's named carve-outs; no re-opening of the paper reviews; cohorts `ODMFYbwH` (unqualified) and `PFc7eGp2` (qualified) stay as recorded; the runtime fill control is "live-qualified in that fixture and configuration" only; Console reconciliation for both cohorts stays on the user's side. The Claude session's user-global memory is not yours to edit; project memory under `.claude/memory/` is shared and yours to append.

## 6. Where things are

| Item | Path |
|---|---|
| Locked packet | `docs/m8-mcp-adapter-packet.md` (rev 5.2) |
| Register (append-only) | `docs/m8-review-findings.md` |
| Runtime-control requirements carried | `docs/m7-runtime-fill-control-packet.md` §6.6, §6.7, §8.3, §9 |
| Ladder and ownership protocol | `docs/handoff-pattern.md` §0, §4, §5, §7.1, §13 |
| Claude review dispatch | `.agents/skills/tinyvault-claude-review/SKILL.md`, `scripts/claude-review.mjs`, `templates/claude-review-packet.md` |
| Codex gotchas (sandbox, job monitoring, classifier) | `.claude/memory/gotchas_codex.md` |
| Runtime gotchas incl. the measured stdio-client facts | `.claude/memory/gotchas_runtime.md` ("Standalone entry points and MCP") |
| Probe artifacts and fetched spec pages | `artifacts/review-evidence/tinyvault-m8-rev5-20260911/` |
