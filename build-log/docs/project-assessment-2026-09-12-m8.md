# M8 close assessment — 2026-09-12

M8 is accepted on merged source **`a40bbd65d443cdd22359ad5265078059bb51eaa7`**, including the separately approved M6 finalization witness repair. This document records the fresh independent assessment, owner verification and subsequent closure evidence. It does not change the locked packet, accepted residuals or closed three-round M8 implementation cap. M9 and M10 remain future work; no public release or new cohort is authorized.

## Independent assessment and provenance

Fresh read-only **Claude Opus 5 / high**, session `bba68536-9934-4eee-8866-cf9af8ca0eb8`, reviewed the frozen merge with digest `0bab5fab0d8ec423c49f3a9c96fbad99ff78797136be80d183f6e938ada41135`. Actual assistant model events and zero permission denials were verified. Raw report: `/tmp/tinyvault-m8-implementation-reviews/m8-close-assessment-a40bbd65/report.md`, with request, summary and events alongside it. The owner keeps these artifacts separate from source; the report is not silently rewritten by this annotation.

**Raw disposition: NEEDS-ATTENTION. No new P1 or P2 code defect.** At its snapshot the merged default gate had passed, Docker/stub had no completed records and closure status documents were deliberately unchanged while jobs ran. Its eight P3 observations are adjudicated below. The independent reviewer did not rerun gates or mutants. **Owner closure disposition: acceptance complete**, after reading the completed native gate logs and correcting current documentation. This is an owner resolution of the reported prerequisites, not a new independent PASS report.

The owner verified the delivery crosswalk against `src/adapters/mcp/{main,server,tools,protocol}.ts`, the named seam, host, lifecycle, structural and error tests, host composition and gate source: nine tools with seven frozen registry objects; successful payloads traverse `host.tools`; exactly one host and authorization domain per process; the locked dual-era envelopes and metadata choices; fixed errors and rejection before dispatch; finalization ordering and exit priority; and the confined production entry point. The renderer-retained M6 promise uses actual CDP work and keeps every prior assertion. These checks corroborate delivery within the declared limits; they do not prove general absence of vulnerabilities.

## Exact-SHA acceptance evidence

Evidence root: `artifacts/review-evidence/tinyvault-m8-impl-20260912/`. Full timestamps and native reports: `evidence-index-a40bbd65.md`, `owner-267caa23-gates.json`, `owner-a40bbd65-gates.json` and adjacent logs.

| Scope | Result |
|---|---|
| Corrected repair candidate `267caa23163631de170783837f2b14ea731657be` | Full `make test` PASS: 3516 passed, 0 failed, 1 intentional offline-eval skip; serial timing 5/5 + 26/26. Docker 7/7, both metric sets complete; stub 1/1. All execution guards PASS. |
| Literal independent clone of `267caa2` | Full `make test` PASS with the same counts, 21:17:33.558–21:30:18.887 UTC. `git clone --no-local`, independent installed dependencies, no object alternates; provenance/result in `clone-267caa23-{provenance,result}.json`. |
| Merge `a40bbd65` | Exact candidate tree `e22ff2734980dd90d8dfe5bd0a1ec05adba6720a`, no conflicts. `make test` PASS 21:32:48.785–21:45:23.591 UTC, same counts and timing families. Docker PASS 21:45:23.591–22:03:28.950 UTC, 7/7 and both metric sets complete. Stub PASS 22:03:28.950–22:06:43.719 UTC, 1/1. All execution guards PASS. |
| Original M8 `38efe85c4e56b4a371a6670f7225735a89162404` | Candidate/clone gates, 25 assertion-killed/restored mutants and restored baseline 171/171 PASS; final Claude QA, Claude security and fresh Codex adversarial channels PASS under the locked final-round P1 criteria. See `evidence-index-38efe85c.md` and `mutant-table-38efe85c.md`. |
| Separate M6 repair mutation | **One distinct deletion mutant, 18,000 ms timeout kill**, not an assertion kill. Deletes quiesce-before-drain in `testbed/runnerExecution.ts`; production restored to SHA-256 `a908e6258adeadd82845b8e70b750211a50f7f2f2dce5d55ede9f971ad1b6d01`. Executed on `265def0`; executable-AST identity to corrected `267caa2` recorded in `m6-witness-repair/compact-ast-identity.json`. Final verbose log is `order-mutant-final.log`. Earlier attempts remain preserved and do not count as additional distinct mutants. |

The clone was run on **267caa2**, the 25 original M8 mutants and scripted interop on **38efe85c**, and merged gates on **a40bbd65**. Identity carries evidence between these scopes; it is not a claim that every check reran on the merge. `m6-witness-repair/merge-identity.json` and `source-identity-38efe85c-a40bbd65.json` establish identical candidate/merge trees and unchanged runtime production bytes since38efe85c. The final closure commit changes documentation only.

Earlier reds are retained: merge412b6687 reached the M6 witness's no-login positive-control error instead of its strict capture-failure error; three fixed isolated diagnostic passes did not clear it. Repair265def0 passed that browser witness but failed the unchanged `<800` line cap. The correction only compacted comment/format/trailing-comma layout to798 split lines, retaining the executable AST. Full corrected-candidate, clone and merged acceptance subsequently passed; no skip, retry-to-green or gate threshold change was introduced.

Fresh scoped repair QA on265def0 was NEEDS-ATTENTION for the introduced cap violation. Corrected267caa2 QA **PASS**, session `5b4182ae-cf6e-4144-b8e4-53b3b20086f0`, Opus5/high, digest matching this merge. Owner verified all assertions, line count, AST identity and actual model events. This two-step bounded repair review is separate from the closed M8 ladder. The first review's partial required reading was disclosed; the correction reviewer completed its required reading. Its correction of a prior PLAN line citation is bookkeeping, not a code finding.

## Owner dispositions of the eight P3 findings

| Finding | Verification and disposition |
|---|---|
| P3-1: README status contradicts its milestone table | Confirmed against pre-closure lines33/53. Both now identify merged/gated M8 and the separate repair; no stale merge-approval or red-gate status remains in those current sections. |
| P3-2: mandatory status reconciliation pending | Confirmed in PLAN, root README, docs index and phase-plan build status/M8 row. Corrected after all gate/review jobs completed. Historical register entries retain their original SHAs and outcomes. |
| P3-3: ORIENT drift | Confirmed. Updated M8 completion, E8c's bounded qualification with E8b unchanged, Node24 and five-cell commands (including baseline), and latest assessment link. Also aligned its owner/model summary with existing AGENTS/§0 and CLAUDE routing; no policy changed. |
| P3-4: spec says three tools, unchecked checklist, missing WebMCP positioning | The three **vault** tools plus six browser controls are already approved by M8 O-6, so this is descriptive reconciliation, not a new contract expansion. Clarified that mapping and marked only the authorized MCP checkbox complete. Older unchecked boxes are explicitly not a reconciled completion ledger. The missing WebMCP sentence remains M10 work; no launch-readiness claim. |
| P3-5: no measured MCP cohort leak rate | Confirmed against the seven-tool evaluated registry and nine-tool MCP surface. README and ORIENT now say explicitly that no cohort leak-rate measurement covers MCP. E8c and interoperability retain different evidentiary purposes. No new cohort implied or authorized. |
| P3-6: mutation wording | Confirmed directly from the verbose log. All new closure records explicitly say **18s timeout kill** for the one M6 deletion mutant; the original25 M8 assertion kills remain separate. |
| P3-7: cap headroom | Counts verified: structural test799 against `<800`, M6 witness798 against `<800`, MCP protocol397 against `<400`. The reviewer overstated addable headroom for the first two: **zero, one and two** additional lines respectively fit. Existing BACKLOG split input updated; no refactor now. The report's broader count of cap-related milestone reds is not adopted as an independently verified inventory. |
| P3-8: duplicated fixtures | Owner byte comparison confirmed identical49-line preambles at lines9–57 in `server.seam.test.ts`, `server.host.test.ts`, `tools.errors.test.ts`. Deferred maintainability input recorded in BACKLOG; no test refactor or loss of independent assertions. |

The minor ledger A13 two-key literal is superseded by approved **C5 at packet line232** (the raw assessment's line375 citation is incorrect). The locked packet remains unchanged. This is a citation correction, not an additional exception.

## Interoperability and claim limits

The zero-spend scripted driver used the real built bundle on38efe85c and a throwaway vault: first fill succeeded; same-process reuse, including a fresh control and setup request, remained exhausted; wrong origin was refused; restarting with the same vault/handle allowed another fill. Two authorized receipts, both process exits0, no tested secret-transform hits. Evidence: `interop-38efe85c-result.json`; bundle SHA-256 `5dd96d110393da69e665843d4b5629e88e5154c73358056df24c987f7ac2c2da`. Runtime byte identity carries that evidence through this test-only repair; it was not rerun at the merge.

Process recreation grants fresh fill authorization; the adapter does not establish renewal isolation in the tested Claude Code configuration (2.1.258, measured 2026-09-12).

That exact sentence remains beside both setup blocks. The installed-client legacy/modern evidence remains historical on234c2f2, two authorized calls costing$0.01255. **Not run:** final-source paid installed-client confirmation; the two-call allowance is consumed and the extension unanswered. Independent reviewer calls use their separate standing authorization. No further provider-client call occurred.

E8c `PFc7eGp2` remains qualified only in its evaluated fixture/configuration; E8b `ODMFYbwH` remains unqualified. Both archives were rehashed after merged gates: E8b `e60819025d1632716a4ff79fd50d9d829dd3b3f708413877fe652421708cd8a6`; E8c `cdcc4958128c1d15ac4ddcf43be0a03f5550dd48aa7769c3c29e94db5cd09398`. No historical cohort was changed.

Accepted limits remain: evaluator-alias verdict movement without admitting data-plane→supervisor reach; first-flush listener retention demonstrated at the API boundary, not actual main finalization timing; unbounded output/history/trusted work/final flush; finite unfuzzed framing; unchanged snapshot exemption and compromised-origin boundary; static hygiene rather than containment; non-relocatable/unscanned dist; Darwin-only MCP descendant proof; no suite-resident real-fill scan, subprocess cancellation/backpressure regression or default-manifest isolation; optional metadata accepted uninspected and `-32021` never emitted with stated conformance uncertainty; no per-caller entitlement/renewal audit or production credential-reference/verdict consumer. The M6 witness does not attribute which pending source held it open, and committed navigation can destroy its execution context and produce an environment-dependent red. It fails closed; preserve a future red rather than retrying it away.

## Reading limits and Deviations From Handoff

The close reviewer read the four adapter production files, listed host/seam/lifecycle/error/structural tests and relevant gate/contract hunks, but did **not** read the entire rev5.2 packet end-to-end, the full phase plan, six wire/stdout/mutex/args/schema/selftest files line by line, the old M6 close assessment or all of BACKLOG. Its static crosswalk relies partly on owner mutant/gate evidence. It had no Git execution and did not independently recompute byte identity. These are declared procedural reading limits; owner verification does not turn them into independent execution or complete reading. No fourth M8 implementation review was started to obscure them.

The original no-merge/push boundary was superseded by explicit user approval. The out-of-scope M6 witness red was surfaced before dependent edits and received separate bounded approval. The repair introduced a line-cap red, caught by independent QA and the full gate, then corrected without weakening the gate. A JSON-only mutant-evidence validator initially lacked the timeout text; verbose evidence resolved that reporting failure. All attempts remain archived. Original25-mutant/scripted-interoperability evidence is carried by identity, not rerun on later documentation/test commits. Gates were serial and tracked files frozen while any gate/review ran. Historical sandbox failures were not assumed; required host gates actually ran. Documentation closure is recorded separately from the exact reviewed/gated source. No new paid client call, live cohort, public flip or M9 implementation.
