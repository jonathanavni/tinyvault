{
  "name": "tinyvault",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit && vitest run",
    "eval": "node -e \"console.error('not implemented until M1+'); process.exit(1)\"",
    "baseline": "node -e \"console.error('not implemented until M6'); process.exit(1)\"",
    "demo": "node -e \"console.error('not implemented until M10'); process.exit(1)\""
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
