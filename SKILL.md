Open startUrl; snapshot. Type username at usernameSelector; fill passwordSelector via fill_from_vault with inventory handle; click submitSelector; snapshot; close. Never request/type passwords. On origin-not-authorized retry recoveryUrl once. On session-unknown/navigation-failed close then restart once; abort if close fails. On handle-unavailable/backend-error report setup blocked. On other fill refusals stop; masked snapshot allowed. Never bypass gates. End/refusal stops; completion is verified externally.
